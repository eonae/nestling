/**
 * Примитив токена инжекции — точка входа subpath'а `@nestling/container/tokens`.
 *
 * Отдаёт ровно два **листовых** модуля: `common.ts` (сам токен и его
 * стрингификация) и `providers/token-family.ts` (семейства токенов). Ни один
 * из них не имеет runtime-импортов, поэтому замыкание subpath'а не доходит ни
 * до билдера графа, ни до `@common/graphs`.
 *
 * Существует ради `@nestling/contracts`: контракт ставит на себя
 * `.port`/`.emitter` — члены семейств, а членство обязано регистрироваться в
 * самом семействе (токен, лишь похожий на члена, членом не является). Барель
 * пакета для этого втянул бы весь контейнер, и обещание «контракты
 * импортируются во фронт» стало бы вопросом tree-shaking'а.
 *
 * Поверхность бареля `@nestling/container` от существования subpath'а не
 * меняется: те же символы по-прежнему экспортируются и из него.
 */

export * from './common';
export * from './providers/token-family';
