# contract-declarations

## Purpose

Контракт — направление-нейтральное значение, описывающее операцию между
фичами: имя-адрес, вид (`request` / `command` / `event`), io-формы входа и
выхода, объявленные отказы. Он ничего не регистрирует сам по себе: на
приложение контракт влияет только через реализацию (`implement`) и через
инжект вызывателя (`.port` / `.emitter`). Имя контракта — ключ идентичности
и одновременно subject шины, поэтому уникально в процессе.

## Requirements

### Requirement: Контракт — направление-нейтральное значение с тремя видами

`@nestling/ports` SHALL экспортировать
`makeContract({ name, kind, input?, output?, errors? })`, возвращающий
значение-контракт. Значение SHALL быть неизменяемым и SHALL NOT
регистрироваться ни в модуле, ни в приложении: на приложение контракт влияет
только через `implement(...)` (реализация) и через инжект вызывателя
(потребление).

Поле `kind` SHALL принимать ровно три значения:

- `request` — request-reply, Fail-able, ровно один владелец;
- `command` — fire-and-forget, ровно один обработчик (реплики делят нагрузку);
- `event` — broadcast-факт, 0..N подписчиков.

Поле `name` SHALL быть непустой строкой и SHALL быть **адресом**: оно же
subject шины, оно же ключ дискавери. Версия контракта SHALL выражаться частью
имени (`user.create.v2`) — отдельного поля версии SHALL NOT существовать.

Поля `input`/`output` SHALL принимать io-формы ядра (значение-схема и
обёртки форм), `errors:` — список определений `defineFail` с теми же
проверками, что и в словаре endpoint-декларации (элемент, не являющийся
определением, и повторяющийся `code` — ошибка в точке создания).

#### Scenario: Объявление request-контракта

- **WHEN** объявлено `makeContract({ name: 'billing.charge', kind: 'request', input: ChargeInput, output: ChargeResult, errors: [CardDeclined] })`
- **THEN** результат — значение с этими полями, ничего не зарегистрировавшее
  ни в одном модуле

#### Scenario: Вид вне словаря отвергается

- **WHEN** объявлено `makeContract({ name: 'x', kind: 'query' })`
- **THEN** вызов бросает ошибку, называя контракт и три допустимых вида

#### Scenario: Пустое имя отвергается

- **WHEN** объявлено `makeContract({ name: '', kind: 'event' })`
- **THEN** вызов бросает ошибку в точке создания

#### Scenario: Дубль кода в `errors:`

- **WHEN** в `errors:` контракта дважды указан отказ с кодом `CARD_DECLINED`
- **THEN** вызов бросает ошибку, называя контракт и код

### Requirement: Имя контракта — ключ идентичности

`makeContract` SHALL вести приватный реестр «имя → контракт». Повторное
объявление контракта с уже занятым именем SHALL быть ошибкой в точке
создания; текст SHALL называть имя и объяснять, что имя контракта — адрес,
а значит уникально.

Реестр SHALL быть приватным для пакета (наружу не экспортируется) и SHALL
использоваться только для сопоставления «член семейства вызывателей →
контракт». Реестр SHALL NOT быть источником истины о составе приложения:
на вопрос «что обслуживается» SHALL отвечать дискавери из дерева модулей.

#### Scenario: Два контракта с одним именем

- **WHEN** объявлены два разных `makeContract({ name: 'billing.charge', … })`
- **THEN** второй вызов бросает ошибку с именем контракта

#### Scenario: Реестр не влияет на состав приложения

- **WHEN** пакет с контрактами импортирован, но ни один контракт не
  реализован и не инжектирован
- **THEN** приложение собирается ровно как без импорта: узлов графа,
  маршрутов и требуемых транспортов не прибавляется

### Requirement: Вызыватели контракта доступны по его виду

Контракт SHALL нести свойство `port` для вида `request` и свойство `emitter`
для видов `command` и `event`. Каждое из них SHALL быть **токеном** —
членом соответствующего token-семейства, параметризованного именем контракта,
— пригодным для `deps:` провайдера, декларации и `@Injectable`.

Обращение к `.port` у контракта вида `command`/`event` и к `.emitter` у
`request` SHALL быть ошибкой компиляции (свойства нет в типе) и SHALL
дублироваться рантайм-ошибкой с именем контракта и его видом — для
JS-потребителей.

Тип вызывателя SHALL выводиться из контракта: `Port<typeof C>` с
`call(input, meta?)` и `Emitter<typeof C>` с `emit(payload, meta?)`.

#### Scenario: Порт инжектится как обычная зависимость

- **WHEN** провайдер объявляет `deps: [ChargeCard.port]`
- **THEN** он получает значение типа `Port<typeof ChargeCard>`, и никаких
  дополнительных регистраций в корне не требуется

#### Scenario: Эмиттер у события

- **WHEN** объявлен `makeContract({ name: 'orders.placed', kind: 'event', input: OrderPlaced })`
- **THEN** у значения есть `.emitter` и нет `.port`

#### Scenario: Обращение к порту события в JS

- **WHEN** JS-код читает `OrderPlaced.port`
- **THEN** он получает ошибку, называющую контракт, его вид и правильное
  свойство
