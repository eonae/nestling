## 1. Переключатели в `@nestling/container`

- [ ] 1.1 Завести `src/switches` со своим баррелем: типы `Switch`,
  `SwitchBranch`, `SwitchValues`, бренд ветки
- [ ] 1.2 Реализовать `makeSwitch(name, values?, options?)` в четырёх формах;
  проверки имени, списка значений и умолчания при создании
- [ ] 1.3 Реализовать `pick(table)` у перечисления и `pick({ on, off })`
  с `when(x)` у двухпозиционного; типом требовать полноты таблицы
- [ ] 1.4 Реализовать `Switch.schema` — Standard Schema значений с
  умолчанием; добавить `@standard-schema/spec` в зависимости пакета
- [ ] 1.5 Реализовать `resolveBranches(items, values)`: раскрытие ветки,
  вложенной ветки и пустой ветки
- [ ] 1.6 Принять значения переключателей опцией `ContainerBuilder`;
  раскрывать `providers:` и `dependsOn:` при регистрации модуля, не копируя
  его значение
- [ ] 1.7 Ошибка регистрации ветки без переданных значений: сообщение
  называет переключатель и опцию `switches`
- [ ] 1.8 Расширить типы `Module.providers` и `Module.dependsOn` веткой
- [ ] 1.9 Рантайм-тесты: формы конструктора, полнота таблицы, раскрытие,
  схема, ошибки; type-тесты на неполную таблицу и на ветку не в своей
  позиции

## 2. Формы корня в `@nestling/app`

- [ ] 2.1 Переписать `AppSpec` тремя взаимоисключающими формами:
  `{ endpoints, providers? }`, `{ endpoints, modules? }`, `{ features }`
- [ ] 2.2 Добавить поле `switches:` и проверку дублей имён переключателей
  при создании декларации
- [ ] 2.3 Нормализовать корень первых двух форм во внутреннюю единицу с
  ролью фичи и именем `app`
- [ ] 2.4 Отвергнуть `providers:` рядом с `features:` — типом и сообщением
  рантайма, называющим плагин
- [ ] 2.5 Обновить перечень полей и сообщения о неизвестном поле словаря
- [ ] 2.6 Рантайм-тесты форм корня: обе плоские формы, форма с фичами,
  смешанная запись, атрибуция endpoint'ов и провайдеров именем `app`

## 3. Аргумент сборки и раскрытие веток

- [ ] 3.1 Ввести тип `AssembleArgs`, выводимый из `switches:`; сделать
  `makeApp` generic по списку переключателей
- [ ] 3.2 Разобрать аргумент сборки: выбор фич, `includeDeps`, значения
  переключателей; отвергнуть неизвестное поле перечнем известных
- [ ] 3.3 Построить карту значений из аргумента и умолчаний; ошибки
  «значение не из словаря» и «значение не передано»
- [ ] 3.4 Отвергнуть `pick` на переключателе вне `switches:` сообщением с
  именем переключателя
- [ ] 3.5 Раскрыть ветки списков корня и выбранных единиц до discovery и до
  замыкания `includeDeps`
- [ ] 3.6 Запретить переключатели с именами `features` и `includeDeps`
- [ ] 3.7 Рантайм-тесты: все четыре ошибки ASSEMBLE, порядок «раскрытие до
  замыкания», строковая форма с умолчаниями, объектная форма целиком из
  `load(RootConfig)`
- [ ] 3.8 Type-тесты: обязательность поля без умолчания, опциональность
  поля с умолчанием, отказ на лишнем поле

## 4. Отчёт, строка старта и тестовый корень

- [ ] 4.1 Добавить поле `switches` в `CheckReport`; заполнить его в
  порядке объявления
- [ ] 4.2 Печатать выбор в строке состава на старте рядом с фичами
- [ ] 4.3 Переименовать опцию `assembleTest` из `select` в `args` и
  принять объектную форму
- [ ] 4.4 Принять аргументы сборки списком в `checkTopologies`
- [ ] 4.5 Рантайм-тесты: отчёт с измерением, строка старта, матрица по
  двум веткам, тестовый корень с веткой

## 5. Примеры

- [ ] 5.1 `users-service`: перевести корень на форму с фичами без
  `providers:`, если она использовалась
- [ ] 5.2 `app-with-http`: добавить переключатель к одному из плагинов и
  прогнать матрицу топологий по его веткам
- [ ] 5.3 `split-nats`: перевести `assemble(cfg.features)` на объектный
  аргумент сборки
- [ ] 5.4 Прогнать тесты примеров и проверить, что `yarn verify` зелёный

## 6. Документация пакетов и дизайна

- [ ] 6.1 Сверить `docs/design/composition.md` §2–3 с реализацией:
  формы корня, переключатели, аргумент сборки, строка старта
- [ ] 6.2 Обновить README `@nestling/container`: переключатели в
  справочнике API и в плашке статуса
- [ ] 6.3 Обновить README `@nestling/app`: три формы корня, `switches:`,
  аргумент сборки
- [ ] 6.4 Обновить README `@nestling/testing`: опция `args`,
  `checkTopologies` по веткам
- [ ] 6.5 Обновить `docs/glossary.md`: переключатель состава, ветка,
  аргумент сборки

## 7. Гайд

- [ ] 7.1 Переименовать главы `02`–`25` в `03`–`26` и починить ссылки
  внутри глав, в `docs/guide/README.md` и `docs/README.md`
- [ ] 7.2 Написать новую главу 2 «Из чего состоит приложение»: endpoint'ы и
  провайдеры корня, модули, фичи, переключатели, split
- [ ] 7.3 Переписать главу 1 на форму `{ endpoints, transports }` без
  `makeFeature`
- [ ] 7.4 Перевести главы 2 и 5 (новые 3 и 6) на шаг-форму: анонимный
  `httpEndpoint` без операции и без пайплайна
- [ ] 7.5 Дописать в главу о выборе фич (новая 17) раздел о переключателях
  и матрице топологий
- [ ] 7.6 Обновить карту понятий в `docs/guide/README.md` номерами глав и
  строкой о переключателе
- [ ] 7.7 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
  изменённым текстам

## 8. Definition of Done

- [ ] 8.1 Все задачи выше отмечены
- [ ] 8.2 `yarn verify` зелёный: `build`, `typecheck`, `lint`, `test` и
  `type-budget` по всем пакетам
- [ ] 8.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 8.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`; статус change'а 47 в `roadmap.md` обновлён
- [ ] 8.5 `yarn docs:audit` — 0 ERROR
- [ ] 8.6 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [ ] 8.7 Коммиты осмысленные, ветка `change/switches` запушена
