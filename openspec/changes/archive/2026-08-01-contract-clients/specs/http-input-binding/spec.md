## MODIFIED Requirements

### Requirement: Пометки `query()` и `body()` — значения в `bind`-записи словаря

HTTP-словарь SHALL принимать необязательное поле `bind` — запись
«имя поля → пометка». Пометки SHALL быть значениями, создаваемыми
экспортируемыми конструкторами `query(options?)` и `body()`; их дом —
`@nestling/operations` (пометка нужна и контракту, и декларации), а
`@nestling/transport.http` SHALL реэкспортировать их, чтобы автор
HTTP-декларации брал их оттуда же, откуда `httpEndpoint`. Строковая форма
записи места SHALL NOT приниматься. `query({ multiple: true })` SHALL
объявлять поле-массив.

Ключи `bind` SHALL быть типизированы полями схемы `input` за вычетом
path-параметров шаблона: неизвестное имя поля и имя path-параметра SHALL
быть ошибками компиляции.

#### Scenario: Поле вытянуто в query из тела

- **WHEN** объявлено `httpEndpoint({ method: 'POST', path: '/users', input: CreateUser, bind: { dryRun: query() }, … })`
- **THEN** `dryRun` читается из query-строки, остальные поля — из тела

#### Scenario: Опечатка в имени поля не компилируется

- **WHEN** в `bind` указано имя, отсутствующее в схеме `input`
- **THEN** это ошибка компиляции

#### Scenario: Пометка на path-параметре не компилируется

- **WHEN** шаблон содержит `:id`, а в `bind` указано `{ id: query() }`
- **THEN** это ошибка компиляции, а при вызове из JS — ошибка в момент
  создания декларации

#### Scenario: Одна пометка на оба места объявления

- **WHEN** `query()` импортирован из `@nestling/operations` и использован в
  секции `http:` контракта, а из `@nestling/transport.http` — в словаре
  `httpEndpoint`
- **THEN** это одно и то же значение-пометка, и правило размещения
  применяется одинаково

### Requirement: Пометки разворачиваются в плоскую bind-карту при создании декларации

Канон и пометки SHALL разворачиваться в плоскую bind-карту **в момент
создания значения** — не при регистрации в приложении. Носителем значения
SHALL быть либо декларация endpoint'а (конструктор транспорта), либо
контракт с секцией `http:` (`makeContract`, capability
`contract-http-binding`). Правило размещения SHALL быть одной реализацией на
оба места вызова.

Карта SHALL быть доступна с несущего значения и SHALL содержать: явные
размещения (path-параметры шаблона и помеченные поля), правило для остальных
полей (`query` или `body`), метод, шаблон пути и признак `rawBody`.

Карта SHALL быть единственным носителем размещения для всех потребителей:
транспорта, генератора документации и типизированного клиента. Ядро
(`@nestling/pipeline`) SHALL переносить её на значение декларации, не
интерпретируя.

Декларация, построенная контракт-формой конструктора, SHALL брать карту с
контракта и SHALL NOT вычислять её повторно.

#### Scenario: Карта читается из одного импорта декларации

- **WHEN** значение декларации импортировано без запуска приложения и без
  серверного кода
- **THEN** bind-карта доступна с этого значения и описывает размещение
  path-параметров, помеченных полей и правило для остальных

#### Scenario: Карта читается из одного импорта контракта

- **WHEN** значение контракта с `http:` импортировано внешним потребителем —
  без серверного кода
- **THEN** bind-карта доступна с контракта и описывает то же размещение

#### Scenario: Регистрация в приложении карту не меняет

- **WHEN** та же декларация объявлена в `endpoints:` модуля и приложение
  поднято
- **THEN** транспорт использует ту же карту, что была вычислена при
  создании значения

#### Scenario: Ядро не интерпретирует носитель

- **WHEN** декларация создана kernel-примитивом `makeEndpoint` с полем
  `binding`
- **THEN** значение несёт это поле без изменений, а `@nestling/pipeline`
  не содержит понятий `path`/`query`/`body`

#### Scenario: Контракт и его реализация делят одну карту

- **WHEN** контракт с `http:` реализован контракт-формой `httpEndpoint`
- **THEN** декларация несёт ту же карту, что и контракт, — тем же значением

## ADDED Requirements

### Requirement: Клиент собирает запрос по той же карте, что транспорт его разбирает

Bind-карта SHALL обслуживать обе стороны провода: HTTP-транспорт SHALL
собирать по ней payload из запроса (strict-приём), а типизированный клиент
SHALL раскладывать по ней payload в запрос. Правила и коерсия клиентской
стороны — capability `typed-http-client`.

Инвариант SHALL держаться: для любого payload, представимого на проводе,
разбор запроса, собранного клиентом, SHALL давать исходный payload. Он
SHALL проверяться round-trip-тестом на общем наборе карт, а не выводиться
из совпадения реализаций.

#### Scenario: Round-trip по одной карте

- **WHEN** клиент собрал запрос по карте контракта, а транспорт разобрал
  его по той же карте
- **THEN** полученный payload равен исходному

#### Scenario: Массив query переживает round-trip

- **WHEN** поле помечено `query({ multiple: true })` и содержит `['a']`
- **THEN** клиент отправляет одно вхождение ключа, а транспорт возвращает
  `['a']`, а не `'a'`
