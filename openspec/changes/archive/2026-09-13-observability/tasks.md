## 1. Переменная `Trace` и юнит `withTracing()`

- [x] 1.1 Объявить тип `TraceContext` и well-known переменную `Trace` в
  `pipeline/core/context/well-known.ts` с флагом `{ propagate: true }`
- [x] 1.2 Зарезервировать ключ `'trace'` там же, где зарезервированы
  `'signal'` и `'requestId'`, с текстом ошибки, называющим `Trace`
- [x] 1.3 Написать разбор и сборку строки W3C trace-context: 32 знака
  `traceId`, 16 знаков `spanId`, флаг `sampled`; неизвестный вид даёт
  `undefined`, а не исключение
- [x] 1.4 Реализовать `withTracing()` через `Trace.provide(…)`: источник
  родителя — поле `trace` атрибутов, затем заголовок `traceparent`, затем
  новая трасса
- [x] 1.5 Экспортировать `Trace`, `TraceContext` и `withTracing` из
  `@nestlingjs/app`
- [x] 1.6 Рантайм-тесты: продолжение трассы по заголовку, продолжение по
  атрибуту шины, новая трасса, непонятный заголовок, `parentSpanId`
  предыдущего участка
- [x] 1.7 Рантайм-тест политики `everyEndpoint(…).hasVar(Trace)`

## 2. `traceId` в записях логгера

- [x] 2.1 Добавить `ambientTraceId()` рядом с `ambientRequestId()` в
  `pipeline/core/context/reader.ts`
- [x] 2.2 Научить `ConsoleLogger` добавлять поле `traceId`, если трасса
  есть и поле не задано вызовом
- [x] 2.3 Рантайм-тесты: поле внутри трассируемого запроса, отсутствие поля
  вне запроса, значение вызова сильнее ambient

## 3. Интерфейс `Metrics`, корень и семейство

- [x] 3.1 Создать подсистему `packages/nestling.app/src/metrics/`:
  `interface.ts` (`Metrics`, `MetricAttributes`), `noop.ts`, `tokens.ts`
  (`RootMetrics$`, `Metrics$`), `kernel.ts`, `index.ts`
- [x] 3.2 Реализовать рецепт семейства: член `Metrics$(scope)` добавляет
  атрибут `scope`, `Metrics$.auto` даёт члена по имени класса
- [x] 3.3 Добавить опцию `metrics` в `root/plan.ts` и `root/app.ts` по
  образцу опции `logger`
- [x] 3.4 Сделать провайдер под `RootMetrics$` ошибкой дубля с текстом,
  называющим опцию `metrics`
- [x] 3.5 Зарегистрировать kernel-модуль метрик в корне безусловно
- [x] 3.6 Экспортировать `Metrics`, `MetricAttributes`, `RootMetrics$` и
  `Metrics$` из `@nestlingjs/app`
- [x] 3.7 Рантайм-тесты: член добавляет `scope`, `.auto` даёт члена по
  имени класса, провайдер под корнем падает, подмена в `overrides` работает

## 4. Инструментовка endpoint'ов

- [x] 4.1 Добавить `metrics?: Metrics` в `ExecuteOptions` и
  `MakeDispatchOptions`; заполнять поле только при заданной опции
  `makeApp({ metrics })`
- [x] 4.2 Писать `nestling.requests` и `nestling.request.duration` в
  `executeWithHandler` рядом с вычислением `outcome`
- [x] 4.3 Провести запись через путь потокового ответа: метрика появляется
  после закрытия итератора, вместе с `.finally`
- [x] 4.4 Рантайм-тесты: успех, отказ, разрыв соединения, отмена, поток;
  `pattern` равен шаблону маршрута, а не адресу запроса
- [x] 4.5 Рантайм-тест: без опции `metrics` ни один метод не вызван

## 5. Инструментовка вызовов порта

- [x] 5.1 Обернуть узел порта в `ports/kernel.ts` в обеих точках выбора
  биндинга: одна обёртка покрывает все четыре формы вызывателя
- [x] 5.2 Писать `nestling.port.calls` и `nestling.port.duration` с
  атрибутами `operation`, `kind`, `binding`, `outcome`
- [x] 5.3 Рантайм-тесты: `local` и `remote` дают одинаковый набор метрик,
  `emit` учитывается, отказ даёт `outcome: 'failed'`
- [x] 5.4 Рантайм-тест: локальный вызов даёт и метрику порта, и метрику
  endpoint'а реализации

## 6. Клиент и тестовый пакет

- [x] 6.1 Добавить в `@nestlingjs/client` опцию `trace` и заголовок
  `traceparent` из неё; заголовок конфигурации сильнее. Читалку
  `traceparent()` экспортирует `@nestlingjs/app`
- [x] 6.2 Рантайм-тесты клиента: трасса продолжается, явный заголовок
  сильнее, вне запроса заголовка нет
- [x] 6.3 Реализовать `spyMetrics()` в `@nestlingjs/testing` по образцу
  `spyLogger()` и экспортировать его
- [x] 6.4 Рантайм-тесты: записи сервиса через подмену корня, метрики ядра
  видны тесту

## 7. Горячий путь

- [x] 7.1 Прогнать бенч под Node 24 до правок и записать результат
- [x] 7.2 Прогнать бенч после правок на приложении без опции `metrics`:
  расхождение с бюджетом `hot-path-trim` объяснено или отсутствует
- [x] 7.3 Прогнать `yarn type-budget`: бюджет типов не сдвинут

## 8. Пример `split-nats`

- [x] 8.1 Добавить `withTracing()` в базовый слой примера
- [x] 8.2 Написать адаптер `Metrics` с форматом Prometheus и endpoint
  `/metrics` внутри примера, подключить его через `makeApp({ metrics })`
- [x] 8.3 Тест примера: записи двух процессов несут один `traceId`
- [x] 8.4 Проверить, нужен ли `withTracing()` примерам `users-service` и
  `app-with-http` — открытый вопрос 1 `design.md`

## 9. Документация

- [x] 9.1 Обновить `docs/design/pipeline.md`: переменная `Trace`, юнит
  `withTracing()`, запись метрик в ответной фазе
- [x] 9.2 Обновить `docs/design/container.md`, раздел о логгере ядра:
  рядом появляется корень и семейство метрик
- [x] 9.3 Обновить `docs/design/transports.md`: граница переноса трассы —
  шина и HTTP
- [x] 9.4 Добавить в главу 9 гайда раздел о `withTracing()` и поле
  `traceId`, обновить дату в плашке «сверено с кодом»
- [x] 9.5 Добавить в главу 20 раздел о сквозной трассе между процессами,
  обновить дату в плашке
- [x] 9.6 Написать главу 22 о метриках на примере `split-nats`; добавить
  строку в таблицу части 2 и понятия в карту понятий `docs/guide/README.md`
- [x] 9.7 Переименовать иллюстративный переключатель `Metrics` в
  `docs/design/composition.md`, `docs/conventions.md`, `docs/glossary.md` и
  `docs/guide/README.md`: имя занято типом ядра
- [x] 9.8 Добавить в `docs/glossary.md` термины «трасса», «участок трассы»
  и «метрика»
- [x] 9.9 Обновить README пакетов `nestling.app`, `nestling.client` и
  `nestling.testing`, включая плашки статуса и перечень экспортов
- [x] 9.10 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
  всем изменённым текстам

## 10. Закрытие change'а

- [x] 10.1 Поставить пометку «РЕАЛИЗОВАНО» у пункта 2 записи
  [ideas.md [2026-09-12]](../../../docs/decisions/ideas.md) «Разбор обзоров
  d/10 и d/13»: что вышло целиком, что уехало дальше, чем реализация
  уточнила решение
- [x] 10.2 Вычеркнуть открытые вопросы 1 и 2 той же записи: граница
  change'а выбрана, имя сателлита — `otel`
- [x] 10.3 Обновить строку 77 roadmap и добавить строку 85 на change
  `otel-satellite`: настоящая реализация `RootMetrics$`, экспорт спанов,
  плагин с endpoint `/metrics`
- [x] 10.4 Обновить оглавление `ideas.md`
  (`node .claude/skills/docs-audit/scripts/ideas-toc.mjs`)

## 11. Definition of Done

- [x] 11.1 Все задачи разделов 1–10 отмечены
- [x] 11.2 `yarn verify` зелёный
- [x] 11.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 11.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`
- [x] 11.5 Запись `ideas.md`, по которой шёл change, несёт пометку
  «РЕАЛИЗОВАНО»
- [x] 11.6 `yarn docs:audit` — 0 ERROR
- [x] 11.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [x] 11.8 Коммиты осмысленные, `main` не тронут: слияние делает Merger
  после `/opsx:archive`
