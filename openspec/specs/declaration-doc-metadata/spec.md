# declaration-doc-metadata

## Purpose

Метаданные документации, которых нет в схемах: словарь `doc` на endpoint-декларации
и на контракте. Слот транспорт- и формат-нейтрален (тот же `doc` читает будущая
генерация AsyncAPI), закрыт по составу полей и проверяется в момент создания
значения, а не на сборке приложения. Ядро переносит `doc` на значение декларации
и не интерпретирует его: ни один путь исполнения запроса от него не зависит.
В контракт-форме `doc` принадлежит контракту — документация операции часть её
интерфейса, а не реализации; `resolve(...)` переносит `doc` наравне с `binding`,
`errors` и `detached`.

## Requirements

### Requirement: Слот `doc:` несёт метаданные, которых нет в схемах

Словарь endpoint-декларации и словарь `makeContract` SHALL принимать
необязательную секцию `doc` со следующими полями и никакими другими:

- `summary?: string` — однострочное название операции;
- `description?: string` — развёрнутое описание;
- `tags?: readonly string[]` — группировка операции;
- `deprecated?: boolean`;
- `status?: SuccessStatus` — статус успешного ответа из словаря статусов
  ядра;
- `hidden?: string` — причина, по которой операция не попадает в
  документацию.

Слот SHALL быть транспорт- и формат-нейтральным: полей, осмысленных только
для OpenAPI (`operationId`, `externalDocs`, `servers`), в нём SHALL NOT
быть — тот же слот читает будущая генерация AsyncAPI.

Ядро SHALL переносить `doc` на значение декларации и SHALL NOT его
интерпретировать: ни один путь исполнения запроса от него не зависит.

#### Scenario: Декларация с документацией

- **WHEN** объявлено `httpEndpoint({ method: 'GET', path: '/users', doc: { summary: 'List users', tags: ['users'] }, … })`
- **THEN** значение декларации несёт `doc` с этими полями, а исполнение
  запроса не меняется ни в чём

#### Scenario: Контракт с документацией

- **WHEN** объявлено `makeContract({ name: 'users.create', kind: 'request', doc: { summary: 'Create user' }, … })`
- **THEN** значение контракта несёт `doc`, доступный и реализации, и
  внешнему потребителю контракта из того же импорта

### Requirement: Словарь `doc` проверяется в момент создания значения

Конструктор декларации и `makeContract` SHALL отвергать дефектный `doc` в
точке создания, а не на сборке приложения: не-строку в `summary`/
`description`, не-массив строк в `tags`, `status` вне словаря успешных
статусов ядра, `hidden`, не являющийся непустой строкой, и неизвестное
поле секции.

Формы `hidden: true` SHALL NOT существовать: тип поля — строка, а рантайм
SHALL отвергать не-строку и пустую строку. Причина обязательна ровно по
той же логике, что и у `detached`: тотальный opt-out обязан быть читаем в
diff'е.

Текст ошибки SHALL называть ручку (или контракт), поле и требование.

#### Scenario: Пустая причина скрытия

- **WHEN** объявлено `httpEndpoint({ …, doc: { hidden: '' } })`
- **THEN** создание декларации бросает ошибку: причина обязательна

#### Scenario: `hidden: true` отвергается

- **WHEN** JS-потребитель объявил `doc: { hidden: true }`
- **THEN** создание бросает ошибку, называющую требование причины и
  отсутствие булевой формы

#### Scenario: Статус вне словаря

- **WHEN** объявлено `doc: { status: 'PARTIAL_CONTENT' }`
- **THEN** создание бросает ошибку, перечисляющую допустимые успешные
  статусы

#### Scenario: Неизвестное поле секции

- **WHEN** объявлено `doc: { operationId: 'listUsers' }`
- **THEN** создание бросает ошибку: имя операции выводится, а не
  объявляется

### Requirement: В контракт-форме `doc` принадлежит контракту

`doc` SHALL входить в перечень полей, которыми владеет контракт, наравне с
`input`/`output`/`errors`/`bind`: объявление `doc` в
`httpEndpoint({ contract, … })` SHALL быть ошибкой компиляции и ошибкой
рантайма для JS-потребителя. Декларация, построенная из контракта, SHALL
получать `doc` контракта.

Документация операции — часть её интерфейса, а не её реализации: две
реализации одного контракта SHALL NOT описывать его по-разному.

#### Scenario: Переобъявление отвергается

- **WHEN** объявлено `httpEndpoint({ contract: CreateUser, doc: { summary: 'x' }, … })`
- **THEN** создание бросает ошибку: `doc` принадлежит контракту и не может
  быть переобъявлен реализацией

#### Scenario: Документация приезжает с контракта

- **WHEN** контракт `CreateUser` объявлен с `doc: { summary: 'Create user' }`,
  а декларация — контракт-формой
- **THEN** значение декларации несёт тот же `doc`

### Requirement: `doc` переживает гашение зависимостей

`resolve(...)` SHALL переносить `doc` на новую декларацию так же, как
переносит `binding`, `errors` и `detached`: интроспекция исполнимой
декларации SHALL видеть то же, что и интроспекция исходной. Иначе документ,
построенный по погашенным декларациям, отличался бы от построенного по
объявленным.

#### Scenario: Погашенная декларация несёт ту же документацию

- **WHEN** декларация с `deps` и `doc` погашена резолвером
- **THEN** результирующее значение несёт тот же `doc`
