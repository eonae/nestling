## 1. `microservice`

- [x] 1.1 Переименовать `examples/users-service` в `examples/microservice`, имя пакета — `@examples/microservice`; обновить `ci.yml` (шаг `db:migrate`), корневые `README.md` и `README.ru.md`
- [x] 1.2 Перенести из `app-with-http` живую ленту: `events(T)`, `sse:`, `Topic`, отмена по `AbortSignal`
- [x] 1.3 Перенести реестр подписок (`@nestlingjs/subscriptions`) и endpoint списка подписок
- [x] 1.4 Перенести транспорт MCP: инструменты агента на том же сокете, конвертеры общие с документом OpenAPI
- [x] 1.5 Перенести пробы `httpProbes()` и переключатель состава документации
- [x] 1.6 Перенести метрики из `split-nats`: адаптер Prometheus, опция `metrics`, endpoint `/metrics`
- [x] 1.7 Перенести endpoint webhook'а с проверкой подписи (источник рецепта `webhook.md`)
- [x] 1.8 Свести `app.ts`: плагины, транспорты и политики одного сервиса; проверить, что файл читается сверху вниз
- [x] 1.9 Перенести тесты `e2e` (`users-crud`, `files`, `streaming`, `mcp`, `webhook`) и helpers; убедиться, что `test:e2e` поднимает приложение
- [x] 1.10 Написать `examples/microservice/README.md`: что показывает, как поднять, что потрогать
- [x] 1.11 Проверить обвязку: `.env.example`, `docker-compose.yml`, миграции drizzle, скрипты `package.json`

## 2. `modular-app`

- [x] 2.1 Создать `examples/modular-app` из `app-with-http` и `split-nats`: одна декларация приложения, шина NATS с `intercom`, HTTP для проб и метрик
- [x] 2.2 Написать фичу `notifications`: письмо о регистрации, шаблоны, повтор при отказе почты, порт отправки с заглушкой в тестах
- [x] 2.3 Удалить фичу `quotas`; перевести операции и события на пару `users` ↔ `notifications`
- [x] 2.4 Перенести транзакционный emit (`outbox`) и приём (`inbox`) на PostgreSQL: относятся к паре фич, а не к одиночному сервису
- [x] 2.5 Перенести выбор фич и переключатели состава: `assemble(args)`, `includeDeps`, `load()` до сборки, `check()`
- [x] 2.6 Перенести снимок операций и сверку совместимости: `operations.snapshot.json`, скрипт и спека `diffOperations`
- [x] 2.7 Написать `docker-compose.yml` с профилями `mono` и `split`, общие PostgreSQL и NATS; `Dockerfile` приложения
- [x] 2.8 Перенести скрипт `visualize` (граф зависимостей для `@nestlingjs/viz`); обновить `packages/nestling.viz/README.md` и `README.ru.md`
- [x] 2.9 Перенести спеки топологий: `checkTopologies`, изолированный запуск фичи, `stubs` и `testApp.emit`
- [x] 2.10 Написать `examples/modular-app/README.md`, включая запуск обоими профилями

## 3. `cli`

- [x] 3.1 Создать `examples/cli` из `simple-cli`: запуск по аргументам и REPL, вопросы команды из JSON Schema, обработка stdin
- [x] 3.2 Добавить зависимость `@examples/microservice` и построить типизированный клиент из его деклараций операций
- [x] 3.3 Написать команды поверх операций сервиса: создание пользователя, список, выгрузка потоком
- [x] 3.4 Написать спеки команд и `examples/cli/README.md`

## 4. Удаление старого и обвязка репозитория

- [x] 4.1 Удалить `examples/app-with-http`, `examples/split-nats`, `examples/simple-cli`, `examples/simple-http-server`, `examples/container`
- [x] 4.2 Проверить, что ни один `package.json`, конфиг nx и скрипт не ссылается на удалённые имена; `yarn install` проходит
- [x] 4.3 Дать джобе `verify` в `.github/workflows/ci.yml` полную историю (`fetch-depth: 0`) — иначе хэш плашки не разрешается

## 5. Гайд и рецепты: развязка от примеров

- [x] 5.1 Убрать из глав и рецептов пометки «шаг главы N; итоговая версия: …» и пересказы файлов примера
- [x] 5.2 Переписать подписи блоков кода на путь в проекте читателя (`// src/…`), убрав приставку `examples/<name>/`
- [x] 5.3 Заменить в командах запуска `yarn workspace @examples/<name> …` на команды проекта читателя
- [x] 5.4 Убрать колонку «Пример» из таблиц `docs/guide/README.md` и абзац «Код всех глав лежит в `examples/*`» из раздела «Как читать»
- [x] 5.5 Перевести плашки глав и рецептов на форму «сверено с кодом `<хэш>`»
- [x] 5.6 Повторить 5.1–5.5 в `docs/en/guide/` и `docs/en/recipes/`; форма английской плашки — «verified against `<хэш>`»

## 6. Гайд и рецепты: домен и целостность

- [x] 6.1 Перевести главы 14–22 с фичи `quotas` на `notifications` — обе языковые половины
- [x] 6.2 Пересверить рецепты, потерявшие источник (`token-families`, `config-sources`, `standalone`, `extending`, `alternatives`): код пишется в тексте рецепта
- [x] 6.3 Пересверить рецепты `cli`, `mcp`, `ops`, `webhook` с новыми примерами
- [x] 6.4 Решить судьбу рецепта `alternatives.md` (открытый вопрос design): оставить или перенести содержание в главу 2
- [x] 6.5 Проверить `docs/glossary.md`, `docs/en/glossary.md`, `docs/conventions.md`, `docs/guarantees.md` и `docs/from-nestjs.md` на упоминания старых примеров и фичи `quotas`

## 7. Аудит документации и правила ведения

- [x] 7.1 Переписать в `check.mjs` разбор плашки: хэш вместо даты, ERROR на неразрешимый хэш
- [x] 7.2 Заменить инвариант `guide-stale`: свежесть считается по `packages/*/src` от хэша плашки, WARN с числом коммитов
- [x] 7.3 Удалить инвариант `guide-example` вместе с резолвингом имени примера
- [x] 7.4 Переписать `lang-stale`: перевод отстал, если хэш английской пары — предок русского (`git merge-base --is-ancestor`)
- [x] 7.5 Добавить обработку мелкого клона: один WARN и пропуск проверок свежести
- [x] 7.6 Обновить `docs/README.md`: README у примеров обязателен, части пути не определяются примерами, новая форма плашки
- [x] 7.7 Обновить разбор подписи блока кода в `scripts/site/` под путь без приставки `examples/`
- [x] 7.8 Прогнать `yarn docs:audit` и довести до 0 ERROR

## 8. Синхронизация решений

- [x] 8.1 Проверить `docs/design/` на упоминания примеров и фичи `quotas`; поправить по месту
- [x] 8.2 Добавить строку change'а в таблицу `docs/decisions/roadmap.md`
- [ ] 8.3 Спросить пользователя, записывать ли решение в `docs/decisions/ideas.md`; по подтверждению — запись о роли примеров и развязке гайда, затем `node .claude/skills/docs-audit/scripts/ideas-toc.mjs`

## 9. Definition of Done

- [ ] 9.1 Все задачи выше отмечены
- [ ] 9.2 `yarn verify` зелёный (build, typecheck, lint, test, type-budget по всем пакетам и примерам)
- [ ] 9.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 9.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [ ] 9.5 Запись `ideas.md`, если она заведена по 8.3, несёт пометку «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем реализация уточнила решение
- [ ] 9.6 `yarn docs:audit` → 0 ERROR
- [ ] 9.7 Три примера мигрированы, главы и рецепты пересверены с новым хэшем в плашке
- [ ] 9.8 Коммиты осмысленные, `main` не тронут: слияние делает Merger после `/opsx:archive`
