## 1. Переименование пакета конвертера

- [x] 1.1 `git mv packages/nestling.openapi.zod packages/nestling.schema.zod`
- [x] 1.2 Манифест пакета: `name`, `description`, `keywords`,
      `repository.directory`, `homepage`
- [x] 1.3 Заменить имя в `@nestlingjs/openapi` — `devDependencies`, спеки,
      текст ошибки про отсутствующий конвертер в `src/schema.ts`
- [x] 1.4 Заменить имя в `examples/app-with-http` и `examples/users-service`
      (манифесты и импорты)
- [x] 1.5 `yarn install` — обновить `yarn.lock`; убедиться, что имя
      `@nestlingjs/openapi.zod` не встречается нигде вне `docs/history/` и
      `openspec/changes/archive/`
- [x] 1.6 README пакета: заголовок, установка, примеры; плашка статуса
- [x] 1.7 `yarn verify` зелёный на переименовании отдельным коммитом

## 2. Политика в декларации

- [x] 2.1 `CliEndpointDictionary`: поле `missing?: 'error' | 'prompt'`,
      JSDoc вместо комментария-отсылки к отдельной работе
- [x] 2.2 `cliEndpoint`: снять `missing` со словаря и положить в
      `binding: { missing }`; политика `'error'` поля `binding` не добавляет
- [x] 2.3 Чтение политики с проекции маршрута: функция уровня модуля по
      образцу `httpBindingOf`
- [x] 2.4 Спека: политика видна транспорту, декларация исполнима без
      изменений в ядре

## 3. Потоки транспорта

- [x] 3.1 `CliTransportOptions`: `input`, `output`, `errorOutput` с
      умолчаниями `process.stdin`, `process.stdout`, `process.stderr`
- [x] 3.2 Перевести печать результата, отказа и NDJSON с `console` на
      `output`/`errorOutput`; снять `eslint-disable no-console`
- [x] 3.3 Чтение формы `stream(...)` — из `input`, а не из `process.stdin`
- [x] 3.4 REPL: интерфейс readline поверх `input`/`output`
- [x] 3.5 Спеки: результат уходит в подставленный поток, поток команды
      читается из подставленного ввода

## 4. План вопросов

- [x] 4.1 `CliTransportOptions.converters` и `interactive`
- [x] 4.2 Построение плана на `serve`: JSON Schema формы `input` в
      направлении `io: 'input'`, перечень полей из `required`, форма
      вопроса по узлу
- [x] 4.3 Ошибка `serve` при отсутствии конвертера: имя команды, вендор,
      опция `converters`
- [x] 4.4 Ошибка `serve` при `stream`-входе вместе с `missing: 'prompt'`
- [x] 4.5 Узел непонятной формы пропускается без вопроса и без ошибки
- [x] 4.6 Спеки: план строится один раз, конвертер нужен только командам с
      политикой

## 5. Вопросы в терминале

- [x] 5.1 Определение интерактивности: `input.isTTY` и переменная `CI`,
      опция `interactive` перекрывает
- [x] 5.2 Формы вопроса: нумерованный список для `enum`, `[y/n]` для
      `boolean`, строка для скаляра; `description` подсказкой, `default` по
      пустому вводу
- [x] 5.3 Форма ответа: строка, `true`/`false`, элемент списка; приведения
      к типу JSON Schema нет
- [x] 5.4 Один интерфейс readline: в `'repl'` — открытый, в `'argv'` —
      временный на время вопросов
- [x] 5.5 Конец ввода оставляет поле незаданным
- [x] 5.6 Достроенный payload уходит в `dispatch.call` тем же путём, что и
      собранный из argv

## 6. Тесты транспорта

- [x] 6.1 Команда без политики: вопросов нет, отказ `bad_request`
- [x] 6.2 Выбор из списка, подтверждение, подсказка с умолчанием
- [x] 6.3 Заданное флагом поле не спрашивается
- [x] 6.4 Число вводится строкой и приводится схемой
- [x] 6.5 Отключение вопросов переменной `CI` и опцией `interactive`
- [x] 6.6 Вопрос внутри REPL: ответ не разбирается как команда
- [x] 6.7 Ошибки `serve`: нет конвертера, `stream` вместе с политикой

## 7. Пример и документация

- [x] 7.1 `examples/simple-cli`: команда с `missing: 'prompt'` — поле
      `enum`, поле `boolean`, поле с описанием и умолчанием; конвертер в
      `cli({ converters })`; тест команды через `execute`
- [x] 7.2 `docs/design/transports.md` §5 и `docs/design/endpoints.md` §6:
      политика реализована, отсылки к отдельной работе сняты
- [x] 7.3 `docs/recipes/cli.md`: раздел о недостающем входе; плашка «сверено
      с кодом» с новой датой
- [x] 7.4 `docs/guide/13-openapi-and-client.md`, `docs/design/schemas.md`,
      `docs/recipes/config-sources.md`, `docs/README.md`, манифесты
      `scripts/waves/*.manifest` — новое имя пакета
- [x] 7.5 README `@nestlingjs/transport.cli`: политика, опции транспорта,
      таблица экспортов; расхождение про потоки закрыто
- [x] 7.6 `docs/decisions/ideas.md`: запись [2026-07-13] «Контракт первичен»
      получает пометку «РЕАЛИЗОВАНО» вместо «РЕАЛИЗОВАНО ЧАСТИЧНО» с тем,
      что вышло, что уехало дальше и чем реализация уточнила решение;
      запись о переименовании пакета конвертера
- [x] 7.7 `docs/decisions/roadmap.md`: строка 56 и строка в разделе «После
      волны 6» — статус **done** со ссылкой на архив

## 8. Definition of Done

- [x] 8.1 Все задачи выше отмечены
- [x] 8.2 `yarn verify` зелёный (`build` + `typecheck` + `lint` + `test` +
      `type-budget`)
- [x] 8.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 8.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 8.5 Запись `ideas.md`, по которой шёл change, несёт пометку
      «РЕАЛИЗОВАНО»
- [x] 8.6 `yarn docs:audit` — 0 ERROR, кроме двух ссылок на каталог
      архива: его создаёт `/opsx:archive`, и после него аудит чист
- [x] 8.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [x] 8.8 `main` не тронут — слияние делает Merger после `/opsx:archive`
