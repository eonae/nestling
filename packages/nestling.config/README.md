# @nestling/config

Конфигурация Nestling: секция — рекорд полей со схемами, который вы инжектируете как обычную зависимость и нигде не регистрируете.

> 🚧 Активная разработка, API может меняться. Целевой дизайн:
> [`docs/design/config.md`](../../docs/design/config.md).
> Гайд: [`docs/guides/config.md`](../../docs/guides/config.md).

## Установка

```bash
npm install @nestling/config
```

Пакет не поставляет валидатор схем. Подойдёт любая реализация [Standard Schema v1](https://standardschema.dev): zod, valibot, arktype.

## Минимальный пример

```typescript
import { from, makeConfig, secret } from '@nestling/config';

export const OrdersConfig = makeConfig('orders', {
  maxItems: z.coerce.number().default(100),           // ORDERS_MAX_ITEMS
  databaseUrl: secret(from('DATABASE_URL', z.url())), // точное имя, скрывается в выводе
  apiToken: secret(z.string()),                        // ORDERS_API_TOKEN
});

export const ordersKeys = OrdersConfig.keys;
```

Объявите секцию через `makeConfig(prefix, fields)`. Экспортируйте из модуля только `ordersKeys`, а не `OrdersConfig`. Инжектируйте `OrdersConfig` как токен любой другой зависимости:

```typescript
@Injectable([OrdersConfig])
class OrdersService {
  constructor(private cfg: Config<typeof OrdersConfig>) {}
}
```

## Секция

Секция — рекорд полей, где каждому имени сопоставлена схема-лист. Пакет читает список полей с обычного JS-объекта и не заглядывает внутрь схем.

Узел графа для секции создаётся, когда кто-то её инжектирует. Регистрировать секцию в `providers:`, `imports:` или отдельном поле `configs:` модуля не нужно.

Секция не называет источник значения: она знает только имя ключа. Настройте, откуда брать значения, в корне приложения (раздел «Источник и привязка»).

## Ключ

Имя ключа выводится из префикса секции и имени поля. Разделитель ставится на границе «строчная буква или цифра, затем прописная» и «прописная, затем прописная и строчная»:

| Поле | Ключ (префикс `orders`) |
|---|---|
| `maxItems` | `ORDERS_MAX_ITEMS` |
| `httpURL` | `ORDERS_HTTP_URL` |

Вызовите `from(key, schema)`, чтобы задать точное имя ключа и отключить действие префикса. Так объявляют ключ, который читает больше одной секции (раздел «Общие ключи»).

## Источник и привязка

Источник — обычный объект, а не провайдер:

```typescript
interface ConfigSource {
  get(key: string): unknown;
  name?: string;
  init?(): void | Promise<void>;
  watch?(notify: () => void): void;
  close?(): void | Promise<void>;
}
```

Привяжите источники к ключам плоским списком:

```typescript
configKernel([
  [vault(), [ordersKeys]],
  [file('config.yaml'), ['*_URL']],
]);
```

Таргетом привязки служит хэндл `.keys` секции, глоб (`'*_URL'`, `'*'`) или массив из них. Источник, чей таргет не покрывает ключ, вообще не опрашивается.

`assemble({ config })` пакета `@nestling/app` вызывает `configKernel` сам, поэтому в корне на `assemble` про конфиг ничего писать не нужно, если хватает `process.env`. Собираете контейнер напрямую через `@nestling/container`, без `@nestling/app`, — зарегистрируйте `configKernel(bindings)` как обычный модуль.

Порядок элементов списка задаёт приоритет: побеждает первый источник, чей таргет покрывает ключ и который вернул значение, отличное от `undefined`. `process.env` опрашивается последним и в списке не упоминается: это источник по умолчанию с низшим приоритетом.

Одна приватная читалка опрашивает все источники. Читалка — узел графа с асинхронной фабрикой: `init()` каждого источника завершается раньше, чем спроецируется любая секция, а `@OnDestroy` закрывает источники на остановке.

## `.keys`

`.keys` — второе право секции, отдельное от права инжекта.

| Право | Значение | Что позволяет |
|---|---|---|
| инжект | токен, который вернул `makeConfig` | читать секцию |
| привязка | `Section.keys` | указать источник для её ключей |

Экспортируйте из модуля только `.keys`, а токен секции держите приватным: тогда чужой инжект нечем написать. `.keys` не является `InjectionToken`, и указать его в `deps` — ошибка компиляции.

## Общие ключи

Любое число секций может читать один ключ. Право читать ключ не означает владения им: вторая секция объявляется без ведома первой. Каждая секция валидирует сырое значение своей схемой, поэтому две секции законно видят один ключ по-разному (`z.string()` и `z.coerce.number()`).

Единственный конфликт сборки — несогласованный флаг `reloadable` двух читателей одного ключа. `ConfigSharedKeyError` называет ключ, обе секции, оба поля и обе починки. Проверка учитывает только секции, которые граф создал при текущей сборке: объявление вне выбранной топологии конфликта не создаёт.

## Секреты

Оберните поле в `secret(leaf)`, чтобы его значение не попадало в текст, который печатает фреймворк. Порядок вложения единственный: `secret()` снаружи, `from()` внутри. Секретность — свойство поля, а `from()` только называет его ключ. Тип поля при этом не меняется: `secret(z.string())` остаётся строкой.

Фреймворк скрывает секрет на трёх поверхностях:

- `ConfigValidationError` заменяет сообщение проваленного поля на `<redacted>` в тексте ошибки и в `failures[].issues`. Если значение не было задано, сообщение показывается целиком: «ключ не задан» — основной сценарий отладки секрета.
- Проекция секции получает `toJSON()` и `nodejs.util.inspect.custom`, которые возвращают копию с `'***'` вместо секретных полей. `console.log(cfg)` и `JSON.stringify(cfg)` безопасны, а чтение поля напрямую по-прежнему отдаёт настоящее значение.
- `describeConfig()` помечает ключ флагом `secret`. Значений в снимке нет.

Секретность ключа складывается по всем его читателям: если хотя бы одна секция пометила ключ `secret()`, он скрывается и в секциях, которые сами его так не пометили. Гарантия распространяется только на вывод, который производит сам фреймворк: `{ ...cfg }`, `Object.values(cfg)` и ваша собственная интерполяция строки возвращают настоящее значение.

## Reloadable

Объявите секцию через `makeConfig.reloadable(prefix, fields)`, чтобы её значения обновлялись без перезапуска процесса:

```typescript
const Runtime = makeConfig.reloadable('runtime', {
  rps: z.coerce.number().default(100),
});
```

Чтение поля отдаёт последнее валидное значение без подписки, а сам инстанс секции остаётся тем же. Подпишитесь на обновление через `onChange(signal, callback)` (построен на `Topic` из [`@nestling/streams`](../nestling.streams)): подписка снимается по взведению `signal`. Поле с именем `onChange` в рекорде reloadable-секции запрещено — это имя занято подпиской.

Источник с `watch()` сообщает читалке о новом значении. Невалидное значение на старте останавливает приложение. Невалидное значение при обновлении отбрасывается: остаётся последний валидный снимок, а читалка выдаёт предупреждение. Reloadable-секция, привязанная только к источникам без `watch()`, поднимается и предупреждает при старте: несовпадение возможностей не считается ошибкой сборки.

## Первичное чтение `load(section)`

```typescript
const RootConfig = makeConfig('app', { features: z.string().default('all') });

const cfg = load(RootConfig); // синхронно, только process.env
```

Вызовите `load(section)`, чтобы прочитать секцию до сборки контейнера, например для `select` в `assemble`. Читалки и привязанных источников на этой фазе ещё нет, поэтому `load` знает только `process.env`. Валидация такая же, как у проекции из графа: независимая по полям, все отказы попадают в одну ошибку `ConfigValidationError`, fail-fast.

## Интроспекция

Вызовите `describeConfig()`, чтобы получить снимок реестра без значений и без обращения к источникам. Снимок несёт две проекции одних данных: `sections` — что читает каждая секция, `keys` — кто читает каждый ключ (`readers.length > 1` означает, что ключ общий), `globs` — объявленные unbound-глобы.

Вызовите `keysGlob(pattern)`, чтобы объявить unbound-глоб для ключей, которых ни одна секция не называет, и привязать к нему источник так же, как к `.keys` секции.

Семейство `Config(key)` даёт узел на одиночный сырой ключ для инфраструктуры по требованию: параметризованный провайдер клиента может зависеть от `Config(addressKey(server))`, и граф создаёт узел только для ключей, упомянутых в `deps`. Значение ключа не проходит валидацию: она остаётся свойством секции.

## Справочник API

| Экспорт | Что это |
|---|---|
| `makeConfig(prefix, fields)` | объявляет секцию; возвращает токен с `.keys` |
| `makeConfig.reloadable(prefix, fields)` | объявляет reloadable-секцию |
| `from(key, schema)` | задаёт точное имя ключа поля |
| `secret(leaf)` | помечает поле секретным |
| `load(section)` | читает секцию до сборки контейнера, только из `process.env` |
| `configKernel(bindings?, options?)` | модуль, который привязывает источники к ключам |
| `objectSource(values?, name?)` | источник поверх обычного объекта, с `set`/`assign` |
| `describeConfig()` | снимок реестра секций и ключей |
| `keysGlob(pattern)` | объявляет unbound-глоб ключей |
| `Config(key)` | семейство токенов на одиночный сырой ключ |
| `Config<typeof Section>`, `ConfigProjection<typeof Section>` | тип проекции секции (два имени одного типа) |
| `ConfigKeys` | класс-хэндл ключей секции; `Section.keys` — его экземпляр |
| `ConfigTarget`, `ConfigGlob` | типы таргета привязки |
| `ConfigSectionToken`, `ConfigValues`, `ConfigRecord`, `ConfigField` | типы декларации секции |
| `FromField`, `SecretField`, `ReloadableConfig` | типы обёрток и дополнения проекции reloadable-секции |
| `ConfigSource`, `ConfigBinding`, `ObjectSource` | типы источника и привязки |
| `ConfigKernelOptions`, `ConfigWarn` | опции `configKernel` и канал предупреждений |
| `ConfigValidationError`, `ConfigFieldFailure` | ошибка невалидной секции и одно провалившееся поле |
| `ConfigSharedKeyError`, `SharedKeyReader` | ошибка несогласованного `reloadable` общего ключа |
| `ConfigDescription`, `ConfigSectionDescription`, `ConfigKeyDescription`, `ConfigSharedKeyDescription`, `ConfigKeyReader` | типы снимка `describeConfig()` |

## Границы пакета

Готовые источники пакет не поставляет: `file()`, `vault()` и подобные реализуют интерфейс `ConfigSource` в отдельных пакетах.
