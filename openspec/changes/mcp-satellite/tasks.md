## 1. Пакет

- [ ] 1.1 Завести `packages/nestling.mcp` по общему набору конфигов:
  `package.json` (имя `@nestlingjs/mcp`, скрипты `clear`, `typecheck`,
  `build`, `lint`, `lint:fix`, `test`, экспорт только `.`), `tsconfig.json`,
  `tsconfig.build.json`, `eslint.config.js`, `jest.config.js`
- [ ] 1.2 Объявить зависимости: `@nestlingjs/app`, `@nestlingjs/container`,
  `@nestlingjs/operations`, `@nestlingjs/transport.http` —
  `workspace:*`; `@modelcontextprotocol/sdk` — только в `devDependencies`
- [ ] 1.3 `yarn install` и пустой `build` проходят; пакет виден `nx`

## 2. Типы протокола

- [ ] 2.1 Объявить типы определения инструмента и результата его вызова
  (`McpToolDefinition`, `McpCallToolResult`, `McpContent`,
  `McpToolAnnotations`) — без импорта SDK
- [ ] 2.2 Объявить типы конверта JSON-RPC 2.0: запрос, ответ, нотификация,
  ошибка с кодами протокола
- [ ] 2.3 Объявить список поддерживаемых версий протокола константой
- [ ] 2.4 `mcp.type-test.ts`: присваивание наших типов `Tool` и
  `CallToolResult` из `@modelcontextprotocol/sdk/types.js`

## 3. Объявление инструмента

- [ ] 3.1 `tool(operation, options?)`: значение-объявление, тип-параметр
  сужен до `RequestOperation`, рантайм-проверка вида операции
- [ ] 3.2 Проверка словаря `options`: только `name`, `description`,
  `annotations`; неизвестное поле — ошибка с перечнем известных
- [ ] 3.3 Вывод имени: точки на подчёркивания, сверка с
  `^[a-zA-Z0-9_-]{1,128}$`
- [ ] 3.4 Вывод описания: `options.description` → `doc.description` →
  `doc.summary`; ни одного — нарушение
- [ ] 3.5 Спеки на 3.1–3.4, включая столкновение имён после подстановки

## 4. Построение определений

- [ ] 4.1 Перевод схем в JSON Schema через `leafJsonSchema` с
  `converters:`; `inputSchema` обязан быть объектным
- [ ] 4.2 Накопитель нарушений: список вместо первой ошибки, каждая строка
  называет инструмент и место
- [ ] 4.3 Проверка уникальности имён в списке `tools:` с указанием
  подстановки как причины совпадения
- [ ] 4.4 Спеки: перевод схемы, отсутствие конвертера, непереводимая схема,
  необъектный вход, два нарушения одним списком

## 5. Отображение результата

- [ ] 5.1 `Ok` → `structuredContent` плюс `content` текстом JSON
- [ ] 5.2 `Fail` → `isError: true`, текст с `code` и `message`, `details` в
  `structuredContent`
- [ ] 5.3 `internal_error` отдаётся тем же путём, без текста исключения и
  стека
- [ ] 5.4 Спеки на 5.1–5.3

## 6. Обработчик протокола

- [ ] 6.1 Разбор конверта JSON-RPC: дефектный JSON, не объект, нет
  `jsonrpc`, нет `method` — ответ с кодом ошибки протокола
- [ ] 6.2 `initialize`: согласование версии, сведения о сервере, возможности
  с одним `tools`
- [ ] 6.3 `notifications/initialized` и `ping`
- [ ] 6.4 `tools/list` из готовых определений, без страничного перебора
- [ ] 6.5 `tools/call`: поиск по имени, проверка входа схемой операции,
  вызов порта с `meta` запроса; неизвестное имя — «неверные параметры»
- [ ] 6.6 Неизвестный метод — «метод не найден»
- [ ] 6.7 Спеки на 6.1–6.6 без поднятия приложения

## 7. Сессии

- [ ] 7.1 Ресурс `McpSessions`: карта сессий, `acquire` на INIT, `release`
  на SHUTDOWN
- [ ] 7.2 Выдача `Mcp-Session-Id` на `initialize`, чтение заголовка из
  `ctx.raw.attributes`, неизвестная сессия — отказ `not_found`
- [ ] 7.3 Сверка заголовка `MCP-Protocol-Version`; значение вне списка —
  отказ `bad_request`
- [ ] 7.4 Предел числа сессий и срок бездействия опциями со значениями по
  умолчанию; превышение предела — отказ на `initialize`
- [ ] 7.5 `DELETE` по пути endpoint'а закрывает сессию
- [ ] 7.6 Спеки на 7.1–7.5, включая пустую карту после `release`

## 8. Плагин

- [ ] 8.1 `mcp(options)`: `makePlugin` с одним endpoint'ом `POST` по
  `options.path` (умолчание `/mcp`), `doc.hidden` с причиной, опции
  `pipeline` и `detached`
- [ ] 8.2 Провайдер определений: зависимости — `tools.map(t =>
  t.operation.caller)`, построение на фазе ASSEMBLE
- [ ] 8.3 Ответ заголовками через `HttpResponse`; нотификация — статус
  `accepted` без тела
- [ ] 8.4 Спеки: сборка падает до INIT, ни один порт не открыт; endpoint
  плагина не попадает в документ OpenAPI; операция вне `tools:` узла не
  создаёт

## 9. Проверка настоящим клиентом

- [ ] 9.1 Интеграционный прогон: приложение поднято, `Client` из SDK
  соединяется по HTTP
- [ ] 9.2 Прогон проверяет `listTools()`, успешный `callTool()` и
  `callTool()`, завершившийся отказом операции
- [ ] 9.3 Тест границы пакета: список разрешённых импортов, ни одного
  валидатора и ни одного HTTP-сервера в рантайме
- [ ] 9.4 Тест манифеста: SDK только в `devDependencies`, валидаторов нет ни
  в `dependencies`, ни в `peerDependencies`

## 10. Пример и документация

- [ ] 10.1 `examples/app-with-http`: подключить плагин, объявить два-три
  инструмента из существующих операций
- [ ] 10.2 Снимок определений инструментов примера сверяется тестом
- [ ] 10.3 README пакета с плашкой статуса, таблицей экспортов и разделом
  «Границы пакета»
- [ ] 10.4 Рецепт `docs/recipes/mcp.md` и строка в `docs/recipes/README.md`
- [ ] 10.5 Строка пакета в таблице «Инструменты и сателлиты»
  `docs/README.md`
- [ ] 10.6 Раздел в `docs/design/operations.md` о третьем потребителе
  деклараций со ссылкой на пакет
- [ ] 10.7 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
  всем написанным текстам

## 11. Закрытие открытых вопросов design.md

- [ ] 11.1 Решить, объявлять ли `outputSchema` всегда, и записать решение в
  README пакета
- [ ] 11.2 Выбрать срок бездействия сессии по умолчанию и назвать его в
  README
- [ ] 11.3 Дополнить запись `ideas.md [2026-09-12]` тем, чем реализация
  уточнила решение

## 12. Definition of Done

- [ ] 12.1 Все задачи выше отмечены
- [ ] 12.2 `yarn verify` зелёный
- [ ] 12.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 12.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`
- [ ] 12.5 Запись `ideas.md [2026-09-12]` несёт пометку «РЕАЛИЗОВАНО» по
  пункту 7: что вышло целиком, что уехало дальше, чем реализация уточнила
  решение
- [ ] 12.6 `yarn docs:audit` — 0 ERROR
- [ ] 12.7 `examples/app-with-http` мигрирован, затронутые главы гайда и
  рецепты пересверены с обновлённой датой в плашке «сверено с кодом»
- [ ] 12.8 Коммиты осмысленные, `main` не тронут: слияние делает Merger
  после `/opsx:archive`
