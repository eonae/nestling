# release-notes-0-2-0 — tasks

## 1. Разбор диапазона

- [x] 1.1 Выписать состав диапазона:
      `git log --oneline v0.1.3..v0.2.0 --grep='архив'` — 18 change'ей;
      каждому найти папку `openspec/changes/archive/*-<имя>/`
- [x] 1.2 Прочитать раздел «What Changes» каждого предложения, выписать
      в черновик пользовательские изменения с пометками **BREAKING**
- [x] 1.3 Сверить черновик с `git log --oneline v0.1.3..v0.2.0`: правки
      вне change'ей (коммиты `feat` и `feat!`) тоже попали в черновик
- [x] 1.4 Сверить каждое утверждение черновика с кодом на теге —
      `git show v0.2.0:<путь>`. Предложение описывает намерение,
      заметка описывает то, что вышло
- [x] 1.5 Собрать таблицу переименований: имя 0.1.3 → имя 0.2.0, по
      пакетам
- [x] 1.6 Выписать, что из этого 0.3.0 переименовал ещё раз: такой
      раздел ведёт в [v0.3.0.md](../../../docs/releases/v0.3.0.md), а не
      в главу

## 2. Русская заметка

- [x] 2.1 Создать `docs/releases/v0.2.0.md`: заголовок `# 0.2.0`, плашка
      с диапазоном `v0.1.3`..`v0.2.0` и датами 12–13 сентября 2026
- [x] 2.2 «Коротко» — абзац на всё главное выпуска
- [x] 2.3 «Декларация endpoint'а: было и стало» — пара блоков кода 0.1.3
      и 0.2.0 на одном endpoint'е
- [x] 2.4 «Декларация endpoint'а»: конструктор по HTTP-методу,
      имя команды первым аргументом `cliEndpoint`,
      `httpEndpoint.implement(Operation, { … })`, `UndeclaredFailError`
- [x] 2.5 «Пайплайн и ядро»: досрочный успех pre-шага `done()`,
      `Var.provide([Deps], compute)`, `makeToken(id, { hint })`,
      ключ идемпотентности у события и свой словарь `meta` у `Port` и
      `Emitter`
- [x] 2.6 «Данные и доставка»: `@nestlingjs/drizzle.pg`,
      `@nestlingjs/inbox`, outbox на новых примитивах,
      `Nats-Msg-Id` и окно дедупликации потока
- [x] 2.7 «Наблюдаемость»: `withTracing()`, переменная `Trace`,
      `traceparent` из клиента; `Metrics`, `RootMetrics$`,
      `Metrics$(scope)`, `makeApp({ metrics })`
- [x] 2.8 «MCP»: пакет `@nestlingjs/mcp`, `mcp({ … })` в `transports:`,
      `mcpTool` двумя формами
- [x] 2.9 «Схемы и OpenAPI»: `@nestlingjs/openapi.zod` →
      `@nestlingjs/schema.zod`; параметр описывается разобранной формой
- [x] 2.10 «CLI»: `missing: 'prompt'`, опции `cli()` — `converters`,
      `interactive`, `input`, `output`
- [x] 2.11 «Барели перечисляют имена»: `export *` из бареля убран, имя
      без читателя наружу не выходит, возврат — одна строка
- [x] 2.12 «Runtime-строки — английские»: что печатает процесс, стало
      английским; правило линтера в общем конфиге
- [x] 2.13 «Удалено» — одним списком: ключ `operation`, поля `method`,
      `path`, `command`, девять полей `never`, имя `openapi.zod`
- [x] 2.14 «Тестирование»: `spyMetrics()`, сборка сниппетов скилла
- [x] 2.15 «Примеры и документация»: `guide/` и `recipes/` двумя
      папками, сквозная нумерация глав, сайт разделами на GitHub Pages,
      два языка, README пакета парой
- [x] 2.16 «Для тех, кто собирает репозиторий»: публикация по OIDC без
      ключа доступа, правило линтера `dependency-list`
- [x] 2.17 «Таблица переименований» с якорем `{#renames}`
- [x] 2.18 Открыть каждую ссылку заметки: раздел ведёт туда, где имя
      совпадает с блоком кода; где 0.3.0 переименовал — ссылка на
      `v0.3.0.md` вместо главы

## 3. Английская пара

- [x] 3.1 Написать `docs/en/releases/v0.2.0.md`: порядок разделов и
      блоки кода те же
- [x] 3.2 Термины взять из `docs/en/glossary.md`; новых не заводить
- [x] 3.3 Ссылки не пересекают границу языка: из `docs/en/` — только в
      `docs/en/`

## 4. Оглавления

- [x] 4.1 Строка 0.2.0 в таблицу `docs/releases/README.md` — под строкой
      0.3.0: таблица идёт от новой версии к старой
- [x] 4.2 Та же строка в `docs/en/releases/README.md`

## 5. Инвариант аудита

- [x] 5.1 В `.claude/skills/docs-audit/scripts/check.mjs` вызвать
      `checkToc` для `docs/releases` и `docs/en/releases`, исключив
      `README.md` из перечня файлов
- [x] 5.2 Развязать хвост `checkToc`: имя папки для ссылки из
      `docs/README.md` он выводит как `label.split('/')[1]`, и для
      `docs/en/releases` это даёт `en`. Передавать имя папки отдельно
      либо проверять ссылку только у русского вызова
- [x] 5.3 Проверить обе стороны инварианта: убрать строку оглавления —
      ERROR; сослаться на несуществующий файл — ERROR; вернуть как было
      — молчит
- [x] 5.4 Проверить, покрывает ли `lang-outline` пару оглавлений
      `releases/`. Если да — второго сообщения о том же не заводить
- [x] 5.5 Дополнить `.claude/skills/docs-audit/SKILL.md`: оглавлений,
      которые сверяются с папкой, стало три

## 6. Правила заметки

- [x] 6.1 Раздел «releases/» в `docs/README.md` — назвать сверку
      оглавления с составом папки
- [x] 6.2 Шаг 2 `RELEASING.md` — сверить текст со спекой
      `release-notes`: расхождений остаться не должно

## 7. Проверки

- [x] 7.1 `node .claude/skills/docs-style/scripts/lint.mjs` по
      `docs/releases/v0.2.0.md`, `docs/en/releases/v0.2.0.md` и обоим
      оглавлениям — 0 запрещённых слов
- [x] 7.2 `yarn docs:audit` — 0 ERROR
- [x] 7.3 `yarn docs:build` — раздел `/releases/v0.2.0/` печатается на
      обоих языках, ссылки внутри заметки живые

## 8. Definition of Done

- [x] 8.1 Все задачи выше отмечены
- [x] 8.2 `yarn verify` зелёный
- [x] 8.3 README пакетов change не трогает: правятся `docs/`, `docs/en/`
      и один скрипт проверки
- [x] 8.4 `design/` правки не требует — заметка о выпуске не входит в
      целевое V1 фреймворка. `decisions/` не правится в этом change'е:
      пометка «РЕАЛИЗОВАНО» в записи [2026-09-14] «Разбор отчёта об
      обновлении до 0.3.0» и строка 104 roadmap ставятся после
      `/opsx:archive`. Работа пришла из пунктов 1 и 2 этой записи; пункт
      3 (константу передаёт схема) в объём change'а не входит
- [x] 8.5 `yarn docs:audit` — 0 ERROR
- [x] 8.6 `examples/*` и главы гайда не трогаются: заметка описывает
      прошлый выпуск, API не меняется, плашки сверки остаются с прежними
      датами
- [x] 8.7 Коммиты осмысленные, `main` не тронут: слияние делает Merger
      после `/opsx:archive`
