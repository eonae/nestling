/**
 * Наружу из папки конфига выходит только описание ключей. DI-токен секции
 * остаётся приватным: потребители внутри приложения импортируют его из
 * `./app.config` напрямую.
 */
export { appConfigKeys } from './app.config.js';
