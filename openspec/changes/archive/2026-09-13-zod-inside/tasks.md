## 1. Пакет `@nestlingjs/schema.zod`

- [x] 1.1 Переписать шапку `src/index.ts`: правило «конвертер называется
      явно одной строкой» отменено, пакет — единственный дом zod во
      фреймворке
- [x] 1.2 Добавить `withZodDefault(converters?)`: дописывает
      `zodConverter()`, пропускает шаг при конвертере вендора `zod` в
      списке; тест на четыре исхода (список не передан, пустой список,
      добавленный чужой вендор, свой `zodConverter({ … })`)
- [x] 1.3 Добавить билдеры `int()` и `flag()` открытыми значениями zod;
      тесты на чейнинг `.min().default()`, на строку из окружения и на обе
      записи флага
- [x] 1.4 Перенести `fromType`, `fromScratch`, `makeModel` вместе с их
      тестами и type-тестами; барель перечисляет имена поимённо
- [x] 1.5 Тест конвертации: `zodConverter()` переводит поле `int().min(1)`
      в JSON Schema с `type: 'integer'` и `minimum: 1` — доказательство,
      что схема пакета и конвертер приложения работают вместе
- [x] 1.6 Манифест: `zod` остаётся в `peerDependencies`

## 2. Удаление `@nestlingjs/models`

- [x] 2.1 Удалить каталог `packages/nestling.models` целиком
- [x] 2.2 Убрать пакет из корневых конфигураций сборки и публикации, если
      он там перечислен
- [x] 2.3 Проверить, что строки `@nestlingjs/models` не осталось нигде,
      кроме `docs/decisions/`, `docs/history/` и архива openspec

## 3. Правило пустого значения в ядре

- [x] 3.1 Свести пустую строку к `undefined` в одной точке — там, где сырое
      значение ключа отдаётся схеме поля (`config/project.ts` и
      `config/load.ts`)
- [x] 3.2 Рантайм-тесты: пустой ключ даёт умолчание, пустой ключ у
      обязательного поля даёт `ConfigValidationError`, пустое значение не
      уводит читалку к следующей привязке, оба пути чтения ведут себя
      одинаково

## 4. Секции ядра на zod

- [x] 4.1 `@nestlingjs/app`: объявить `zod` в `dependencies`
- [x] 4.2 Секция логгера на `z.enum(...).default(...)`; хелпер
      `enumeration` удалён
- [x] 4.3 Секция проб на цепочке zod; хелпер `milliseconds` удалён
- [x] 4.4 Секция портов на `z.enum(...)`; `dispatchSchema` удалён
- [x] 4.5 Тест: `describeConfig({ converters: [zodConverter()] })` несёт
      JSON Schema для `NESTLING_LOG_LEVEL` и `NESTLING_HEALTH_TIMEOUT` с
      исходом «получена конвертером»
- [x] 4.6 Сверить тексты отказов: сообщение приходит от валидатора, тесты
      на текст обновлены

## 5. Секции и факты сателлитов

- [x] 5.1 `@nestlingjs/inbox`: секция на билдерах, `src/schema.ts` удалён,
      `zod` в `dependencies`
- [x] 5.2 `@nestlingjs/outbox`: секция на билдерах, факты `z.object({ … })`,
      `src/schema.ts` и аннотация `jsonSchema()` вокруг рекордов удалены
- [x] 5.3 `@nestlingjs/drizzle.pg`: секция семейства на билдерах, `str()`
      заменён на `z.string().min(1)`, `src/schema.ts` удалён
- [x] 5.4 `@nestlingjs/subscriptions`: факты `z.object({ … })`,
      `src/schema.ts` удалён
- [x] 5.5 `@nestlingjs/mcp`: отказы пакета на `z.object({ … })`, хелпер
      `singleField` удалён
- [x] 5.6 Проверить оставшиеся литералы `vendor: 'nestling'`: у каждого
      рядом названа причина, по которой вендорская схема не подходит

## 6. Умолчание конвертера

- [x] 6.1 `@nestlingjs/openapi`: зависимость на `schema.zod`, разрешение
      списка через `withZodDefault`, `converters` необязателен
- [x] 6.2 `@nestlingjs/mcp`: то же; тест границы переписан с «валидатора
      нет в манифесте» на «валидатора нет в объявлениях типов»
- [x] 6.3 `@nestlingjs/transport.cli`: то же; план вопросов строится без
      объявления конвертера
- [x] 6.4 Переписать диагностику непереводимой схемы во всех трёх пакетах:
      текст называет вендора схемы и способ починки, а не отсутствие списка
- [x] 6.5 Проверка публичной границы: в `dist/*.d.ts` публикуемых пакетов
      строки `zod` нет нигде, кроме `@nestlingjs/schema.zod`

## 7. Документ у плагина

- [x] 7.1 `openapi(options)` возвращает значение с методом
      `document(discovery)`; метод и провайдер зовут одну внутреннюю
      функцию
- [x] 7.2 `buildOpenApiDocument` уходит из барреля; проверить судьбу
      `hiddenEndpoints` — остаётся публичным или становится внутренним вместе
      с ним
- [x] 7.3 Тесты: документ строится при выключенной ветке переключателя;
      тело `GET /openapi.json` равно документу, построенному методом

## 8. Примеры

- [x] 8.1 `examples/app-with-http/src/app.ts`: `openapiOptions` удалён,
      `openapi({ info, pipeline })`, `mcp({ … })` без `converters`
- [x] 8.2 `examples/app-with-http/src/openapi.ts`: документ строится
      методом плагина
- [x] 8.3 `examples/simple-cli`: `cli()` без списка конвертеров
- [x] 8.4 Прогнать примеры: `openapi.json` пересобран и совпадает по
      составу с прежним

## 9. Документация

- [x] 9.1 `docs/design/schemas.md`: §1 про роли границы и авторства, §2 про
      умолчание конвертера, таблица §2.1 — две поверхности пакета и метод
      плагина
- [x] 9.2 `docs/design/config.md`: правило пустого значения ключа
- [x] 9.3 `docs/README.md`: таблица пакетов без `models`, строка
      `schema.zod` переписана
- [x] 9.4 Главы гайда `13-openapi-and-client.md` и `21-compatibility.md`,
      рецепты `mcp.md`, `cli.md`, `config-sources.md`
- [x] 9.5 README пакетов на обоих языках: `schema.zod`, `openapi`, `mcp`,
      `transport.cli`, `inbox`, `outbox`, `drizzle.pg`, `subscriptions`,
      `app` — включая плашки статуса
- [x] 9.6 Английские пары всех правленых глав и дизайн-доков (`docs/en/`)
- [x] 9.7 Скилл агента: таблица пакетов и разделы про схемы
- [x] 9.8 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
      всем правленым текстам — 0 запрещённых слов

## 10. Definition of Done

- [x] 10.1 Все задачи выше отмечены
- [x] 10.2 `yarn verify` зелёный
- [x] 10.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 10.4 `design/` и `decisions/` синхронизированы по правилам
      `CLAUDE.md`
- [x] 10.5 Запись `ideas.md [2026-09-13]` «Схемы: Standard Schema на
      границе, zod внутри; один пакет `schema.zod`» несёт пометку
      «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем
      реализация уточнила решение
- [x] 10.6 `yarn docs:audit` — 0 ERROR
- [x] 10.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [x] 10.8 Оглавление `ideas.md` пересобрано. Строку 89 `roadmap.md`
      закрывает `/opsx:archive`: «done» несёт ссылку на каталог архива,
      которого до архивации нет, и `docs:audit` считает её битой
- [x] 10.9 `main` не тронут — слияние делает Merger после `/opsx:archive`
