## 1. Проверки ставятся первыми

Порядок не случаен: сначала `yarn verify` краснеет и называет всё, что
чинится дальше. Так видно объём, и ни одно место не теряется.

- [x] 1.1 Проверка версии: `SKILL.md` несёт версию Nestling, и она совпадает
      с полем `version` файла `package.json` пакета. Отказ называет обе
      версии
- [x] 1.2 Проверка правила сверки: вводная часть `SKILL.md` содержит правило
      о версии `@nestlingjs/app` в проекте. Отказ печатается, когда правила
      нет
- [x] 1.3 Разбор таблиц переименований `docs/releases/*.md`: левая ячейка →
      имя, фильтр по правой колонке той же таблицы, список исключений с
      причиной на каждое
- [x] 1.4 Поиск удалённых имён в файлах скилла целым словом. Отказ называет
      имя, файл, строку, строку таблицы и границу проверки
- [x] 1.5 Проверка ограды: блок кода без языка — ошибка с именем файла и
      номером строки
- [x] 1.6 Проверка фаз: таблица фаз `references/container.md` перечисляет
      номера подряд, пропуск — ошибка
- [x] 1.7 Проверка каталога пакетов: публикуемый пакет репозитория, которого
      нет в таблице `references/packages.md`, — ошибка
- [x] 1.8 Все проверки идут таргетом, который `yarn verify` уже гоняет.
      Отдельной команды не заводится

## 2. Участок снипета

- [x] 2.1 Форма маркеров участка выбирается и записывается в `design.md`
      (открытый вопрос 1): `// #region имя` против `// snippet:start имя`
- [x] 2.2 Разбор участков в файле `snippets/`: пара границ, снятие общего
      отступа, отказ на вложенности и на повторе имени
- [x] 2.3 Сверка блока с участком по пометке
      `<!-- snippet: файл.ts#участок -->`; отказ называет файл, участок и
      markdown
- [x] 2.4 Проверка «снипет без читателя» считает файл прочитанным по ссылке
      на любой его участок
- [x] 2.5 Команда переписывания блоков работает с участками и не трогает
      блоки других участков того же файла
- [x] 2.6 Отказ «участка нет в файле»

## 3. Одиннадцать мест переписываются на 0.3.0

- [x] 3.1 `references/config.md`, раздел «Sources» целиком: привязка на
      `run({ config })` и `bind(source, { keys })` вместо поля `config:`
      корня и кортежа `[source, keys]`; `load(RootConfig)` уходит; константа
      приложения живёт в `.default()` схемы секции, а не в источнике
- [x] 3.2 `references/config.md`, блок чтения секции: фрагмент становится
      участком снипета
- [x] 3.3 `references/http.md`: `doc: { status }` → поле `status` декларации
- [x] 3.4 `references/features.md`: строковая форма `build('users')` →
      `build({ features: 'users' })`; выбор состава из конфига переписан
- [x] 3.5 `references/testing.md`, матрица топологий: `config: vars({ … })` →
      `bind(vars({ … }))`, `args: 'users'` и `'all'` строкой → форма объекта.
      Таблица опций и снипет того же файла перестают противоречить друг другу
- [x] 3.6 `references/from-nest.md`: кортеж привязки в таблице перехода
- [x] 3.7 Оставшиеся блоки без языка — `endpoints.md`, `errors.md`,
      `http.md`, `testing.md` — становятся снипетами или участками; дерево
      каталогов в `SKILL.md` получает язык `text`
- [x] 3.8 `yarn verify` по проверкам раздела 1 зелёный: ни одного удалённого
      имени, ни одной ограды без языка

## 4. Пробелы в существующих справочниках

- [x] 4.1 `references/errors.md`: тело отказа по HTTP документом RFC 9457 —
      `type`, `title`, `status`, `detail`, `details`; названа цена теста,
      написанного на прежнюю форму
- [x] 4.2 `references/features.md`: `errorsOf` подключает отказы соседней
      операции
- [x] 4.3 `references/http.md`: объявление сервера `server()`, которое в
      `transports:` не входит, и встраивание в чужой процесс
- [x] 4.4 `references/container.md`: строка фазы `5 RUN` в таблице фаз
- [x] 4.5 Снипеты под новые темы: компилируемые файлы, пакеты в
      `devDependencies` через `workspace:*`

## 5. Состав: четырнадцать файлов

- [x] 5.1 `references/packages.md`: таблица всех публикуемых пакетов, строка
      на пакет; у `common.*` строка говорит, что это утилита репозитория
- [x] 5.2 Таблица пакетов уходит из `SKILL.md`; раздел «куда смотреть
      дальше» получает строку на `packages.md`
- [x] 5.3 `references/observability.md`: логирование и опция `logging`,
      метрика-декларация (`makeMetrics`, `MetricsOf`, `metrics:` у фичи,
      `MetricsStore$`), участок трассы; пакеты `logging.pino`, `prometheus`,
      `otel`
- [x] 5.4 `references/storage.md`: соединение ресурсом и `drizzle.pg`,
      транзакция переменной контекста, outbox и inbox с их хранилищами
- [x] 5.5 Гарантированная доставка уходит из `references/features.md` в
      `storage.md`, на её месте остаётся ссылка
- [x] 5.6 Вводная часть `SKILL.md`: версия и правило сверки. Частей второго
      уровня по-прежнему пять
- [x] 5.7 Потолки соблюдены: `SKILL.md` не длиннее 250 строк, каждый файл
      `references/` — не длиннее 200

## 6. Установщик и манифест

- [x] 6.1 `installSkill` пишет `.manifest.json`: версия из `package.json`
      пакета, хэш каждого положенного файла по пути относительно каталога
      скилла
- [x] 6.2 Сравнение идёт с манифестом: совпал хэш — файл обновляется молча,
      не совпал — попадает в `diverged` и ждёт `--force`
- [x] 6.3 Манифеста нет — команда падает на сравнение с источником, как
      сегодня
- [x] 6.4 Отчёт получает четвёртое число: создано, обновлено, совпало,
      расходится. Команда печатает четыре числа
- [x] 6.5 Спека установки: нетронутая старая копия, правленый файл, копия без
      манифеста, `--force`
- [x] 6.6 `yarn pack:check` зелёный, манифеста в тарболе нет

## 7. Документация

- [x] 7.1 `packages/nestling.agent-skill/README.md` и `README.ru.md`:
      манифест, поведение повторной установки, четырнадцать файлов, плашка
      статуса
- [x] 7.2 `docs/decisions/ideas.md`: пункт 4 записи [2026-09-14] «Разбор
      отчёта об обновлении до 0.3.0» несёт пометку «РЕАЛИЗОВАНО» по правилу 9
      `docs/README.md` — что вышло целиком, что уехало дальше, чем
      реализация уточнила решение
- [x] 7.3 `docs/decisions/roadmap.md`: статус строки 105, ссылка на архив и
      на спеки; строка 109 остаётся планом
- [x] 7.4 Оглавление `ideas.md` пересобрано:
      `node .claude/skills/docs-audit/scripts/ideas-toc.mjs`

## 8. Definition of Done

- [x] 8.1 Все задачи разделов 1–7 отмечены
- [x] 8.2 `yarn verify` зелёный. Прогон без кэша — `yarn verify:fresh`:
      закэшированный результат nx зелёному не свидетель
- [x] 8.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 8.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 8.5 Запись `ideas.md`, по которой шёл change, несёт пометку
      «РЕАЛИЗОВАНО»
- [x] 8.6 `yarn docs:audit` — 0 ERROR
- [x] 8.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [x] 8.8 Коммиты осмысленные, `main` не тронут: слияние делает Merger после
      `/opsx:archive`
