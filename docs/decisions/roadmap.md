# Roadmap доработок до целевого состояния

План работ по приведению кода к целевому дизайну из [ideas.md](./ideas.md).
Каждая строка — отдельный OpenSpec change (`openspec/changes/<имя>/`); статус
обновляется по ходу. Это живой документ, в отличие от append-only журнала
решений.

План до целевого состояния V1 — строки 1–28 — закрыт 2026-08-01 волнами 0–6.
Порядок тех волн, граф зависимостей, итог каждой волны и происхождение
остальных строк заморожены в
[history/worklog/roadmap-waves-0-6.md](../history/worklog/roadmap-waves-0-6.md).
Строки 29 и дальше заводятся по мере находок, в волны не входят и идут в
порядке отдачи.

## Что осталось

Десять строк.

| # | Change | Размер | Сессий | Что нужно до `propose` |
|---|---|---|---|---|
| 105 | `agent-skill-upgrade` | L | 4–6 | ничего: решение в записи [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0» |
| 104 | `release-notes-0.2.0` | S–M | 1–2 | ничего |
| 109 | `deleted-names-check` | S–M | 1–2 | ничего; идёт после 105 и закрепляет его результат |
| 107 | `include-deps-default` | S | 1 | ничего |
| 108 | `argv-owns-process` | S | 1 | ничего |
| 78 | `auth-layer` | M | 2–3 | дизайна нет, есть постановка в [d/10 §3.6](../history/discussions/10-framework-review-2.md) |
| 80 | `readme-positioning` | S | 1 | ничего |
| 96 | `eslint-rules` | S | 1 | ничего: предпосылка 91 закрыта |
| 98 | `transport-ws` | M–L | 3–4 | дизайна нет: открыта семантика двунаправленного вызова |
| 99 | `transport-grpc` | L | 4–6 | дизайна нет: открыт источник схем — Standard Schema или `.proto` |

Первыми идут 105 и 104: устаревший скилл учит агентов удалённому API каждый
день, а обновление с 0.1.x без заметки о выпуске 0.2.0 не описано вовсе.
Дальше порядок свободный — предпосылки внутри плана закрыты у всех.

Порядок величины остатка — 19–27 сессий `apply` плюс пять на `propose`, то
есть 24–32 сессии. Семь—десять из них забирают два транспорта, и у обоих
дизайн начинается с чистого листа.

## План

Колонка «Размер» переводится в сессии так: S ≈ одна сессия `apply`,
S–M ≈ 1–2, M ≈ 2–3, M–L ≈ 3–4, L ≈ 4–6; плюс ~0.5 сессии на `propose`.

Колонка «Откуда» называет, чем строка порождена:

| Значение | Что означает |
|---|---|
| `волна N` | строка плана V1, волна N — [заморожено](../history/worklog/roadmap-waves-0-6.md) |
| `аудит` | аудит кода 2026-07-06, с которого план начался |
| `d/NN` | дискуссия или обзор [history/discussions/NN](../history/discussions/) |
| `ideas ММ-ДД` | запись [ideas.md](./ideas.md) за этот день 2026 года |
| `deferred` | сработал триггер возврата темы из [deferred.md](./deferred.md) |
| `apply #NN` | найдено при реализации строки NN |
| `из #NN` | выделено из строки NN на планировании |
| `сверка` | сверка роадмапа с журналом 2026-09-09 |
| `находка` | замечено в работе, отдельной записи нет |

Ломающие строки после волны 2 заведены осознанно: они правят гарантии, а не
добавляют способности. Пользователей у фреймворка нет, поэтому гарантия стоит
дороже совместимости, и цена правки растёт с каждым приложением, написанным
поверх старого поведения.

| # | Change | Откуда | Суть | Размер | Статус |
|---|---|---|---|---|---|
| 1 | `transport-hardening` | аудит | утечка stack trace в 500-ответах, лимит body, таймауты, 400 вместо 500 для ошибок входа, дренаж `close()` | S | **done** — [архив](../../openspec/changes/archive/2026-07-07-transport-hardening/) |
| 2 | `container-fixes` | аудит | module-метаданные функциональных провайдеров, накопление lifecycle-метаданных per-instance, JSDoc `get()` | S | **done** — [архив](../../openspec/changes/archive/2026-07-07-container-fixes/) |
| 3 | `abort-signal` | аудит | `meta.signal` (AbortSignal) насквозь: транспорт (дисконнект) + App (shutdown) | S–M | **done** — [архив](../../openspec/changes/archive/2026-07-07-abort-signal/) |
| 4 | `pipeline-v2` | аудит | фазы `.pre/.ok/.catch/.after/.finally`, `makePipeline`, слои + `compose`, `TNeeds`, рантайм-тесты ядра | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-07-13-pipeline-v2/) |
| 5 | `token-families` | волна 1 | `makeTokenFamily`, `.auto`, `familyProvider`; ~~опционально `strictExports`~~ — СУПЕРСИД 2026-09-01 (change `remove-module-exports`). **Покрывает и конфиг (`Config(key)`), и on-demand-клиенты (`GrpcClient(server)` + unbound properties)** — см. [discussions/05 §15](../history/discussions/05-modular-monolith-features-ports.md#15) | M | **done** — [архив](../../openspec/changes/archive/2026-07-29-token-families/); разблокирует 9, 14, 16 |
| 6 | `streaming-v2` | волна 3 | `stream` ≠ `events`, item-цепочки на io-декларации, `Topic`, `summary`, SSE; io-декларация как дерево форм (`value`/`stream`/`events`/`multipart` + `upload()`, листья — Standard Schema), поэлементная валидация; capability-валидация биндинга: формы контракта vs способности транспорта, fail-fast на ASSEMBLE | L | **done** — [архив](../../openspec/changes/archive/2026-07-31-streaming-v2/), [ideas.md [2026-07-06]](./ideas.md), новый пакет `@nestling/streams` (влит в [`@nestling/operations`](../../packages/nestling.operations/) change'ем `package-consolidation`); `Topic` отсюда переиспользуют 9 (reloadable) и 11 (InProcessBus) — значит, до волны 4 |
| 7 | `subscriptions-registry` | волна 6 | пакет реестра подписок поверх signal + finish-хуков (dogfooding публичных примитивов): слой `tracked` из двух класс-юнитов, `meta.subscription.signal` = `AbortSignal.any(запрос, админский)`, `CloseReason = Outcome \| 'killed'`, лента на `Topic`, факты `event`-контрактами (opt-in), параметризованный модуль; ядро не тронуто ни строкой | M | **done** — [архив](../../openspec/changes/archive/2026-08-01-subscriptions-registry/), новый пакет [`@nestling/subscriptions`](../../packages/nestling.subscriptions/), [гайд](../history/superseded/guides/subscriptions.md), отчёт о замере [ideas.md [2026-08-01]](./ideas.md); финальная проверка тезиса состоялась: satellite написан, ядро не тронуто, четыре находки — в журнале |
| 8 | `endpoint-discovery` | волна 2 | эндпоинты и транспорты — дискавери из дерева зарегистрированных модулей вместо глобального registry (чинит протечку глобального `Set` при любом импорте). Предпосылка фич | S | **done** — [архив](../../openspec/changes/archive/2026-07-29-endpoint-discovery/), [d/05 §1](../history/discussions/05-modular-monolith-features-ports.md) |
| 9 | `config-module` | волна 4 | `makeConfig('prefix', schema)` + `from`; источники = объекты `ConfigSource` в одной приватной читалке (env — база, координаты из примордиального env); приватность = keys-capability (токен секции не экспортируется, наружу — branded-хэндл `.keys`; без `configs:`-регистрации и build()-проверки владения); привязка в корне плоским списком `config: [[src, keys \| glob]]`; reloadable (`Topic`/`AbortSignal`, живой хэндл); on-demand/unbound + доки из реестра (тег фичи из графа + флаг). Поверх `token-families` (5) | M–L | **done** — [архив](../../openspec/changes/archive/2026-07-31-config-module/), новый пакет `@nestling/config` (влит в [`@nestling/app`](../../packages/nestling.app/) change'ем `package-consolidation`), [d/05 §11,§15](../history/discussions/05-modular-monolith-features-ports.md), ревизия владения [ideas.md [2026-07-10]](./ideas.md), форма секции — рекорд полей [ideas.md [2026-07-14]](./ideas.md); поверх 5 и `Topic` из 6 |
| 10 | `features` | волна 4 | `makeFeature`/`select`/`assemble`; `@OnStart`/go-live (гарантия `dispatch`: `serve(dispatch, signal)` вместо `listen()`); транспорты как провайдеры; capability = DI + fail-fast | L | **done** — [архив](../../openspec/changes/archive/2026-07-31-features/), [`@nestling/app`](../../packages/nestling.app/), [d/05 §2,§7–§10](../history/discussions/05-modular-monolith-features-ports.md); `assemble`, фазовый lifecycle, `@OnStart`/go-live, транспорты-провайдеры |
| 11 | `ports` | волна 5 | `makeContract` (request/command/event), `Port`/`Emitter`, `IMessageBus`, `InProcessBus`, dispatch-policy; local/remote-биндинг на сборке (co-located, L3) | L | **done** — [архив](../../openspec/changes/archive/2026-07-31-ports/), новый пакет `@nestling/ports` (влит в [`@nestling/app`](../../packages/nestling.app/) change'ем `package-consolidation`), [гайд](../history/superseded/guides/ports.md), [ideas.md [2026-07-31]](./ideas.md), [d/05 §3](../history/discussions/05-modular-monolith-features-ports.md); `makeContract`, `Port`/`Emitter`, `InProcessBus`, dispatch-политики |
| 12 | `transport.nats` | волна 5 | NATS как inbound+outbound транспорт; queue-groups для реплик; remote-биндинг портов; JetStream для `durable` (split, L4) | M | **done** — [архив](../../openspec/changes/archive/2026-07-31-transport-nats/), новый пакет [`@nestling/transport.nats`](../../packages/nestling.transport.nats/), пример разнесения фич по процессам (сегодня — [`modular-app`](../../examples/modular-app/)), [ideas.md [2026-07-31] «NATS: шина приложения»](./ideas.md), [d/05 §3](../history/discussions/05-modular-monolith-features-ports.md); remote-биндинг, queue-groups, JetStream, wire-часть `propagate` из 16; пакет `@nestling/transport.nats` и пример `examples.split-nats` |
| 13 | `plugins` | волна 4 | cross-cutting: инфра = параметризованные модули (конвенция, нового примитива нет); `plugins:` в корне не будет — перечень полей `assemble` закрыт; идентичность модуля — значение, одноимённые разные значения — ошибка сборки; pipeline-слои + policy-check вместо ambient middleware; feature-scoped инфра едет с фичей | S | **done** — [архив](../../openspec/changes/archive/2026-07-31-plugins/), [d/05 §16](../history/discussions/05-modular-monolith-features-ports.md); поверх 10 и машинерии 28 |
| 14 | `multi-injection` | волна 1 | `Family.all` — синтетический узел-агрегат: массив всех зарегистрированных членов семейства на `build()` (multi-injection без `multi: true`; вклады — обычные провайдеры с членскими токенами) | S | **done** — [архив](../../openspec/changes/archive/2026-07-29-multi-injection/), [ideas.md [2026-07-10]](./ideas.md); тот же файл-фронт в контейнере, что и 5 — дешевле сразу следом |
| 15 | `error-model` | волна 3 | Fail — значение (возврат ≡ бросок; фикс `normalizeResponse`: возвращённый `Fail` сейчас уезжает как `200 OK`); `Output<T>` включает `Fail`, дискриминант `isFail`; словарь статусов (`CONFLICT`, `TIMEOUT`, `TOO_MANY_REQUESTS`) + `code`/`cause`; `defineFail` (code-идентичность вместо instanceof); `errors:` в контракте endpoint'а, типизированный канал (`Output<T, E>` + бросатель `meta.fail`); граница нормализует незадекларированное в `UnknownError` → закрытый контракт `E ∪ UnknownError` | M | **done** — [архив](../../openspec/changes/archive/2026-07-31-error-model/), [ideas.md [2026-07-10]](./ideas.md); баг-фикс поставлен: возвращённый `Fail` больше не уезжает как `200 OK` |
| 16 | `async-context` | волна 4 | `contextVar<T>()('key')` + писатель `Var.provide(…)` + инжектируемые ридеры `Ctx(Var)` (token family); read-only ALS-проекция накопленного `input` (+ `signal`), писатель ячейки — только рантайм пайплайна; `get()/peek()` (зеркало полный/Partial); opt-in policy-check присутствия — предикат `hasVar` на `build()`; `contextValue` в тестовом корне | M | **done** — [архив](../../openspec/changes/archive/2026-07-31-async-context/), [ideas.md [2026-07-10]](./ideas.md); `propagate` через remote-порты — вместе с 12; ридеры `Ctx(Var)` — семейство из 5, предикат `hasVar` — машинерия 28 |
| 17 | `pipeline-drop-after` | волна 2 | убрать `.after` из билдера/типов/рантайма (`ResponsePhase` = `'ok' \| 'catch'`); словарь ответного тракта — Promise-тройка `ok`/`catch`/`finally`; правка спеков и доков (`docs/preview`) | S, breaking | **done** — [архив](../../openspec/changes/archive/2026-07-29-pipeline-drop-after/), [ideas.md [2026-07-10]](./ideas.md); чистое удаление фазы; после появления новых слоёв дорожает |
| 18 | `testing-package` | волна 4 | `@nestling/testing`: `assembleTest` (`overrides` только в тестовом корне; подстановка на ASSEMBLE + прунинг осиротевших поддеревьев; фазы 0–3 без START → in-proc `app.call`/`app.emit` по схемам; `await using` → SHUTDOWN), `vars()` (объектный `ConfigSource` c `watch`/`set`), `stub(Contract, impl)` (фейк-порт, валидируемый схемой контракта), `familyOverride`, `.check()` (фазы 0–1, матрица `select`-топологий в CI), `testModule()`; конвенция `./testing`-subpath (conditional export) | M | **done** — ядро: [архив](../../openspec/changes/archive/2026-07-31-testing-package/); остаток (`stub(Contract, impl)`, `app.emit`, `app.stubbed`): [архив](../../openspec/changes/archive/2026-08-01-testing-stub-contract/); пакет [`@nestling/testing`](../../packages/nestling.testing/), [ideas.md [2026-07-10]](./ideas.md); поставлен **раньше, чем требует граф**: `assembleTest`/`vars()`/`.check()` удешевляют каждый следующий change; `stub(Contract)` и `app.emit` — [остаток в волне 6](#волна-6--экосистема-и-выход) |
| 19 | `standard-schema` | волна 2 | ядро принимает `StandardSchemaV1` вместо `z.ZodType`: `parsePayload`/`DomainType` через `~standard.validate`/`InferOutput`; `SchemaValidationError` несёт стандартные `issues` вместо `ZodError`; zod → devDependency; Promise из `validate` = ошибка | S–M, breaking | **done** — [архив](../../openspec/changes/archive/2026-07-29-standard-schema/), [ideas.md [2026-07-13]](./ideas.md); самый нижний слой (валидация): чем раньше, тем меньше кода написано против `z.ZodType` |
| 20 | `openapi` | волна 6 | `@nestling/openapi`: генерация OpenAPI из деклараций endpoints; конвертеры `SchemaDocConverter` — явные, по `~standard.vendor`, отдельными пакетами (`@nestling/openapi.zod`, …); boot-time проверка конвертируемости всех схем; `jsonSchema`-override; `errors:` → responses; предпосылка — bind-карта (21) | M–L | **done** — [архив](../../openspec/changes/archive/2026-08-01-openapi/), новые пакеты [`@nestling/openapi`](../../packages/nestling.openapi/) и [`@nestling/openapi.zod`](../../packages/nestling.schema.zod/), [гайд](../history/superseded/guides/openapi.md), [ideas.md [2026-07-13]](./ideas.md); все предпосылки закрыты: 19 (конвертеры), 8, 21 (bind-карта), 15 (`errors:`) |
| 21 | `input-bind` | волна 2 | канон размещения HTTP-input + bind-карта: детерминированное `(pattern, метод, пометки) → path/query/body`; сахар-пометки `query()` (заголовки — только по пометке, `header()` отложен в deferred.md) → плоская bind-карта — несущий уровень для транспорта/OpenAPI/клиента; разворачивание и fail-fast на создании декларации; strict-приём вместо merge (уходит `PayloadConflictError`); query-массивы (фикс last-wins); opt-in `rawBody: true` в HTTP-словаре — байты в типизированном стартовом контексте (webhook-подписи) | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-07-30-input-bind/), [ideas.md [2026-07-13]](./ideas.md); поверх новых деклараций; несущий уровень для 20 и 22 |
| 22 | `contract-clients` | волна 5 | `@nestling/contracts` (ныне `@nestling/operations`) (`makeContract` с `http:`-биндингом, `defineFail`; zero runtime deps — только Standard Schema types) + `@nestling/client`: `makeClient(record, { baseUrl, headers })` → API-объект, возврат `Ok\|Fail` (call-site ≡ порту); рематериализация `Fail` по `code`; валидация ответа по `output`-схеме (`~standard.validate`); streaming-клиент — v2 (после 6) | M → L | **done** — [архив](../../openspec/changes/archive/2026-08-01-contract-clients/), идея [ideas.md [2026-07-13]](./ideas.md), реализация [ideas.md [2026-08-01]](./ideas.md); `@nestling/contracts` (переезд декларативного слоя, секция `http:`, контракт-форма `httpEndpoint`) + `@nestling/client`; оценка M не сошлась: цена не в новой логике, а в физическом переезде слоя между пакетами |
| 23 | `pipeline-type-dx` | волна 2 | бюджет на DX типов pipeline: типы-ошибки в точке `compose` (читаемый литерал `__error` + `missing` вместо трассировки дженериков), snapshot-тесты текстов диагностик по фикстурам неправильных композиций, бенчмарк tsserver (~50 слоёв) с порогом в CI; попутно — сигнатура `compose` на прямой вывод тип-параметров (`TS2589` на 20 слоях уходит) | S–M | **done** — [архив](../../openspec/changes/archive/2026-07-31-pipeline-type-dx/), [ideas.md [2026-07-13]](./ideas.md); закрывает окно: снапшоты диагностик и порог tsserver снимаются с уже зафиксированных типов |
| 24 | `endpoint-model` | волна 2 | уход классовых endpoint-деклараций: канон — декларации-значения через per-transport конструкторы (`httpEndpoint`/`cliEndpoint` — типизированный словарь: path-параметры, bind-карта из 21); `deps`-инжект + формы хендлера (функция / каррированная фабрика / класс-хендлер через контейнер); `endpoints:` модуля принимает значения; удаление `@Endpoint`/`@HttpEndpoint`/`IEndpoint`/endpoint-registry; standalone-гарантия в типах (`route` — только deps-free); перевод `examples.app-with-http` и гайдов; классы остаются DI-формой провайдеров/юнитов/хендлеров; онтология — контракт первичен: конструкторы = сахар «анонимный контракт + `implement`»; CLI-биндинг — политика сбора недостающего input из схемы (`missing: 'prompt'`) — **вне scope этого change'а** | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-07-30-endpoint-model/), [ideas.md [2026-07-13]](./ideas.md) |
| 25 | `config-secrets` | волна 6 | `secret(leaf)` в `makeConfig` (каноническая композиция `secret(from(...))`; редактирование в трёх поверхностях: текст **и объект** `ConfigValidationError`, display-хуки проекции `toJSON`/`inspect.custom`, снимок реестра; незаданный ключ не редактируется); семантика общих ключей: независимая валидация каждой секцией, fail-fast на несогласованном `reloadable` в границах сборки, секретность по объединению **объявленных** читателей, перечень читателей в `describeConfig()` | S | **done** — [архив](../../openspec/changes/archive/2026-08-01-config-secrets/), идея [ideas.md [2026-07-13]](./ideas.md), реализация [ideas.md [2026-08-01]](./ideas.md); `secret(from(...))`, три поверхности редактирования, общий ключ и конфликт `reloadable` в границах сборки; ядро не тронуто нигде, кроме `@nestling/config` |
| 26 | `contract-versioning` | волна 5 | версия явно в имени контракта; `describeContract` → дескриптор-значение (листья — через `SchemaDocConverter`, без конвертера честно непрозрачны); `snapshotContracts` сводит `.check()`-матрицу объединением топологий; `diffContracts` с закрытым словарём `breaking`/`additive`/`unknown` и направлением по слоту; отчёт-значение плюс `formatCompatibility` и подсказка bump'а — подсвечивает, не блокирует (флага блокировки не существует) | S–M | **done** — [архив](../../openspec/changes/archive/2026-07-31-contract-versioning/), [гайд](../history/superseded/guides/ports.md), [ideas.md [2026-07-31]](./ideas.md), [ideas.md [2026-07-13]](./ideas.md); снапшот из дискавери, дифф с тремя вердиктами, отчёт в `.check()`-матрице (18) |
| 27 | `port-deadline-idempotency` | волна 5 | `meta.deadline` (gRPC-модель: абсолютный момент `Date` в процессе, относительный `timeoutMs` по проводу, пересчёт на приёме; fail-fast до вызова и до обработки, отмена в полёте; встроенный код `DEADLINE_EXCEEDED`, определение `DeadlineExceeded` — в `@nestling/pipeline`, где живёт закрытый набор); `idempotencyKey` в meta **только** у `command` (`MetaOf<C>` по виду; ключ чеканит вызыватель, если не дан; провоз конвертом шины; дедупликация — satellite, не ядро); профиль двумя каналами — `raw.attributes` и переменные `Deadline`/`IdempotencyKey` | M | **done** — [архив](../../openspec/changes/archive/2026-07-31-port-deadline-idempotency/), [гайд](../history/superseded/guides/ports.md), [ideas.md [2026-07-31]](./ideas.md), [ideas.md [2026-07-13]](./ideas.md); `meta.deadline` моментом, `idempotencyKey` у команд, конверт шины |
| 28 | `policy-check` | волна 4 | инварианты на собранном графе: `assemble({ policies })`, `everyEndpoint(фильтр).hasLayer(ref)` (идентичность слоя — по ссылке); `detached: '<причина>'` (строка обязательна) + печать detached-ручек на старте; ESLint-правило как editor-фидбек; машинерия для 13 (plugins) и 16 (async-context), прогон в `.check()`-матрице (18) | S–M | **done** — [архив](../../openspec/changes/archive/2026-07-31-policy-check/), новый пакет [`@nestling/eslint-plugin`](../../packages/nestling.eslint-plugin/), [ideas.md [2026-07-14]](./ideas.md); нужен был полный граф — после 8 (дискавери) и 10 (assemble) |
| 29 | `input-validation-builtin` | ideas 08-29 | проверка входа по `input` — обязанность рантайма: одна точка после всех `.pre`-юнитов и перед хендлером, кандидат — `payload` из контекста или `raw.payload`, отказ от проверки объявляется схемой `z.unknown()`; **BREAKING** — юнит `validate()` удалён, ветка «без пайплайна» в `dispatch` заменена пустым пайплайном (один путь исполнения), копия проверки `multipart` убрана из HTTP-транспорта; новый kernel-код `PAYLOAD_TOO_LARGE` доводит 413 лимита потокового входа до клиента на обоих видах деклараций | M | **done** — [архив](../../openspec/changes/archive/2026-09-01-input-validation-builtin/), идея [ideas.md [2026-08-29]](./ideas.md), реализация [ideas.md [2026-09-01]](./ideas.md); гарантия «хендлер получает проверенный вход» держалась на дисциплине «не забудь `validate()`»; вместе с ней сведены в один два пути исполнения endpoint'а |
| 30 | `remove-module-exports` | ideas 09-01 | удаление `Module.exports` и опции `strictExports`: модуль остаётся меткой принадлежности и единицей упаковки, границу держат ES-модули и границы пакетов; `metadata.exported` уходит из узла графа, вклад в семейство объявляется одним провайдером в `providers` | S, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-01-remove-module-exports/), [ideas.md [2026-09-01]](./ideas.md); модуль остаётся меткой принадлежности, границу держат ES-модули |
| 31 | `composition-model` | ideas 09-02 | три роли слоя приложения: модуль (метка принадлежности, `dependsOn` вместо `imports`, без `endpoints`), фича (`makeFeature`, без `dependsOn`) и плагин (`makePlugin`, поле корня `plugins:`); граница фичи проверяется обходом собранного графа; токен становится объектом с идентичностью по ссылке, члены семейств хранят принадлежность полями; `select: { features, includeDeps }`; `intercom:` назначает роль переносчика операций ссылкой на объявленный транспорт; транспорты — именованные экземпляры с `on:` в декларации; `makeContract({ kind })` заменён на `makeRequest`/`makeCommand`/`makeEvent`, `.port` → `.caller`; слово «контракт» ушло из словаря целиком — пакет `@nestling/contracts` стал `@nestling/operations` | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-02-composition-model/), [ideas.md [2026-09-02]](./ideas.md); фича, плагин и операция как три роли с разными правилами; граница фичи проверяется на собранном графе, токен стал объектом, `intercom:` назначает роль ссылкой на транспорт |
| 32 | `guide-review-1` | ideas 09-03 | декларация приложения `makeApp` → `app.assemble(select)` → `AssembledApp`, `check(select)` на декларации, `assembleTest(app, …)`; код отказа `category:detail` вместо `status` + `code`, `makeFail`, отказы ядра голой категорией, `E ∪ InternalError`, статусы в нижнем регистре; поле `handler` с тремя формами, endpoint создаёт класс-хендлер сам, `meta.fail` удалён, `Output<T, typeof Def>`; заголовки `Ok` не зависят от транспорта; главы 1–5 гайда переписаны по ревью (health в плагине, глава «Хендлер как класс», DI после неё, `conventions.md`), примеры мигрированы; превью собирается из `docs/guide` | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-03-guide-review-1/), [ideas.md [2026-09-03]](./ideas.md) |
| 33 | `handler-two-forms` | d/09 | удаление объектной формы `{ deps, handle }`: две формы хендлера — функция без зависимостей и класс; уходят перегрузки `httpEndpoint`/`cliEndpoint`/`implement`, позиционный `resolve(instances)`, ветка discovery; `@Injectable` сверяет длину списка зависимостей с конструктором; type-tests диагностик `httpEndpoint` в `transport.http`; примеры и главы 5, 12, 13, 17, приложение А | S–M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-05-handler-two-forms/), [ideas.md [2026-09-04]](./ideas.md) «Две формы хендлера», «`@Injectable` сверяет длину списка»; объектная форма хендлера нарушала правило «зависимости из контейнера получает класс», не поддерживала `Family.auto` и держала две лишние перегрузки у каждого конструктора деклараций; идёт первым, потому что `layer-fails` меняет те же перегрузки |
| 34 | `layer-fails` | d/09 | отказы слоя: `.pre(unit, { errors })`, `TFails` у пайплайна и множество определений на значении, канал `return` у pre-юнита, эффективное множество `errors` endpoint'а (граница, OpenAPI, `Output`), проверка «отказы пайплайна входят в `errors:` операции» в типах и при создании декларации; `Output<T, E>` допускает отказы ядра, `void` у хендлера без `output`; главы 3, 9, 13, приложение А | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-05-layer-fails/), [ideas.md [2026-09-04]](./ideas.md) «Отказы слоя», «`Output<T, E>` допускает отказы ядра»; отказ pre-юнита объявлялся на каждом endpoint'е со слоем, забытое объявление давало молчаливый `500`, OpenAPI не показывал `401`; вместе с ним закрываются два шва типа результата: отказы ядра в `Output` и `void` без `output` |
| 35 | `http-transport-boundary` | d/09 | граница `@nestling/transport.http` в README пакета (обещания и то, что не входит: HTTP/2, WebSocket, TLS); байтовые части как публичная поверхность — satellite-транспорт поверх своего `node:http`-сервера собран из одних публичных экспортов в спеке пакета, экспорт `HTTP_CAPABILITIES`; бенчмарк относительно Fastify с записью результата в ideas.md | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-http-transport-boundary/), новая спека [`http-transport-boundary`](../../openspec/specs/http-transport-boundary/spec.md), [ideas.md [2026-09-04]](./ideas.md) «Граница `@nestling/transport.http`»; граница собственного HTTP-транспорта не была записана рядом с кодом, а состав экспортов никто не сверял со списком байтовых частей: аудит нашёл один пробел, satellite-спек его закрыл |
| 36 | `guide-concept-map` | d/09 | README гайда: приложение Б вторым документом сразу после вводного абзаца, раздел «Карта понятий» — таблица «понятие, одно предложение, глава» на 25 строк по частям 1–4; спека `docs-preview-guide` получила требование о разделах README вне «Часть N.»/«Приложения» | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-guide-concept-map/), [ideas.md [2026-09-04]](./ideas.md) «Подача гайда»; формулировка прогрессивного раскрытия в `principles.md` синхронизирована раньше и в объём change'а не входила; проблема была в первом впечатлении от числа понятий, а не в их числе: словарь называется до первого раздела, который его употребляет, а читатель из NestJS видит приложение Б вторым документом |
| 37 | `abort-signal-registry` | d/10 | сигнал запроса без `AbortSignal.any`: реестр контроллеров в `transport.http`, контроллер вызова со слушателем в бюджете и in-process шине `@nestling/ports`; бенчмарк печатает разброс и максимум латентности; Node 24 в README | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-abort-signal-registry/), [ideas.md [2026-09-05]](./ideas.md) «Сигнал отмены запроса», «Целевая версия Node — 24»; профиль показал около 20% сэмплов в `AbortSignal.any` и паузы до 1.8 с: композит копил `WeakRef` на сигнале остановки по числу запросов; реестр контроллеров снял паузы, а замер цены `AsyncLocalStorage` в Node 22 перевёл репозиторий на Node 24 |
| 38 | `hot-path-trim` | d/10 | обвязка запроса без лишней работы: `writeHead` с `content-length` и тело буфером, адрес без `new URL`, запись маршрута с bind-картой и формами, инварианты пайплайна в конструкторе, меньше `await`, контекст в одном объекте; каждый пункт по замеру `nestling,fastify` | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-05-hot-path-trim/), [ideas.md [2026-09-05]](./ideas.md) «Горячий путь»; с равными обязанностями Fastify теряет против голого 2%, Nestling отставал на 20% — разрыв давала обвязка запроса, а не гарантии; шесть пунктов по замеру дали +15% на `GET` и +13% на `POST` |
| 39 | `examples-out` | ideas 09-05 | шесть примеров из `packages/examples.<name>` в `examples/<name>`: `packages/` содержит только публикуемые пакеты, имена `@examples/<name>`, `private: true` у каждого; `smoke.mjs` обходит `packages/` без фильтра по префиксу, `docs-audit` резолвит пример из плашки главы как `examples/<name>`; 278 подписей сниппетов и 25 плашек гайда переписаны на новый путь | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-examples-out/), новая спека [`examples-layout`](../../openspec/specs/examples-layout/spec.md); первый change серии из трёх (`examples-out`, `common-inline`, `package-consolidation`); `packages/` смешивал публикуемые пакеты с иллюстрациями к гайду, три примера из шести публиковались бы первым же `lerna publish`, а обход каталога опирался на префикс имени там, где хватило бы каталога |
| 40 | `docs-layout-cleanup` | d/11 | уборка документации, волна 1: генератор и тема сайта в `scripts/site/`, вывод в `docs/.site/` вне git, команды `docs:build` и `docs:dev`; правила ведения одним экземпляром в `docs/README.md`, корневые README без таблиц пакетов, примеров и глав, приложение В удалено; `docs:audit` проверяет, что сгенерированный HTML не отслеживается git | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-docs-layout-cleanup/), новая спека [`docs-site`](../../openspec/specs/docs-site/spec.md) вместо `docs-preview-guide`, [d/11](../history/discussions/11-docs-structure-and-site.md); сгенерированный HTML лежал в git и давал диффы в двух местах, правила ведения были написаны трижды, таблицы пакетов и глав — тоже; генератор сайта переехал в `scripts/site/`, вывод в `docs/.site/` вне git, приложение В удалено — [ideas.md [2026-09-05] «Структура документации»](./ideas.md), [d/11](../history/discussions/11-docs-structure-and-site.md) |
| 41 | `packaging-cleanup` | ideas 09-05 | два дефекта упаковки: `tsconfig.build.json` не исключал `*.type-test.ts` и `__fixtures__`, и они попадали в `dist` вместе с импортом двух `devDependencies`; манифесты семи пакетов расходились с фактическими импортами | S | **done** — [архив](../../openspec/changes/archive/2026-09-05-packaging-cleanup/), новая спека [`packages-layout`](../../openspec/specs/packages-layout/spec.md); сплошная сверка добавила к семи расхождениям ещё два и две правки в манифестах `examples/`, [ideas.md [2026-09-05]](./ideas.md) «Раскладка монорепы»; опубликованный `@nestling/testing` нёс в `dist` файлы проверки типов и фикстуры, а один из них импортировал `@nestling/transport.http` и `zod` из `devDependencies`: у установившего их нет; сплошная сверка нашла девять расхождений «объявлено ≠ импортируется» у семи пакетов и ещё два в `examples/` |
| 42 | `package-consolidation` | ideas 09-05 | ядро в три пакета: `container`, `operations` (+ формы io, `streams`), `app` (+ `pipeline`, `config`, `ports`, `transport`); три пакета `@common/*` остаются под своими именами; 22 каталога `packages/` становятся 17, 63 ребра — 29, восемь уровней глубины — шесть; приложение импортирует из трёх пакетов вместо восьми; направление зависимостей держит правило, миграция примеров, глав гайда, README и `design/composition.md` | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-06-package-consolidation/), новая спека [`core-package-layout`](../../openspec/specs/core-package-layout/spec.md), [ideas.md [2026-09-05]](./ideas.md) «Раскладка монорепы»; третий change серии из трёх (`examples-out`, `packaging-cleanup`, `package-consolidation`); восемь пакетов ядра не дают выбора: `pipeline` тянет `container`, `operations` и `streams`, остальные тянут `pipeline` — их ставят вместе; идёт последним, по дереву, очищенному 39 и 41 |
| 43 | `guide-single-file` | находка | гайд одним файлом: `yarn docs:build` кладёт в `docs/.site/` один `index.html` вместо двадцати восьми страниц; тема и поведение встраиваются в него, ссылки между главами становятся якорями с приставкой главы (`#06-config--секреты`), подсветка кода выполняется на сборке | S, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-06-guide-single-file/), спека [`docs-site`](../../openspec/specs/docs-site/spec.md) переписана на один файл; гайд читают подряд, а отдать его целиком было нельзя: страницы ссылались друг на друга по именам файлов, тема лежала соседними файлами, и копия страницы теряла оформление; подсветка в браузере пропадала везде, где JavaScript не выполняется |
| 44 | `docs-di-token` | d/12 | термин «DI-токен» везде: гайд, README пакетов, JSDoc, комментарии и сообщения ошибок; линтер стиля ловит голое «токен»; токен доступа в главе 9 — «Bearer-токен» | S | **done** — [архив](../../openspec/changes/archive/2026-09-07-docs-di-token/), новая спека [`docs-terminology`](../../openspec/specs/docs-terminology/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Термины и гайд»; голое «токен» в главе 9 означало то Bearer-токен, то DI-токен через десять строк |
| 45 | `sync-assemble` | d/12 | фаза 0 BOOTSTRAP отдельно: источники читаются в снимок до сборки, секции считаются из снимка; `build()` синхронный и без I/O; `factoryProvider` и фабрики провайдеров модулей только синхронные, Promise из фабрики — ошибка сборки; `check()` принимает `config` | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-06-sync-assemble/), новые спеки [`config-bootstrap-snapshot`](../../openspec/specs/config-bootstrap-snapshot/spec.md) и [`synchronous-assembly`](../../openspec/specs/synchronous-build/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Фаза 0 BOOTSTRAP»; фаза 0 из design-дока в коде отсутствовала: источники читала async-фабрика внутри графа, а билдер ждал фабрики вопреки синхронному типу; без снимка до сборки невозможны ни синхронный `check()`, ни переключатели |
| 46 | `resources-and-roles` | d/12 | `@Component`, `@Resource`, `@Handler` с брендами позиций; `resourceProvider`; экземпляры создаются на INIT, `acquire` и `release`, `@OnInit` и `@OnDestroy` удалены, `@Injectable` удалён; `classProvider` вместо перегрузки с DI-токеном; `capabilities` на объявлении транспорта; глава 5 гайда с `Database` как ресурсом | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-07-resources-and-roles/), новые спеки [`class-roles`](../../openspec/specs/class-roles/spec.md), [`resource-lifecycle`](../../openspec/specs/resource-lifecycle/spec.md) и [`instantiation-on-init`](../../openspec/specs/instantiation-on-init/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Ресурсы и роли классов»; проверка `#users \| undefined` в гайде защищала от вызова, который фазы уже исключают; убрать её можно только созданием зависимых после захвата; `@Injectable(token, deps)` смешивал две заботы |
| 47 | `switches` | d/12 | `makeSwitch`, `pick` и `when` во всех списках единиц, `switches:` корня, типизированный аргумент `assemble`, измерение в `check()` и `checkTopologies`, `.schema` для `RootConfig`; формы корня `{ endpoints, providers? }`, `{ endpoints, modules? }`, `{ features }`; глава о композиции после главы 1, главы 2 и 5 в шаг-форме | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-07-switches/), новая спека [`composition-switches`](../../openspec/specs/composition-switches/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Переключатели состава»; состав по значению из окружения выражался либо непрозрачной фабрикой, либо фичей ради одного провайдера; маленьким сервисам навязывалось понятие фичи, а корень уровня L0 повторял состав одной фичи и записать это можно было только через `makeFeature` |
| 48 | `kernel-logger` | d/12 | интерфейс `Logger` с тремя формами вызова и `child`, `RootLogger$` с умолчанием ядра, семейство `Logger$` с `.auto`; ядро пишет только через него, `onWarn` и `onUnknownFail` удалены; секция `nestlingLog`; глава 8 гайда | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-06-kernel-logger/), новая спека [`kernel-logger`](../../openspec/specs/kernel-logger/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Логгер ядра»; ядро писало тремя каналами, приложения заводили логгер заново, семейство `ILogger` жило только в примере |
| 49 | `http-server` | d/12 | `httpServer({ name })` как ресурс, `http({ server })`, секция порта семейством по имени сервера, опция `port` удалена, `listen` последним на START; дубликат «транспорт, паттерн» падает на ASSEMBLE | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-07-http-server/), новые спеки [`http-server-resource`](../../openspec/specs/http-server-resource/spec.md) и [`pattern-uniqueness`](../../openspec/specs/pattern-uniqueness/spec.md), [ideas.md [2026-09-06]](./ideas.md) «HTTP-сервер как ресурс»; transports.md противоречил себе про порт, два экземпляра читали один `HTTP_PORT`, а дубликат паттерна ловился на START после захвата ресурсов |
| 50 | `http-handler-form` | d/12 | `Handler<Op>`, `HttpHandler<Op>`, `HandlerMeta`, `HttpHandlerMeta`, `HttpOutput`, `HttpResponse`; `Ok` без заголовков; стартовый контекст транспорта и юниты `withHeader`, `withClientIp`, `httpAccessLog`; `implement` принимает только хендлер без `http` | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-08-http-handler-form/), новые спеки [`http-handler-form`](../../openspec/specs/http-handler-form/spec.md) и [`transport-pipeline-units`](../../openspec/specs/transport-pipeline-steps/spec.md), [ideas.md [2026-09-06]](./ideas.md) «HTTP-хендлер явной формой»; `Location` в заголовках `Ok` был HTTP под чужим именем, редирект и cookie не выражались вовсе; юниты, знающие транспорт, не имели правила |
| 51 | `config-derived` | d/12 | `derived([deps], fn)` с наследованием секретности и пересчётом, `env({ prefix })`, `describeConfig({ converters })`; глава 6 гайда | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-07-config-derived/), расширены спеки [`config-sections`](../../openspec/specs/config-sections/spec.md), [`config-secrets`](../../openspec/specs/config-secrets/spec.md), [`config-reloadable`](../../openspec/specs/config-reloadable/spec.md), [`config-registry`](../../openspec/specs/config-registry/spec.md) и [`config-sources-binding`](../../openspec/specs/config-sources-binding/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Конфиг: `derived`…»; вычисляемые поля требовали класса-прокси, общий `.env` на несколько сервисов не поддерживался, описания полей не попадали в документацию |
| 52 | `openapi-declaration` | d/12 | `app.discover(args?)` — фаза 0 без продолжения на декларации, вход генератора вместо ручного `discoverEndpoints`; фаза 0 одной функцией `resolveComposition` на сборку и на метод; `discoverEndpoints` уходит из публичных экспортов; скрипт `openapi` в примере `app-with-http` | S, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-09-openapi-declaration/), обновлены спеки [`endpoint-discovery`](../../openspec/specs/endpoint-discovery/spec.md) и [`openapi-document`](../../openspec/specs/openapi-document/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Документ OpenAPI без запуска»; документ для CI собирался тремя строками через `discoverEndpoints` без учёта аргумента сборки; вход генератора — метод декларации `app.discover(args?)`, а не вторая сигнатура `buildOpenApiDocument` |
| 53 | `health` | d/12 | `HealthCheck$` и `Health$` в ядре, флаг `critical`, таймаут и кэш через `nestlingHealth`; `httpProbes()`; `health` у ресурса; главы 1, 9 и 23 гайда | M | **done** — [архив](../../openspec/changes/archive/2026-09-09-health/), новые спеки [`health-probes`](../../openspec/specs/health-probes/spec.md) и [`http-probes`](../../openspec/specs/http-probes/spec.md), [ideas.md [2026-09-06]](./ideas.md) «Пробы»; закрывает [deferred [2026-07-14]](./deferred.md); deferred выводил liveness из фазы, а гайд делал пробу endpoint'ом с `detached`; два дизайна жили параллельно |
| 54 | `outbox` | ideas 09-07 | Пакет `@nestling/outbox`: семейство `outboxed(Op)`, интерфейс `OutboxStore` с реализацией в памяти, relay ресурсом с `drain()`, секция конфига, факты `outbox.published` и `outbox.stuck`, политика `requiresTransaction`; глава 27 гайда | L | **done** — [архив](../../openspec/changes/archive/2026-09-07-outbox/), новая спека [`transactional-outbox`](../../openspec/specs/transactional-outbox/spec.md), [ideas.md [2026-09-07]](./ideas.md) «Транзакционный outbox»; закрывает [deferred [2026-07-13]](./deferred.md) в части outbox'а; событие, отправленное соседу, не переживало падение процесса между коммитом и отправкой — дыра в главном обещании модульного монолита; второй мотив — замер границы ядра на задаче тяжелее реестра подписок |
| 55 | `stream-finish` | сверка | `.finally` у потокового ответа, закрытого до первого элемента: `withFinish` — объект-итератор вместо асинхронного генератора, закрытие доводит до источника обёртка формы `bindOutputStream`, `untilAborted` закрывает источник и в ветке «сигнал уже взведён»; тест границы `core-limits.spec.ts` удалён | S | **done** — [архив](../../openspec/changes/archive/2026-09-09-stream-finish/), обновлены спеки [`pipeline-phase-model`](../../openspec/specs/pipeline-phase-model/spec.md), [`http-request-cancellation`](../../openspec/specs/http-request-cancellation/spec.md) и [`subscription-registry`](../../openspec/specs/subscription-registry/spec.md), находка №4 записи [ideas.md [2026-08-01]](./ideas.md) «Реестр подписок»; контракт с транспортом обещал «потребить итератор либо закрыть его», и вторая половина не работала: подписка, закрытая до первого элемента, оставалась в реестре до конца жизни процесса; дефектов нашлось два — второй в `untilAborted`, через которую потоковый ответ читают оба транспорта |
| 56 | `cli-input-prompt` | сверка | политика сбора недостающего входа в CLI: `missing: 'prompt'` достраивает payload из схемы `input` вопросами в терминале | M | **done** — [архив](../../openspec/changes/archive/2026-09-12-cli-input-prompt/), [ideas.md [2026-07-13]](./ideas.md) «Контракт первичен», п. 7; обещание, вынесенное из scope change'а 24, жило комментарием в коде транспорта, а не строкой плана; попутно переименован пакет-конвертер — `@nestlingjs/openapi.zod` стал `@nestlingjs/schema.zod` |
| 57 | `kernel-boundary-outbox` | сверка | четыре места границы ядра из замера outbox'а закрыты правкой ядра: `Var.provide(deps, compute)` — писатель переменной с зависимостями из контейнера, `EmitMeta` с ключом идемпотентности у события, `makeToken(id, { hint })` с печатью подсказок в ошибке о недостающих зависимостях, тип-параметр словаря `meta` у `Port` и `Emitter`; outbox: раздел аргументом `emit`, `core-limits.spec.ts` удалён | S–M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-12-kernel-boundary-outbox/), новая спека [`dependency-hints`](../../openspec/specs/dependency-hints/spec.md), раздел «Результат замера границы» записи [ideas.md [2026-09-07]](./ideas.md) «Транзакционный outbox»; замер границы ядра нашёл четыре места и намеренно не чинил их: правка ядра ради сателлита обесценила бы сам замер, но сами места остались |
| 58 | `docs-package-readmes` | сверка | волна 2 уборки: шесть разделов README пакета в фиксированном порядке и потолок 120 строк; восемнадцать README переписаны — 1142 строки вместо 6277; четыре инварианта в `docs:audit`, включая совпадение перечня экспортов с барелем в обе стороны; барели `app` и `operations` переписаны поимённым `export` | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-09-docs-package-readmes/), новая спека [`docs-package-readme`](../../openspec/specs/docs-package-readme/spec.md), расширена [`packages-layout`](../../openspec/specs/packages-layout/spec.md), [ideas.md [2026-09-05]](./ideas.md) «Структура документации»; README пакетов писались каждый по-своему и повторяли `design/` и гайд, а проверки на них не было ни одной |
| 59 | `docs-guide-recipes` | сверка | волна 3 уборки: путь из 21 главы в `docs/guide/` и восемь рецептов без номеров в `docs/recipes/`, глава о базе поделена надвое и встала на путь главами 11 и 16, «Что проверяется до первого запроса» и приложение Б переехали в корень `docs/`; генератор берёт источники из `scripts/site/sections.mjs`, `docs:audit` проверяет обе папки и сквозную нумерацию | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-12-docs-guide-recipes/), новая спека [`docs-guide-layout`](../../openspec/specs/docs-guide-layout/spec.md), обновлены `docs-site`, `docs-package-readme`, `examples-layout` и `docs-terminology`, [ideas.md [2026-09-05]](./ideas.md) «Структура документации» и [[2026-09-12]](./ideas.md) «Глава делится по месту в пути»; гайд вёл по пути и одновременно служил справочником, а общая нумерация не давала вставить главу в середину: главу о базе дописали в конец, и шесть глав пути ссылались на неё вперёд — самая дальняя через девятнадцать глав |
| 60 | `docs-site-sections` | сверка | волна 4 уборки: две формы вывода из одной модели разделов — дерево страниц с адресом `/guide/01-first-service/` и файл `nestling-docs.html` со всей документацией сразу; источников десять вместо четырёх, разделов 73 вместо 33, третий вид источника — пакеты; ссылка в неопубликованный файл ведёт на GitHub, поиск по индексу в браузере, метаданные страницы, стартовая страница отдельным шаблоном, `docs:dev` с локальным сервером | M–L | **done** — [архив](../../openspec/changes/archive/2026-09-12-docs-site-sections/), расширена спека [`docs-site`](../../openspec/specs/docs-site/spec.md), [ideas.md [2026-09-05]](./ideas.md) «Структура документации»; сайт собирался одним файлом без разделов, поиска и метаданных — оглавления глубже глав у читателя не было |
| 61 | `docs-publish` | сверка | волна 5 уборки: сайт выкладывается на GitHub Pages по тегу `v*` отдельным workflow `docs.yml`; документация выходит на двух языках целиком — английское зеркало `docs/en/` и README пакета парой `README.md` и `README.ru.md`; генератор печатает два дерева из одной модели, планы уходят с сайта, паритет языков держат шесть инвариантов `docs:audit`, а `ci.yml` прогоняет `docs:audit` и `docs:build` | L, breaking | **done** — [ideas.md [2026-09-05]](./ideas.md) «Структура документации»; сайт собирался локально и никуда не выкладывался, а весь текст был русским: читатель, нашедший пакет в реестре, открывал README и не мог его прочесть |
| 62 | `barrels-by-name` | ideas 09-09 | восемь барелей семи пакетов поимённым `export`, считая подпуть `container/tokens`: публичных имён 117 вместо 230; `export *` запрещён и со специфаером на соседний пакет, реэкспорт `operations` из `transport.http` ушёл целиком; `yarn docs:audit` печатает ERROR на `export *` в бареле поля `exports` | S, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-12-barrels-by-name/), расширена спека [`packages-layout`](../../openspec/specs/packages-layout/spec.md), [ideas.md [2026-09-09]](./ideas.md) «Публичное имя — имя с читателем» (помечена РЕАЛИЗОВАНО, открытый вопрос №1 закрыт); правило про поимённый барель действовало на все пакеты, а выполняли его два из восемнадцати: в остальных барель состоял из `export *`, и публичным было всё, что экспортирует любой внутренний модуль |
| 63 | `npm-publish` | ideas 09-10 | скоуп `@nestlingjs` на все пакеты, внутренние с `common.` в имени, `LICENSE` и `files` в каждом манифесте, поле `main` удалено, проверка упаковки `pack:check` установкой тарболов вне репозитория, релиз тегом `v*` из GitHub Actions | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-11-npm-publish/), новая спека [`package-publication`](../../openspec/specs/package-publication/spec.md), расширены [`packages-layout`](../../openspec/specs/packages-layout/spec.md) и [`testing-subpath-convention`](../../openspec/specs/testing-subpath-convention/spec.md), [ideas.md [2026-09-10]](./ideas.md) «Публикация в npm»; скоуп `@nestling` в реестре занят чужим аккаунтом, тарбол уносил исходники и конфиги, `LICENSE` не было ни одного при `"license": "MIT"` в каждом манифесте |
| 64 | `agent-skill` | ideas 09-11 | пакет `@nestlingjs/agent-skill`: скилл Claude Code про Nestling, команда `npx @nestlingjs/agent-skill`, сниппеты скилла компилируемыми файлами и проверка `bin` в `pack-check` | M | **done** — [архив](../../openspec/changes/archive/2026-09-11-agent-skill/), новые спеки [`agent-skill-content`](../../openspec/specs/agent-skill-content/spec.md), [`agent-skill-package`](../../openspec/specs/agent-skill-package/spec.md) и [`agent-skill-snippet-check`](../../openspec/specs/agent-skill-snippet-check/spec.md), [ideas.md [2026-09-11]](./ideas.md) «Скилл для агента»; пакеты вышли в реестр, и у фреймворка появились пользователи вне репозитория; агенту в чужом проекте документации нет — в `node_modules` только `dist` и README, а гайд и `design/` остались здесь, на русском, и агент пишет по привычкам NestJS |
| 65 | `trusted-publishing` | ideas 09-10 | публикация по OIDC-обмену с доверенным издателем вместо ключа доступа, права `publish` и `stage publish` у издателя, первая версия нового имени с рабочей машины режимом `--interactive`, коммит подъёма версии проверяется один раз | S | **done** — [архив](../../openspec/changes/archive/2026-09-12-trusted-publishing/), обновлены спеки [`package-publication`](../../openspec/specs/package-publication/spec.md) и [`examples-layout`](../../openspec/specs/examples-layout/spec.md), [ideas.md [2026-09-10]](./ideas.md) «Публикация в npm»; первый релиз упал на `403`: реестр требует 2FA от любой публикации, а ключ доступа секретом репозитория этого не даёт и в январе 2027 теряет право публиковать вовсе |
| 66 | `skill-setup` | d/14 | скилл после первого внешнего прогона: спека собирает `app` из сниппетов через `assembleTest`, новые `references/setup.md` (tsconfig, скрипты, `tsx` для разработки, условие `testing`, конфиг `@nestlingjs/eslint-plugin`) и `references/http.md` (`redirect:`, `HttpResponse`, `Ok.created`), правила 6 и 7 в SKILL.md, таблица шести пакетов в «Where to look next», вариант `node:test` | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-12-skill-setup/), обновлены спеки [`agent-skill-content`](../../openspec/specs/agent-skill-content/spec.md) и [`agent-skill-snippet-check`](../../openspec/specs/agent-skill-snippet-check/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Скилл после первого внешнего прогона», [d/14](../history/discussions/14-first-external-run.md) пункты A1–A3, A5–A7, B8; скилл давал код, который не проходил ASSEMBLE: сниппеты компилируются по одному и как приложение противоречат друг другу; настройки проекта и HTTP-формы ответа в скилле не было, и агент искал их опытом |
| 67 | `feedback-polish` | d/14 | пять починок по первому внешнему прогону: runtime-строки на английском во всех пакетах и проверка линтером, страховка от `any` в `ValidComponentShape`, JSDoc без `Ok.of`, `content-length: 0` у пустого ответа, `find-my-way` 9.7 и новее, раздел «когда Nestling не нужен» в корневом README | S | **done** — [архив](../../openspec/changes/archive/2026-09-12-feedback-polish/), новая спека [`runtime-message-language`](../../openspec/specs/runtime-message-language/spec.md), расширены [`class-roles`](../../openspec/specs/class-roles/spec.md) и [`http-transport-boundary`](../../openspec/specs/http-transport-boundary/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Runtime-строки на английском и четыре починки» (помечена РЕАЛИЗОВАНО), [d/14](../history/discussions/14-first-external-run.md) пункты B2, B4, B6, B7, B9, B10; пять мест, о которые первый внешний пользователь споткнулся за час: русские строки в OpenAPI-документе, чужой текст в ошибке `@Component`, JSDoc про несуществующий `Ok.of`, редирект с `transfer-encoding: chunked`, `npm audit` с тремя high по `find-my-way` 8.x |
| 68 | `http-endpoint-two-names` | d/14 | реализация операции получает своё имя `httpEndpoint.implement(Operation, { … })`, у `httpEndpoint` остаётся форма со своим адресом: по две перегрузки на имя вместо четырёх, ложная ошибка на `pipeline:` исчезает; брендированный тип `ValidateHandlerFails` в слоте `handler` называет незаявленный отказ; снапшоты диагностик, миграция примеров, глав и скилла, `references/diagnostics.md` в скилле | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-12-http-endpoint-two-names/), [ideas.md [2026-09-12]](./ideas.md) «Две декларации HTTP вместо четырёх перегрузок»; статики по HTTP-методу вынесены в 72; оценка сошлась: миграция 39 мест механическая, цена оказалась в диагностике — брендирование у хендлера-функции пришлось перенести из слота в возвращаемый тип, потому что в слоте проверка недостижима |
| 69 | `openapi-query-parsed-form` | d/14 | параметр path и query описывается разобранной формой схемы, если она скалярная: второй прогон конвертера с `io: 'output'` в `planInput`, `z.stringbool()` даёт `type: boolean`; тело запроса без изменений | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-12-openapi-query-parsed-form/), обновлена спека [`openapi-document`](../../openspec/specs/openapi-document/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Документ OpenAPI: параметр в разобранной форме» (помечена РЕАЛИЗОВАНО), [d/14](../history/discussions/14-first-external-run.md) пункт B5; канон `z.stringbool()` для query из записи [2026-08-01] документировался как `type: string`, и генератор клиента делал из булева поля строку |
| 70 | `eslint-deps-sync` | d/14 | правило `dependency-list` в `@nestlingjs/eslint-plugin`: список DI-токенов декоратора выводится из типов параметров конструктора по синтаксической таблице и сверяется по позициям; расхождение длины чинится автофиксом, замену элемента предлагает suggestion, непрозрачная позиция принимает написанное; включено в общий конфиг репозитория уровнем `warn` | M | **done** — [архив](../../openspec/changes/archive/2026-09-12-eslint-deps-sync/), новая спека [`dependency-list-rule`](../../openspec/specs/dependency-list-rule/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Правило линтера: список зависимостей из параметров конструктора»; список зависимостей повторяет типы конструктора в каждом файле; компилятор ловит расхождение, а чинит его человек |
| 71 | `inbox` | ideas 09-12 | пакет `@nestlingjs/inbox`: плагин `inbox({ transaction, store })` со слоем для подписчиков, интерфейс `InboxStore` с реализацией в памяти, составной ключ «паттерн endpoint'а + ключ идемпотентности», политика `requiresInbox`, ресурс уборщика; третий исход pre-юнита `done()` в ядре; адаптер `@nestlingjs/drizzle.pg/inbox`; глава 27 гайда | M | **done** — новый пакет [`@nestlingjs/inbox`](../../packages/nestling.inbox/), [ideas.md [2026-09-12]](./ideas.md) «Транзакционный приём»; outbox превратил «может потеряться» в «может продублироваться», а дедупликация на приёме осталась соглашением: в примере это `Set` в памяти, который живёт до перезапуска и не разделяется репликами |
| 72 | `http-method-constructors` | apply #68 | шесть статиков по HTTP-методу у `httpEndpoint`: `httpEndpoint.get(path, { … })` и остальные пять, метод уходит из словаря в имя конструктора; `cliEndpoint(command, { … })` тем же ходом; миграция 378 мест | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-12-http-method-constructors/), [deferred [2026-09-03]](./deferred.md) «Конструктор HTTP-декларации с методом в имени»; мотив синтаксический: `httpEndpoint.get('/users/:id', { … })` короче словаря с `method`, а имя `httpEndpoint` после 68 уже несёт статик. Change 68 его не тронул: миграция там 39 мест, здесь ~305, и ни один call site не переезжает дважды |
| 73 | `drizzle-pg` | ideas 09-11 | пакет `@nestlingjs/drizzle.pg`: соединение значением `drizzlePg({ schema })` с DI-токеном, переменной транзакции, конструктором слоя `db.transaction()`, политикой `requiresTransaction` и ключами конфига семейства `database`; пул — ресурс с пробой; адаптер `OutboxStore` подпутём `./outbox`, таблица записей объявлением drizzle и строкой DDL; пример `users-service` на PostgreSQL | L | **done** — [архив](../../openspec/changes/archive/2026-09-12-drizzle-pg/), новые спеки [`database-connections`](../../openspec/specs/database-connections/spec.md), [`request-transaction`](../../openspec/specs/request-transaction/spec.md) и [`sql-outbox-store`](../../openspec/specs/sql-outbox-store/spec.md), [ideas.md [2026-09-11]](./ideas.md) «Соединение с базой: сателлит `drizzle.pg`»; база данных — первая инфраструктура каждого сервиса, а Nestling не давал для неё ничего: пример держал таблицу в памяти, слой транзакции писался руками, адаптер `OutboxStore` был работой приложения; решение — [ideas.md [2026-09-11]](./ideas.md) «Соединение с базой: сателлит `drizzle.pg`» |
| 74 | `nats-msg-id` | apply #71 | ключ идемпотентности конверта отображается на заголовок `Nats-Msg-Id` транспорта NATS: окно дедупликации JetStream снимает повторы relay без участия приложения. Транзакционной гарантии не даёт — у транспорта нет транзакции приложения, — поэтому дополняет слой приёма, а не заменяет его | S | **done** — [архив](../../openspec/changes/archive/2026-09-12-nats-msg-id/), обновлены спеки [`nats-transport`](../../openspec/specs/nats-transport/spec.md), [`durable-delivery`](../../openspec/specs/durable-delivery/spec.md) и [`transactional-inbox`](../../openspec/specs/transactional-inbox/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Транзакционный приём», открытый вопрос 2 (помечен РЕАЛИЗОВАНО); повторы relay в пределах окна брокера снимаются без участия приложения: JetStream уже умеет дедуплицировать по `Nats-Msg-Id`, а транспорт везёт ключ собственным заголовком |
| 75 | `nats-connector-adapter` | apply #74 | `defaultConnector` собирает `NatsLike` из клиента `nats` явным адаптером вместо приведения через `unknown`: `headers()` функцией модуля, долговечная подписка через `js.consumers.get()` и `consume()`, `StreamInfo` и `ConsumerInfo` разворачиваются до конфигурации, сообщение потока оборачивается до `NatsJsMsgLike` без `respond()`. `compose.yaml` и `yarn test:live` делают живой прогон исполнимым, `RELEASING.md` — воротами выпуска; `nats` переезжает в `peerDependencies` | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-nats-connector-adapter/), обновлены спеки [`nats-transport`](../../openspec/specs/nats-transport/spec.md) и [`package-publication`](../../openspec/specs/package-publication/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Коннектор к живому клиенту `nats`» (помечен РЕАЛИЗОВАНО); транспорт впервые прошёл против живого брокера — семь сценариев на `nats:2` одной командой `yarn test:live`, — и расхождение границы с клиентом теперь ловит тайпчек, а не прогон |
| 76 | `errors-of` | d/10+13 | хелпер `errorsOf(Operation)`: список отказов вызывающей операции — производное значение, а не копия чужого `errors:` | S | **done** — [архив](../../openspec/changes/archive/2026-09-13-errors-of/), новая спека [`errors-of`](../../openspec/specs/errors-of/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 1 (помечен РЕАЛИЗОВАНО); d/13 §4.1, §6.6: на пяти операциях копирование — дисциплина, на пятидесяти — то, чего компилятор требует, но в чём не помогает |
| 77 | `observability` | d/10+13 | well-known переменная `Trace` с юнитом `withTracing()`, формат W3C по HTTP и полем конверта по шине, поле `traceId` в записях логгера; интерфейс `Metrics`, корень `RootMetrics$` с опцией `makeApp({ metrics })`, семейство `Metrics$`, четыре метрики ядра на endpoint и на вызов порта; `spyMetrics()` в тестовом пакете; адаптер Prometheus и endpoint `/metrics` в примере `split-nats` | M–L | **done** — [архив](../../openspec/changes/archive/2026-09-13-observability/), новые спеки [`kernel-metrics`](../../openspec/specs/kernel-metrics/spec.md) и [`trace-context`](../../openspec/specs/trace-context/spec.md), обновлены [`async-context-vars`](../../openspec/specs/async-context-vars/spec.md), [`kernel-logger`](../../openspec/specs/kernel-logger/spec.md), [`port-invocation`](../../openspec/specs/port-invocation/spec.md) и [`typed-http-client`](../../openspec/specs/typed-http-client/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 2 (помечен РЕАЛИЗОВАНО); запись метрики стоит в рантайме пайплайна, а не в `.finally`-юните: иначе endpoint без слоя остался бы неучтённым; клиент получает читалку трассы опцией `trace` — пакет собирается для браузера |
| 78 | `auth-layer` | d/10+13 | слой JWT/OIDC с типизированной переменной `Caller`, типизированный HTTP-контекст для pre-юнитов | M | план — [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13»; d/10 §3.6: гайд проверяет Bearer руками, заголовки читаются через `ctx.raw.attributes` с `typeof` |
| 79 | `adapter-transport` | d/10+13 | `adapter()` — транспорт без сокета: `toNodeHandler(app)` отдаёт `(req, res) => Promise<boolean>`, `toFetchHandler(app)` — `(Request) => Promise<Response>`; байтовая граница описана `HttpSource` и `HttpSink`, у сборки появились `app.transports` и `run({ signals })` | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-adapter-transport/), новая спека [`http-adapter-transport`](../../openspec/specs/http-adapter-transport/spec.md), изменённые [`http-transport-boundary`](../../openspec/specs/http-transport-boundary/spec.md), [`lifecycle-phases`](../../openspec/specs/lifecycle-phases/spec.md) и [`transport-providers`](../../openspec/specs/transport-providers/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 4 (помечен РЕАЛИЗОВАНО), рецепт [embedded.md](../recipes/embedded.md); d/13 §6.1: монтирование в чужое приложение; рантайм помимо Node остался вне границы — `busboy` и `Buffer` на месте |
| 80 | `readme-positioning` | d/10+13 | первый абзац README — три гарантии, аудитория названа явно вместе с теми, кому фреймворк не нужен | S | план — [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13»; d/13 §6.2: `docs/guarantees.md` есть, README на него не ведёт; по d/15 п. 2.3 вступление README и `docs/index.md` переписываются целиком, а не первым абзацем |
| 81 | `pipeline-stale-units` | d/10+13 | `withIdentity`, `withPermissions`, `withRequestLogging`: удалить или переписать под `.pre(unit, { errors })` | S | **done** — [архив](../../openspec/changes/archive/2026-09-14-pipeline-stale-units/), обновлены спеки [`kernel-logger`](../../openspec/specs/kernel-logger/spec.md), [`pipeline-phase-model`](../../openspec/specs/pipeline-phase-model/spec.md) и [`packages-layout`](../../openspec/specs/packages-layout/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 6 (помечен РЕАЛИЗОВАНО); три юнита удалены без замены — инвентаризация импортов не нашла вызывающего кода вне тестов и доков, а плоская модель аутентификации всё равно уступит место `auth-layer` (#78); инвентаризация propose пропустила типовые тесты билдера и JSDoc-пример, оба чинились тем же способом, что design.md уже предписал для type-test фикстур |
| 82 | `mcp-satellite` | d/10+13 | пакет `@nestlingjs/mcp`: транспорт `mcp({ info, converters })` встаёт обработчиком на сокет объявленного HTTP-сервера, инструмент — endpoint этого транспорта в двух формах `mcpTool(name, { … })` и `mcpTool.implement(Operation, { handler })`; схемы переводят конвертеры OpenAPI, `Fail` доходит до агента исходом вызова с `isError`, сессии клиентов живут в транспорте; конверт JSON-RPC 2.0, `initialize`, `tools/list` и `tools/call` реализованы в пакете, SDK стоит в `devDependencies` сверкой типов и интеграционным прогоном настоящим клиентом; рецепт `docs/recipes/mcp.md`, три инструмента в `examples/app-with-http` | M | **done** — [архив](../../openspec/changes/archive/2026-09-13-mcp-satellite/), новые спеки [`mcp-transport`](../../openspec/specs/mcp-transport/spec.md) и [`mcp-tool-declarations`](../../openspec/specs/mcp-tool-declarations/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 7 (помечен РЕАЛИЗОВАНО); MCP оказался транспортом, а не плагином: первая рамка со списком `tools:` и вызовом через `Operation.caller` не собирается — топология считает реализацией операции только декларацию с bus-биндингом, сборка падает на ASSEMBLE, а `Dispatch` есть только у транспорта; `@nestlingjs/transport.http` получил экспорт `PayloadTooLargeError` — сателлит читает тело с пределом и без него не отличил бы слишком большой запрос от внутренней ошибки |
| 83 | `problem-json` | deferred | wire-формат ошибок RFC 9457 (`application/problem+json`) как формат по умолчанию у HTTP-транспорта; `Fail` и его `details` отображаются на поля problem-документа, типизированный клиент и OpenAPI получают стандартный медиатип | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-14-problem-json/), новая спека [`http-problem-details`](../../openspec/specs/http-problem-details/spec.md), изменённые [`error-response-safety`](../../openspec/specs/error-response-safety/spec.md), [`http-request-validation-errors`](../../openspec/specs/http-request-validation-errors/spec.md), [`http-streaming-framing`](../../openspec/specs/http-streaming-framing/spec.md), [`http-transport-limits`](../../openspec/specs/http-transport-limits/spec.md), [`openapi-document`](../../openspec/specs/openapi-document/spec.md) и [`typed-http-client`](../../openspec/specs/typed-http-client/spec.md), [deferred.md [2026-07-14]](./deferred.md) «Wire-формат ошибок: RFC 9457» (помечена РЕАЛИЗОВАНО); идентичность отказа записана в `type` значением `urn:error:<код>`, поля `code` в теле не осталось: голый код не проходит по RFC 3986 (`not_found:user` читается схемой `not_found`, а подчёркивание в схеме запрещено), а расширение рядом с `about:blank` оставило бы стандартную ось типа пустой; формат лёг в `@nestlingjs/operations` и реэкспортируется транспортом — документ читает браузерный клиент, которому серверный пакет не нужен; три места сериализации (ответ пайплайна, ответ до пайплайна, кадр `event: error` у SSE) свелись к одной функции; член `title` выводится из категории фразой статуса HTTP — поле у `makeFail` расширяло бы публичный API ядра; `@nestlingjs/mcp` попал в `main` после предложения change'а со своим ожиданием старого тела — отказ `not_found:mcp_session` на границе сателлита проверялся по `code`, мигрирован на `type` |
| 84 | `typed-success-status` | deferred | успешный HTTP-статус в декларации: `Output`/`Ok` знают статус, документ OpenAPI и клиент читают его из декларации, а не из умолчания `200`/`204` | M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-14-typed-success-status/), новая спека [`declared-success-status`](../../openspec/specs/declared-success-status/spec.md), изменённые [`declaration-doc-metadata`](../../openspec/specs/declaration-doc-metadata/spec.md), [`error-values`](../../openspec/specs/error-values/spec.md), [`io-forms`](../../openspec/specs/io-forms/spec.md), [`openapi-document`](../../openspec/specs/openapi-document/spec.md) и [`typed-http-client`](../../openspec/specs/typed-http-client/spec.md), [deferred.md [2026-08-01]](./deferred.md) «Типизированный успешный статус декларации» (помечена РЕАЛИЗОВАНО); статус объявляется полем `status` верхнего уровня декларации, `doc.status` удалён, `Ok` параметризован статусом (`Ok<TValue, TStatus>`); одного статуса на декларацию не хватило — появилась развилка исходов `outputs({ ok: User, accepted: Job })` со своей формой у каждой ветки и маркером `none()` для исхода без тела: случай «202 со своим телом» одним полем не закрывался; статус остался значением декларации, а не выводом из сигнатуры хендлера — генератор и клиент читают значения, анализа типов в проекте нет; умолчание («`ok` при `output`, `no_content` без него») считается одной функцией ядра, её же читают рантайм, документ и клиент, и расхождение «документ обещает 204, уходит 200» исчезло по построению; дельта `openapi-document` разошлась с 83, влитым в тот же день, — тело отказа осталось по RFC 9457, из дельты взята только успешная сторона; дальше отложена политика `check()` на недостижимую ветку развилки — она записана в [deferred.md](./deferred.md) отдельной темой |
| 85 | `otel-satellite` | из #77 | пакет `@nestlingjs/otel`: экспорт спанов из переменной `Trace` и push метрик по OTLP из снимка `MetricsStore$` по таймеру | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-otel-satellite/), новые спеки [`otel-package`](../../openspec/specs/otel-package/spec.md) и [`otel-span-export`](../../openspec/specs/otel-span-export/spec.md), изменённая [`example-apps`](../../openspec/specs/example-apps/spec.md), [ideas.md [2026-09-12]](./ideas.md) «Разбор обзоров d/10 и d/13», пункт 2 в части сателлита (помечен РЕАЛИЗОВАНО); вход `otel(options)` отдаёт слой участков и плагин: слой открывает участок pre-шагом и закрывает `.finally`-шагом, идентификаторы берёт из переменной `Trace`, статус выводит из исхода, а у потокового ответа закрывается после доставки; плагин объявляет ресурс поверх `MetricsStore$` — таймер отправляет снимок `PushMetricExporter`, освобождение досылает последний; сервис дописывает атрибут через `Ctx(Span)`, забытый слой ловит политика `everyEndpoint(…).hasVar(Span)`; экспозиция `/metrics` и реализация корня метрик ушли из сателлита ещё до реализации — их забрал `declared-metrics` (#101), и сателлиту остались трассы и push; опция названа `service`, а не `serviceName`: пара `service` и `version` читается вместе, а имена атрибутов ресурса пакет проставляет сам (открытый вопрос 2 записи закрыт); требование слоя к контексту записано объектным типом, а не именованным алиасом — алиас пришлось бы экспортировать барелем, иначе композиция слоя не получает выводимого типа и каждая декларация endpoint'а падает с TS2742; участок на вызов порта отложен записью [deferred.md [2026-09-14]](./deferred.md), дерево от его отсутствия не рвётся, а сетевую задержку показывает разность метрик ядра; коллектор OpenTelemetry в интеграционном прогоне отложен тоже — совместимость экспорта доказывают подставные экспортёры в спеках и проверка типов против настоящих экспортёров OTLP по HTTP |
| 86 | `terminology` | d/15 | «шаг» вместо «юнита» (step; идентификаторы `*Unit*` → `*Step*`), «токен семейства» вместо «члена», `build` вместо `assemble` (`app.build(args)`, `BuildArgs`, `BuiltApp`, фаза `BUILD`, `buildTest`); код, глоссарий обоих языков, design, гайды, README и скилл агента одним проходом | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-terminology/), переименованные спеки [`pipeline-step-forms`](../../openspec/specs/pipeline-step-forms/spec.md), [`transport-pipeline-steps`](../../openspec/specs/transport-pipeline-steps/spec.md), [`synchronous-build`](../../openspec/specs/synchronous-build/spec.md) и [`build-policies`](../../openspec/specs/build-policies/spec.md), изменённые [`composition-root`](../../openspec/specs/composition-root/spec.md), [`lifecycle-phases`](../../openspec/specs/lifecycle-phases/spec.md), [`test-composition-root`](../../openspec/specs/test-composition-root/spec.md), [`token-families`](../../openspec/specs/token-families/spec.md) и [`docs-terminology`](../../openspec/specs/docs-terminology/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Термины: шаг, токен семейства, `build`» (помечена РЕАЛИЗОВАНО); `testUnit` стал `testBundle`, а не `testStep`: функция поднимает фичу или плагин в изоляции, и правило `Unit` → `Step` растянуло бы «шаг» на то, что шагом не является; английское `unit` осталось парой «единицы состава», и линтер ловит `pre-unit` и `unit` рядом со словом `pipeline` или `layer`, а не всякое `unit`; «член» вычищен и там, где слово стояло одно: половинчатая замена оставила бы два имени одного понятия в одном абзаце |
| 87 | `naming-conventions` | d/15 | вид значения называет часть речи: экземпляр плагина существительным (`openapi`, `inbox`, `db`), слой причастием (`traced`, `signed`), двухпозиционный переключатель предикатом (`DocsEnabled`); фабрика, возвращающая плагин, называется `make*` — восемь имён от `makeOpenapi` до `makeHttpProbes`; правила в `conventions.md`, проход по пакетам, примерам, гайдам, рецептам и скиллу агента | S–M | **done** — изменённые спеки [`openapi-document`](../../openspec/specs/openapi-document/spec.md), [`transactional-inbox`](../../openspec/specs/transactional-inbox/spec.md), [`transactional-outbox`](../../openspec/specs/transactional-outbox/spec.md), [`subscription-registry`](../../openspec/specs/subscription-registry/spec.md), [`database-connections`](../../openspec/specs/database-connections/spec.md), [`sql-outbox-store`](../../openspec/specs/sql-outbox-store/spec.md) и [`http-probes`](../../openspec/specs/http-probes/spec.md), [ideas.md [2026-09-14]](./ideas.md) «Именование: вид называет часть речи, `make` у фабрики плагина» (запись [2026-09-13] о суффиксах помечена superseded), [deferred.md [2026-09-13]](./deferred.md) «Фабрики пакетов» — триггер сработал; суффиксы `Plugin` и `Layer` из плана отменены: они платят длиной каждого упоминания и оставляют существительное занятым. Сверх записи переименованы слои-примеры в design-доках (`base` в `traced`, `authedBase` в `authed`, `withIdempotency` в `idempotent`) и фабрика-пример `logging()` в `makeLogging()`: доки, где правило записано, ему же и противоречили |
| 88 | `servers-implicit` | d/15 | `server()` вместо `httpServer()`, имя по умолчанию `'default'`; `transports:` перечисляет только транспорты, серверы собираются транзитивно по ссылке `server` с дедупликацией; сервер без транспорта не создаётся | S | **done** — [архив](../../openspec/changes/archive/2026-09-13-servers-implicit/), изменённые спеки [`http-server-resource`](../../openspec/specs/http-server-resource/spec.md) и [`transport-providers`](../../openspec/specs/transport-providers/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Сервер: `server()`, не перечисляется в `transports:`»; design-доки описывали целевое состояние уже до change'а — расходились код, спеки пакетов, README и два примера; `TransportEntry` и `isTransport` удалены целиком, вместе с фильтрацией в `resolveComposition`; проверка списка идёт по кандидатам всех веток в `normalizeSpec`, до `collectServers`, иначе сервер молча пропал бы из сборки |
| 89 | `zod-inside` | d/15 | один пакет `@nestlingjs/schema.zod`: конвертер, `int()`/`flag()` открытыми билдерами zod, содержимое `models` (пакет удаляется); три `schema.ts` в `inbox`, `outbox`, `drizzle.pg` удалены, секции на zod; `zodConverter()` умолчанием у `openapi`, `mcp`, `transport.cli`, `converters` остаётся переопределением; документ OpenAPI из `discover()` с опциями плагина, `openapiOptions` примера исчезает; JSON Schema секций в `describeConfig` | M | **done** — [архив](../../openspec/changes/archive/2026-09-13-zod-inside/), новая спека [`schema-zod-package`](../../openspec/specs/schema-zod-package/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Схемы: Standard Schema на границе, zod внутри; один пакет `schema.zod`»; сверх записи: правило «пустой ключ — отсутствующее значение» перенесено в ядро — рукописные хелперы считали пустую строку отсутствующим значением каждый своим `isBlank`, а открытый билдер zod так не умеет; проверка «публичный API не называет валидатора» сузилась до экспортов барреля и вошла в `yarn verify` |
| 90 | `config-run-bind` | d/15 | привязки принимает `run({ config })`, `check` и `buildTest`, поле `config:` у `makeApp` удалено; одна форма `bind(source, { keys, optional, timeout })`; `env()` обычный источник, хвост `process.env` из читалки удалён; `dotenv(path)` в ядре на `util.parseEnv`; умолчание `[bind(env()), bind(dotenv('.env'), { optional: true })]` значением `defaultSources`; `objectSource` → `vars()` в `testing`; `testApp.run()` до RUN с `baseUrl`; `create-test-app.ts` и `httpPort` примера удалены | M–L | **done** — [архив](../../openspec/changes/archive/2026-09-14-config-run-bind/), изменённые спеки [`composition-root`](../../openspec/specs/composition-root/spec.md), [`config-sources-binding`](../../openspec/specs/config-sources-binding/spec.md), [`health-probes`](../../openspec/specs/health-probes/spec.md), [`structural-check`](../../openspec/specs/structural-check/spec.md) и [`test-composition-root`](../../openspec/specs/test-composition-root/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Конфигурация: привязки на `run()`, `env()` и `dotenv()` умолчанием, `bind()`, `needs` у источника» (помечена РЕАЛИЗОВАНО); реализованы пп. 1–5, 9–10, пп. 6–8 (`needs`, `watch`/`retries`, сателлит `config.vault`) остаются за 92; форма адреса теста — `testApp.baseUrl(name?)`, а не поле; `check()` без опции `config` поднимает `defaultSources`, а не привязки декларации — у декларации после этого change'а привязок нет вовсе; ветка отстала от уже слитых 79 (`adapter-transport`) и 86 (`terminology`) — при ребейзе на `main` пришлось адаптировать тесты `adapter()` под `bind()` и вернуть спекам терминологию `build` |
| 91 | `argv-args` | d/15 | `argv(process.argv)` — маркер, который `build`, `discover` и `check` разбирают по схеме декларации: `--features`, `--include-deps`, флаг на переключатель, `--help`; разбор строгий; строковая форма и массив имён удалены, объектная остаётся; `RootConfig` и `load()` уходят из примеров; строка в `guarantees.md` о независимости состава от окружения | S–M | **done** — [архив](../../openspec/changes/archive/2026-09-14-argv-args/), новая спека [`build-argument`](../../openspec/specs/build-argument/spec.md), изменённые [`lifecycle-phases`](../../openspec/specs/lifecycle-phases/spec.md), [`composition-switches`](../../openspec/specs/composition-switches/spec.md), [`endpoint-discovery`](../../openspec/specs/endpoint-discovery/spec.md), [`structural-check`](../../openspec/specs/structural-check/spec.md), [`feature-bundles`](../../openspec/specs/feature-bundles/spec.md), [`config-sections`](../../openspec/specs/config-sections/spec.md) и [`config-secrets`](../../openspec/specs/config-secrets/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Аргумент сборки: `argv()` по схеме декларации» (помечена РЕАЛИЗОВАНО); пред-сборочного чтения конфига больше нет: `load()` удалена вместе с `config/load.ts`, состав процесса задаёт командная строка. Дальше уехало правило линтера `no-process-globals` — строка 96 |
| 92 | `config-source-needs` | d/15 | поле `needs` у `ConfigSource`, значения секции в `init(values)`; очерёдность подъёма топологическим порядком из `needs`, порядок списка только приоритет, цикл — отказ до ввода-вывода; секция из `needs` читается из уже поднятых источников и не перепроецируется; `timeout` привязки; сателлит `@nestlingjs/config.vault`, `watch` и `retries` опциями источника | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-config-source-needs/), новые спеки [`config-source-needs`](../../openspec/specs/config-source-needs/spec.md) и [`config-vault-package`](../../openspec/specs/config-vault-package/spec.md), изменённые [`config-bootstrap-snapshot`](../../openspec/specs/config-bootstrap-snapshot/spec.md) и [`config-sources-binding`](../../openspec/specs/config-sources-binding/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Конфигурация: привязки на `run()`, `env()` и `dotenv()` умолчанием, `bind()`, `needs` у источника» (помечена РЕАЛИЗОВАНО) — пп. 6–8 закрыты, запись выполнена целиком; наблюдение за секретом уехало в [deferred.md [2026-09-14]](./deferred.md) «Наблюдение за секретом Vault»: `watch` у сетевого источника — опрос против сигнала извне, и опций у источника осталась одна, `retries`; отказы фазы 0 разделились на два класса, и `optional` проглатывает только недоступность внешней системы — цикл в `needs` и негодная секция (`makeConfig.reloadable` или неимпортированный модуль) отказывают всегда, это дефект кода; порядок подъёма по списку привязок больше не читается, поэтому читалка пишет его в логгер ядра уровнем `debug`; секция координат — аргумент `vault(section, options?)` с типом, ограниченным `VaultCoordinates`: второе хранилище объявляет свою секцию, чужая форма не компилируется; проекцию секции читалка получает значением от фазы 0, а не импортом — иначе `reader → project → reader` замкнул бы модули; при ребейзе на `main` разошлась только точка сверки рецепта |
| 93 | `logging-core` | d/15 | опция `logging: { logger, fields }` вместо `logger:`; декоратор корреляции в ядре, `fields` из переменных контекста с умолчанием `[RequestId, Trace]`, `logFields` у плагина, `logField(Var, name)`, дубликат имени — отказ на сборке; пакет `@nestlingjs/logging` (интерфейс, `makeConsoleLogger`) без зависимостей, `app` реэкспортирует типы; `testing` тихий по умолчанию; примеры без `console`, кроме вывода команд CLI | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-logging-core/), новые спеки [`logging-package`](../../openspec/specs/logging-package/spec.md) и [`log-correlation-fields`](../../openspec/specs/log-correlation-fields/spec.md), изменённая [`kernel-logger`](../../openspec/specs/kernel-logger/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Логгер: опция `logging`, пакет `@nestlingjs/logging`, поля-декларации, pino сателлитом» (помечена РЕАЛИЗОВАНО); умолчание `fields` — `[RequestId, logField(Trace, 'traceId', (t) => t.traceId)]`: без проекции в запись попал бы объект трассы и поиск по `traceId=` перестал бы работать; `silent` — пятое значение секции `nestlingLog`, но не значение типа `LogLevel`; `makeConsoleLogger` реэкспортирует и `app`, иначе скрипт рядом с приложением ставил бы второй пакет ради одной строки; вывод `help` в `examples/cli` стал формой `stream(z.string())`, и `console` в примерах не осталось вовсе; сателлит pino — строка 94 |
| 94 | `logging-pino` | d/15 | сателлит `@nestlingjs/logging.pino`: `pinoLogger(options): Logger`, вывод в `stderr`, человекочитаемый формат своим писателем без `pino-pretty`, `trace`/`fatal` отображаются на `debug`/`error` | S | **done** — новая спека `logging-pino-package`, изменённая [`logging-package`](../../openspec/specs/logging-package/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Логгер: опция `logging`, пакет `@nestlingjs/logging`, поля-декларации, pino сателлитом» (помечена РЕАЛИЗОВАНО); формат записи переехал в экспорт `@nestlingjs/logging` — `formatLine`, `serializeError` и тип `LogEntry`, — и штатный логгер печатает ими же: совпадение вывода держится одной реализацией формата, а не сверкой двух копий; в `text` писатель разбирает строку pino обратно и печатает `formatLine`, в `json` строку пишет сам pino — набор ключей тот же, порядок его; восемь ключей pino заняты адаптером, и вложенный `serializers.err` ловит только проверка в рантайме; `pino.destination` отвергнут — запись уходит мимо `process.stderr.write` и невидима перехвату ни в тесте, ни в приложении |
| 95 | `subscription-identity` | d/15 | `identity` реестра подписок принимает переменную контекста (`identity: RequestId`); функция остаётся второй формой, типизированной контекстом с переменными; `as` уходит из примера, JSDoc и спек | S | **done** — [архив](../../openspec/changes/archive/2026-09-13-subscription-identity/) |
| 96 | `eslint-rules` | d/15 | два правила `@nestlingjs/eslint-plugin`: `conditional-spread` (`...(test ? obj : {})` → `...(test && obj)` с автофиксом при булевом `test`, предупреждение на `...(x && obj)` с небулевым `x`) и `no-process-globals` (`process.env` и `process.argv` запрещены, `process.argv` только аргументом `argv()`); включено в конфиг репозитория с исключением для источника `env()` | S | план — та же запись, п. 11, и запись «Аргумент сборки», п. 6; после 91 (`argv()`) |
| 97 | `docs-site-theme` | d/15 | оформление сайта документации: `scripts/site/layout.html`, `home.html`, `styles.css`; дизайн интерактивно с пользователем | — | **done** — [архив](../../openspec/changes/archive/2026-09-14-docs-site-theme/), изменённая спека [`docs-site`](../../openspec/specs/docs-site/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Разбор фидбэка по коду d/15», пункт 12 (помечен РЕАЛИЗОВАНО); выбран типографский минимализм — тёплая почти-белая база и терракотовый акцент в трёх местах; врезок стало пять видов вместо трёх, вид пишется в имени контейнера, стартовая страница печатается лендингом из частей `docs/index.md`; врезок в публикуемых текстах не нашлось ни одной — все 22 `:::note` лежат в неизменяемом `docs/history/`, и переезд свёлся к примерам разметки в `scripts/site/README.md`; вступление README пакетов осталось за #80 |
| 98 | `transport-ws` | ideas 09-13 | пакет `@nestlingjs/transport.ws`: двунаправленный транспорт поверх WebSocket, `wsEndpoint` с формами io в обе стороны; `events(T)` во входе включается его `capabilities`; отказ и отмена доходят до клиента в открытом соединении; сокет тот же ресурс `server`, апгрейд соединения — обязанность транспорта | M–L | план — [ideas.md [2026-09-13]](./ideas.md) «Совместимость: справочник `compatibility.md`, WebSocket и gRPC строками плана» и [deferred.md [2026-07-31]](./deferred.md) «`events` во входе: клиент льёт поток событий», триггер возврата назвал именно этот транспорт; дизайна нет, семантика двунаправленного вызова открыта |
| 99 | `transport-grpc` | ideas 09-13 | пакет `@nestlingjs/transport.grpc`: контракты операций поверх gRPC, свой HTTP/2-сервер, потоки в обе стороны; deadline по проводу ложится на `deadline` операции | L | план — [ideas.md [2026-09-13]](./ideas.md) «Совместимость: справочник `compatibility.md`, WebSocket и gRPC строками плана»; дизайна нет, главный открытый вопрос — отношение схем Standard Schema и `.proto`: кто из них источник |
| 100 | `examples-rework` | ideas 09-13 | три примера вместо шести: `microservice` (сервис для внешних клиентов), `modular-app` (две фичи над шиной NATS, профили `mono` и `split`), `cli` (командная строка поверх операций сервиса); `app-with-http`, `split-nats`, `simple-cli`, `simple-http-server` и `container` удалены, фича `quotas` заменена на `notifications`; глава и рецепт пишут свой код, плашка называет точку сверки хэшем коммита, свежесть считается по `packages/*/src` | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-13-examples-rework/), новая спека [`example-apps`](../../openspec/specs/example-apps/spec.md), изменённые [`examples-layout`](../../openspec/specs/examples-layout/spec.md), [`docs-guide-layout`](../../openspec/specs/docs-guide-layout/spec.md) и [`docs-site`](../../openspec/specs/docs-site/spec.md), [ideas.md [2026-09-13]](./ideas.md) «Примеры — приложения, а не иллюстрации; глава пишет свой код»; примеры перестали быть иллюстрациями к главам: каждый стал приложением с README, конфигурацией и запуском; главы 10, 16 и 20 пришлось свести к одному домену — расхождение между ними было незаметно, пока каждая сверялась со своим примером; `runtime-message-language` и `docs-bilingual` обосновывали правила цитированием примеров и получили дельты здесь же |
| 101 | `declared-metrics` | ideas 09-14 | метрика — декларация-значение: `makeMetrics(prefix, { … })`, конструкторы `counter` и `histogram`, атрибуты перечнем или пометкой `open`, группа DI-токеном и вклад `metrics:` у фичи, модуля, плагина и корня; каталог фазы BUILD, `MetricsStore$` со `snapshot()`, ряды по индексам; ядро объявляет свои четыре метрики той же группой; новый пакет `@nestlingjs/prometheus`; опция `makeApp({ metrics })`, `RootMetrics$`, семейство `Metrics$` и `spyMetrics()` удалены | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-14-declared-metrics/), новые спеки [`metric-declarations`](../../openspec/specs/metric-declarations/spec.md), [`metrics-store`](../../openspec/specs/metrics-store/spec.md) и [`prometheus-package`](../../openspec/specs/prometheus-package/spec.md), изменённые [`kernel-metrics`](../../openspec/specs/kernel-metrics/spec.md), [`port-invocation`](../../openspec/specs/port-invocation/spec.md) и [`example-apps`](../../openspec/specs/example-apps/spec.md); ряды ядра пришлось заводить списком пар «транспорт — шаблон», а не произведением перечней: произведение дало бы endpoint'ы чужих транспортов; `ExecuteOptions.metrics` остался необязательным с умолчанием-standalone — симметрично `logger`, иначе standalone-путь транспортов остался бы без писателя; выход `tap(sink)` и интерфейс `MetricSink` сняты по ходу change'а: единственный ожидаемый получатель (push по OTLP) принимает накопленные точки, а подписчик потока завёл бы второй агрегат рядом со store — поток записей уехал в [deferred.md [2026-09-14]](./deferred.md); endpoint экспозиции несёт и `detached`, и `doc.hidden` — из документа API убирает второе; `makeCatalog`, `makeWriter`, `findSeries` и `findSeriesOne` перенесены в подпуть `@nestlingjs/app/testing`, чтобы прод-поверхность пакета не росла ради теста |
| 102 | `vitest-swc` | находка | тесты репозитория гоняет vitest со swc-трансформом (`decoratorVersion: '2022-03'`): `vitest.config.js` в пакете вместо `jest.config.js`, условие `"testing"` полем `resolve.conditions`, резолв соседей на `src` таблицей `resolve.alias`; импорт из `vitest` в 214 файлах — спеки, общие наборы проверок и хелперы e2e, — снапшоты сняты заново; jest, ts-jest и полифилл `Symbol.asyncDispose` удалены, `NODE_OPTIONS` из скриптов ушёл | M | **done** — [архив](../../openspec/changes/archive/2026-09-14-vitest-swc/), новая спека [`test-runner`](../../openspec/specs/test-runner/spec.md), изменённые [`testing-subpath-convention`](../../openspec/specs/testing-subpath-convention/spec.md), [`examples-layout`](../../openspec/specs/examples-layout/spec.md), [`agent-skill-package`](../../openspec/specs/agent-skill-package/spec.md) и [`package-publication`](../../openspec/specs/package-publication/spec.md); записи в журнале идей под него нет — решён обсуждением 2026-09-13; сверка «прогон эквивалентен» — [baseline.md](../../openspec/changes/archive/2026-09-14-vitest-swc/baseline.md), 204 файла и 2593 теста до и после на общей базе; `dependsOn: ["^build"]` у таргета `test` пришлось оставить вопреки решению дизайна — спека границы пакета обходит замыкание `dist`, а не исходники, и без сборки падает прямым сообщением |
| 103 | `verify-speedup` | ideas 09-14 | холодный прогон проверки за 91 секунду вместо 170: `typecheck`, `lint` и `test` объявляют и `^build`, и `build` (без своей сборки они падали на `TS2307` при параллелизме больше трёх), шесть параллельных слотов, кэш nx вне рабочего дерева, одна программа компилятора у `public-api-validator`, `skipLibCheck` в базе и строгий проход `verify:strict` по собранному `dist`; сборка перестала проверять типы — JavaScript выпускает swc, декларации — пофайловый эмиттер TypeScript, 85 публичных экспортов получили явные аннотации | L, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-14-verify-speedup/), новые спеки [`verify-pipeline`](../../openspec/specs/verify-pipeline/spec.md) и [`explicit-public-types`](../../openspec/specs/explicit-public-types/spec.md), изменённая [`packages-layout`](../../openspec/specs/packages-layout/spec.md), [ideas.md [2026-09-14]](./ideas.md) «Проверка репозитория: тайпчек один за прогон, публичный тип написан» (помечена РЕАЛИЗОВАНО); меняется форма `.d.ts`: рядом с `declare class BuiltApp` встал `export interface BuiltApp`, набор методов и их сигнатуры прежние; `isolatedDeclarations` остался в конфигах сборки, а не в базе — база покрывает спеки, снипеты и примеры, у которых деклараций нет, и потребовала бы 65 аннотаций в коде без `.d.ts`; декларации выпускает не swc, а пофайловый `ts.transpileDeclaration`: у `@swc/core` 1.16.2 такого API нет; TypeScript обновлён до 5.9.3, потому что 5.7.3 молча портит две формы — `typeof X` в значении параметра типа по умолчанию и член класса с ключом-символом, — и не даёт диагностики ни там, ни там; метод с ключом-символом объявлен интерфейсом того же имени и повешен на прототип: эмиттер 5.9.3 выпускает такой член дважды; `drizzle.pg` остался на `tsc` — его таблицы отдают наружу тип drizzle, и аннотация была бы копией чужих внутренностей, которую чинят на каждом обновлении библиотеки |
| 104 | `release-notes-0.2.0` | d/16 | заметка о выпуске `docs/releases/v0.2.0.md` с английской парой и строкой в оглавлении: 15 change'ей между тегами `v0.1.3` и `v0.2.0`, точка входа — смена формы `httpEndpoint` на конструкторы по методу, в таблице переименований `@nestlingjs/openapi.zod` → `@nestlingjs/schema.zod`; правило папки `releases/` дополняется строкой о смене формы вызова при прежнем имени; правило «константу передаёт схема, источник добывает внешнее» записывается в `design/config.md`, рецепт `config-sources.md` перестраивается, строка `objectSource` заметки 0.3.0 ведёт в схему; шаблон deprecation в `RELEASING.md` становится английским и требует строки в таблице переименований | S–M | план — [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункты 1–3; [d/16](../history/discussions/16-upgrade-report.md) §3.1, §3.2, §3.4 |
| 105 | `agent-skill-upgrade` | d/16 | скилл приводится к 0.3.0 и перестаёт устаревать молча: версия Nestling названа в `SKILL.md`, правило сверки версии с `@nestlingjs/app` проекта стоит в первой части, установщик пишет манифест версии и хэшей и по нему отличает нетронутую копию от правленой; тестом закрепляются два инварианта — блок кода на TypeScript только снипетом и отсутствие в тексте имён из колонки «было» таблиц переименований; спека `agent-skill-package` получает дельту — сравнение с манифестом вместо сравнения с источником; одиннадцать мест в пяти файлах учат удалённому API (раздел «Sources» справочника конфигурации целиком, `doc: { status }`, строковая форма `build('users')`, три места в справочнике тестов, кортеж привязки в таблице перехода с NestJS), и все они лежат вне помеченных снипетами блоков, а метрик-деклараций, трассировки, тела отказа RFC 9457, объявления сервера, `errorsOf` и пяти пакетов нет вовсе; состав перекладывается — каталог пакетов отдельным файлом, новые справочники наблюдаемости и хранилища, четырнадцать файлов вместо одиннадцати | L | план — [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункт 4; [d/16](../history/discussions/16-upgrade-report.md) §3.8 |
| 106 | `check-reachability` | d/16 | недостижимая операция — отказ фазы 1 BUILD, а не 2 INIT: проверка переезжает из конструктора порта в сборку, и её видят `check()` и матрица `checkTopologies`; текст отказа прежний, спека `structural-check` получает сценарий; дизайн не меняется — `design/composition.md` и `guarantees.md` уже обещают эту проверку на BUILD, расходится с ними код | S–M, breaking | **done** — [архив](../../openspec/changes/archive/2026-09-14-check-reachability/), изменённые спеки [`port-binding`](../../openspec/specs/port-binding/spec.md), [`structural-check`](../../openspec/specs/structural-check/spec.md), [`message-bus`](../../openspec/specs/message-bus/spec.md) и [`nats-transport`](../../openspec/specs/nats-transport/spec.md), [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункт 5 (помечен РЕАЛИЗОВАНО); проверка переехала в рецепт семейства вызывателей, и вместе с ней туда ушли два других отказа — неимпортированное объявление операции и несовпадение её вида с запрошенным вызывателем; открытый вопрос про природу шины закрылся обязательным полем `remote` у `BusDeclaration`, у `IMessageBus` одноимённого поля больше нет; препятствием оказалась не нехватка экземпляров на BUILD, а то, что вход решения этой фазы не может приходить через граф: рецепт семейства узлов не резолвит — поэтому политику диспатча корень теперь читает снимком секции фазы 0 и передаёт слою портов опцией, как уже читает свою секцию корневой логгер; способ настройки прежний, а зависимости рецептов вызывателей сократились до держателя исполнителей и метрик |
| 107 | `include-deps-default` | d/16 | умолчание `includeDeps` объявляет декларация рядом с умолчаниями переключателей и печатает `--help`; сегодня оно жёстко `false`, а имя зарезервировано, и другое умолчание стоит отказа от `argv()` целиком | S | план — [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункт 6; [d/16](../history/discussions/16-upgrade-report.md) §3.5; форма и флаг обратного действия — в propose; продолжает 91 (`argv-args`), который ввёл `--include-deps` |
| 108 | `argv-owns-process` | d/16 | отказ подъёма при аргументе-маркере `argv()` печатается сообщением без стека и уходит кодом выхода 1, как уже ведёт себя `--help`; объектная форма аргумента поведения не меняет — встроенное приложение и тест ловят промис сами | S | план — [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункт 7; [d/16](../history/discussions/16-upgrade-report.md) §3.9; продолжает 91 (`argv-args`), который ввёл маркер и `--help` |
| 109 | `deleted-names-check` | d/16 | инвариант «удалённое публичное имя не встречается в живых текстах»: список строится разностью имён барелей между прошлым тегом и `HEAD` (37 имён между `v0.2.0` и `HEAD`), проверяются скилл, главы, рецепты, design-доки и README пакетов на обоих языках, не проверяются `history/`, `decisions/` и `releases/`; имя засчитывается только идентификатором в обратных кавычках или в блоке кода, цитата записи в плашке и левая колонка таблицы перехода с NestJS не считаются; скилл добавляется в Definition of Done `CLAUDE.md` и в правило `rules.tasks` файла `openspec/config.yaml`, контекст которого чинится заодно — он называет jest тест-раннером и `@nestlingjs/pipeline` пакетом | S–M | план — [ideas.md [2026-09-14]](./ideas.md) «Разбор отчёта об обновлении до 0.3.0», пункты 9 и 10; прототип проверки нашёл три места в скилле и по одному в design-доке конфигурации на каждом языке; механизм плашки-свежести, как у глав, здесь не годится — часы сбрасываются любым касанием файла |

## Как работать

Единица работы — **один change = одна ветка** в своём worktree. Три шага
делает сессия change'а, каждый в **свежем контексте**, четвёртый — сессия
Merger. Начатый change виден из любого worktree как ветка `change/<имя>`
(`git branch --list 'change/*'`): `/opsx:propose` такие не предлагает, а
при совпадении имени показывает worktree, где идёт работа.

| Шаг | Команда | Что ревьюим |
|---|---|---|
| 1. Предложить | `/opsx:propose "<имя>: <scope + non-goals>"` | `proposal.md` (Why / What / Non-goals) и решения `design.md` — 300–700 строк markdown. Самое дешёвое место поймать ошибку дизайна |
| 2. Реализовать | `/opsx:apply <имя>` | `git diff main...` + дельта-спеки. Тесты — доказательство |
| 3. Закрыть | `/opsx:archive <имя>` | статус в таблице выше + пометка «РЕАЛИЗОВАНО» в [ideas.md](./ideas.md), ветка передана Merger'у |
| 4. Слить | `/merger` — сессия Merger, не сессия change'а | merge-коммит в `main`, зелёные `yarn verify` и `yarn docs:audit` на `main` |

Контекст между шагами передаётся **файлами в `openspec/changes/<имя>/`**, а не
разговором: `tasks.md` с чекбоксами — точка возобновления, если сессия
прервалась. Поэтому propose и apply намеренно разводятся по разным сессиям:
propose — это диалог с перебором вариантов, и apply, унаследовав его, будет
реализовывать *обсуждённое*, а не *записанное*.

Definition of Done одного change — в [`CLAUDE.md`](../../CLAUDE.md) («Workflow
изменений»), плюс `rules.tasks` в `openspec/config.yaml` заставляет `propose`
класть тот же список последним разделом `tasks.md`. Коротко: задачи отмечены,
`yarn verify` и `yarn docs:audit` зелёные, README пакетов и плашки обновлены,
`design/`+`decisions/` синхронизированы, примеры мигрированы и гайды
пересверены, `main` не тронут.

В `main` пишет только Merger — сессия в главном worktree, которая следит за
worktree change'ей. Заархивированную ветку она сливает merge-коммитом.
Ветку, отставшую от `main`, она сначала просит перебазировать сессию
change'а. Процедура и события скана — в скилле `/merger`
(`.claude/skills/merger/SKILL.md`). Push в origin делает пользователь.
