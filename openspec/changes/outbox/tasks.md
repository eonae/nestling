## 1. Каркас пакета

- [ ] 1.1 Создать `packages/nestling.outbox` по общему набору конфигурации:
      `tsconfig.json`, `tsconfig.build.json`, `eslint.config.js`,
      `jest.config.js`, скрипты `clear`/`typecheck`/`build`/`lint`/`lint:fix`/`test`
- [ ] 1.2 Объявить зависимости: только `@nestling/container`,
      `@nestling/app`, `@nestling/operations`, `@common/misc`; внешних нет
- [ ] 1.3 Добавить проект в `nx.json` и убедиться, что `yarn verify` его видит
- [ ] 1.4 Объявить плагин `outbox(options)` через `makePlugin` с полями
      `transaction`, `store`, `operations`

## 2. Транзакционный эмиттер

- [ ] 2.1 Объявить семейство DI-токенов `outboxed(Operation)` со значением
      `Emitter<C>`; операция вида `request` не компилируется
- [ ] 2.2 Рецепт семейства: искать операцию по имени в `operations`,
      ронять сборку с текстом и починкой, если имени нет
- [ ] 2.3 Реализовать `emit`: валидация payload схемой `input`, чтение
      транзакции ридером контекста, запись через `OutboxStore.append`
- [ ] 2.4 `emit` без транзакции завершается ошибкой с обеими починками
- [ ] 2.5 Собрать запись: идентификатор, subject, payload, `durable`,
      `partitionKey`, момент создания, переданный контекст вызова
- [ ] 2.6 Тесты: попадание в транзакцию, откат, ошибка схемы, ошибка вне
      транзакции, сосуществование с `Op.emitter`

## 3. Хранилище

- [ ] 3.1 Объявить типы `OutboxRecord` и `OutboxStore` с `append`, `claim`,
      `settle`
- [ ] 3.2 Реализовать хранилище в памяти для тестов, включая конкурентный
      `claim`
- [ ] 3.3 Тесты интерфейса на реализации в памяти: партия не выдаётся дважды,
      `settle` идемпотентен

## 4. Relay

- [ ] 4.1 Объявить relay ресурсом: `static acquire` конструирует,
      `@OnStart(signal)` повторяет `drain()` до сигнала, `release()`
      дожидается партии
- [ ] 4.2 Метод `drain()` — один проход по партии, вызываем без цикла
- [ ] 4.3 Публикация через `IMessageBus.publish`, ключ идемпотентности равен
      идентификатору записи
- [ ] 4.4 Остановка: сигнал взведён на SHUTDOWN, партия дописана, relay
      освобождается раньше адаптера хранилища
- [ ] 4.5 Порядок внутри `partitionKey`; между разделами порядка нет
- [ ] 4.6 Backoff и отметка «застряла» после исчерпания попыток
- [ ] 4.7 Тесты через `drain()`, без таймеров: доставка, повтор с тем же
      ключом, остановка на партии, порядок раздела, застревание

## 5. Конфигурация и наблюдение

- [ ] 5.1 Объявить секцию конфига: интервал опроса, размер партии, backoff,
      число попыток, признак «relay в этом процессе»; наружу только `.keys`
- [ ] 5.2 Объявить операции-факты `outbox.published` и `outbox.stuck`
- [ ] 5.3 Тест: процесс с выключенным relay пишет записи и не запускает задачу
- [ ] 5.4 Тест: приложение без подписчиков на факты собирается и работает

## 6. Границы сборки

- [ ] 6.1 Отдать политику значением: фильтр endpoint'ов плюс предикат
      `hasVar` по переданной переменной транзакции
- [ ] 6.2 Зависимость от DI-токена шины; сборка без шины падает с обеими
      починками
- [ ] 6.3 Тесты: нарушение политики останавливает сборку до INIT; сборка без
      шины падает с ожидаемым текстом

## 7. Замер границы ядра

- [ ] 7.1 Тест границы пакета: перечень имён, импортируемых из
      `@nestling/app`, не содержит сборки приложения и объявления фичи
- [ ] 7.2 Тест самодостаточности: внешних зависимостей и драйверов нет
- [ ] 7.3 Зафиксировать живыми тестами упёршиеся места: юнит-мост для
      `Var.provide`, отсутствие типизированного ключа идемпотентности у
      событий
- [ ] 7.4 Проверить, что диff по kernel-пакетам пуст

## 8. Пример и гайд

- [ ] 8.1 Добавить в `examples/users-service` слой транзакции, адаптер
      хранилища в памяти и один `emit` через `outboxed(Op)`
- [ ] 8.2 Подключить политику предпосылки в корне примера
- [ ] 8.3 Написать главу гайда про базу данных и транзакцию: репозиторий
      читает транзакцию из контекста, слой открывает и закрывает её
- [ ] 8.4 Обновить оглавление гайда и карту понятий

## 9. Документация и журнал

- [ ] 9.1 README пакета по структуре из docs-style, с плашкой статуса и
      предпосылкой первой строкой
- [ ] 9.2 `docs/design/operations.md`: раздел о транзакционном emit'е и
      ссылка на запись журнала
- [ ] 9.3 `docs/decisions/ideas.md`: запись о точке врезки, отвергнутых
      вариантах и результате замера границы
- [ ] 9.4 `docs/decisions/deferred.md`: пометить запись [2026-07-13]
      сработавшей со ссылкой на новую запись
- [ ] 9.5 `docs/README.md`: строка пакета в таблице сателлитов
- [ ] 9.6 `docs/decisions/roadmap.md`: строка change'а и статус

## 10. Definition of Done

- [ ] 10.1 Все задачи выше отмечены
- [ ] 10.2 `yarn verify` зелёный
- [ ] 10.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 10.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [ ] 10.5 `yarn docs:audit` — 0 ERROR
- [ ] 10.6 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [ ] 10.7 Коммиты осмысленные, ветка `change/outbox` запушена
