## 1. Имена барелей на теге

- [ ] 1.1 Новый модуль `.claude/skills/docs-audit/scripts/tag-names.mjs`:
  перечень тегов выпуска (`git tag --list 'v*'`), раскладка дерева тега
  `git archive <tag> packages` во временный каталог, разбор через
  `collectPackageExports` и `allNames` из `package-exports.mjs`, приватные
  пакеты пропускаются.
- [ ] 1.2 Кэш по SHA коммита тега в `node_modules/.cache/docs-audit/`:
  попадание читает JSON, промах раскладывает дерево. Временный каталог
  удаляется в `finally`.
- [ ] 1.3 Экспорт `deletedNames(repoRoot)` — объединение имён по всем тегам
  минус имена рабочего дерева. Тегов нет — функция говорит об этом
  отдельным значением, а не пустым множеством.
- [ ] 1.4 Вход из командной строки: `node …/tag-names.mjs` печатает
  множество удалённых имён, `--self-test` проверяет разбор на временном
  репозитории с двумя тегами (имя удалено, имя вернулось).

## 2. Инвариант `deleted-names`

- [ ] 2.1 Состав живых текстов: `SECTIONS` из `scripts/site/sections.mjs` на
  обоих языках минус папка `docs/releases/`, плюс `packages/nestling.agent-skill/skill/**`,
  `README.md` и `README.ru.md` корня. Второго перечня файлов в `check.mjs`
  не заводить.
- [ ] 2.2 Разбор строки: блоки кода по оградам, инлайн-код по обратным
  кавычкам. Формы засчитывания — вызов, дженерик, спецификатор импорта из
  `@nestlingjs/*` в блоке кода; в прозе — фрагмент целиком равен имени либо
  содержит имя вызовом или дженериком. Совпадение с куском длинного
  идентификатора, пути или адреса не считается.
- [ ] 2.3 Три исключения: строка блоковой цитаты; первая ячейка строки
  таблицы в `docs/from-nestjs.md`, английской паре и `references/from-nest.md`
  скилла; одиночная форма у имени, совпавшего с сегментом имени сегодняшнего
  пакета.
- [ ] 2.4 Отчёт: ERROR с файлом, строкой и именем. Тегов нет — WARN, код
  возврата не меняется.
- [ ] 2.5 Прогон на `HEAD` до починки текстов: ровно четыре находки —
  `docs/design/config.md`, `docs/en/design/config.md` и две в
  `packages/nestling.agent-skill/skill/references/config.md`. Больше или
  меньше — правило разошлось с замером `design.md`, разбираться до починки.

## 3. Починка находок

- [ ] 3.1 `docs/design/config.md` и `docs/en/design/config.md`: `load` больше
  не имя первичного чтения фазы 0 — назвать сегодняшнее чтение или убрать
  имя из скобок. Обе половины правятся одним изменением.
- [ ] 3.2 `packages/nestling.agent-skill/skill/references/config.md`:
  `load(RootConfig)` в прозе и `objectSource(…)` в блоке без языка. Правка
  минимальная — раздел «Sources» целиком перепишет change 105.
- [ ] 3.3 Прогон аудита зелёный: `node .claude/skills/docs-audit/scripts/check.mjs`
  → 0 ERROR.

## 4. Скилл в Definition of Done

- [ ] 4.1 `CLAUDE.md`: строка Definition of Done — change, изменивший
  публичные имена, формы вызова или состав пакетов, обновляет скилл.
- [ ] 4.2 `openspec/config.yaml`, правило `rules.tasks`: та же строка в
  списке, который попадает в `tasks.md` каждого change'а.
- [ ] 4.3 `openspec/config.yaml`, поле `context`: тест-раннер — vitest на
  swc, а не jest с `--experimental-vm-modules`; `@nestlingjs/pipeline` из
  перечня убрать — ядро это `@nestlingjs/container`, `@nestlingjs/operations`
  и `@nestlingjs/app`; TypeScript 5.9; папка глав — `docs/guide/`, не
  `docs/guides/`.

## 5. Документация

- [ ] 5.1 `.claude/skills/docs-audit/SKILL.md`, слой 1: инвариант
  `deleted-names` — источник множества, состав текстов, формы засчитывания и
  три исключения. Вход `tag-names.mjs` назван рядом с `package-exports.mjs`.
- [ ] 5.2 `docs/decisions/roadmap.md`: строка 109 — **done** со ссылкой на
  архив и итогом; убрать строку из раздела «Что осталось» и поправить счётчик.
- [ ] 5.3 `docs/decisions/ideas.md`: пометка «РЕАЛИЗОВАНО» в записи
  [2026-09-14] «Разбор отчёта об обновлении до 0.3.0» по пунктам 9 и 10 —
  что вышло целиком, что уехало дальше, чем реализация уточнила решение
  (база сравнения сменилась с прошлого тега на объединение всех тегов).
  Оглавление — `node .claude/skills/docs-audit/scripts/ideas-toc.mjs`.

## 6. Definition of Done

- [ ] 6.1 Все задачи выше отмечены.
- [ ] 6.2 `yarn verify` зелёный.
- [ ] 6.3 README затронутых пакетов обновлены, включая плашки статуса.
- [ ] 6.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`;
  запись `ideas.md`, по которой шёл change, несёт пометку «РЕАЛИЗОВАНО».
- [ ] 6.5 `yarn docs:audit` — 0 ERROR.
- [ ] 6.6 Затронутые `examples/*` мигрированы; главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом».
- [ ] 6.7 Скилл обновлён, если change изменил публичные имена, формы вызова
  или состав пакетов. Здесь — правка справочника конфигурации задачей 3.2.
- [ ] 6.8 Тексты прогнаны линтером стиля:
  `node .claude/skills/docs-style/scripts/lint.mjs <пути>` → 0 запрещённых слов.
- [ ] 6.9 `main` не тронут — слияние делает Merger после `/opsx:archive`.
