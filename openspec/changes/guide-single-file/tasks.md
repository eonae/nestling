## 1. Каркас документа

- [x] 1.1 Переписать `scripts/site/layout.html` в каркас всего документа: `{{title}}`, `{{styles}}`, `{{sidebar}}`, `{{chapters}}`, `{{script}}`; убрать `<link href="styles.css">` и `<script src="app.js">`
- [x] 1.2 Убрать из шапки ссылки на страницы (`index.html`, `01-first-service.html`, `12-features.html`, `17-split.html`) — заменить якорями
- [x] 1.3 В `build.mjs` собрать все главы в один HTML: каждая глава — `<section class="chapter" id="<slug>">`, README открывает документ
- [x] 1.4 Писать результат в `docs/.site/index.html`; каталог по-прежнему очищается перед сборкой
- [x] 1.5 Проверить: после `yarn docs:build` в `docs/.site/` ровно один файл

## 2. Ссылки и идентификаторы

- [x] 2.1 Приставка `<slug>--` у идентификаторов заголовков главы (`anchorHeadings`, `sectionsOf`)
- [x] 2.2 `rewriteLinks`: `./NN-имя.md` → `#NN-имя`, `./NN-имя.md#якорь` → `#NN-имя--якорь`, `./README.md` → начало документа
- [x] 2.3 Ссылки внутри главы (`](#якорь)`) получают ту же приставку
- [x] 2.4 Ссылка на несуществующую главу остаётся ошибкой сборки с именем главы и ссылки
- [x] 2.5 Проверить: одинаковый заголовок `## Запуск` в главах 1 и 20 даёт разные идентификаторы
- [x] 2.6 Проверить: в выводе не осталось ссылок вида `href="NN-имя.html"`, а все якоря ведут в существующие идентификаторы

## 3. Навигация

- [x] 3.1 `renderSidebar` строит один сайдбар на документ: все части и главы, ссылки якорями, без деления на «открытую» страницу
- [x] 3.2 Подпункты `##` читаемой главы раскрываются на стороне браузера; в разметку кладутся данные для этого
- [x] 3.3 `renderPager` ведёт якорями на соседние главы; у последнего приложения ссылки «вперёд» нет
- [x] 3.4 В `app.js` расширить ведение активного пункта: определять читаемую главу по положению прокрутки и показывать её разделы
- [x] 3.5 Проверить в браузере: прокрутка до главы 3 отмечает её пункт и раскрывает её разделы

## 4. Подсветка на сборке

- [x] 4.1 Перенести правила подсветки (`RE`, `highlight`) из `scripts/site/app.js` в `build.mjs` без изменения самих правил
- [x] 4.2 Применять подсветку в рендере блока кода: `<span class="tok-…">` попадает в HTML
- [x] 4.3 Перенести в сборку и шапку блока (`code-head`: точки, имя файла, язык) — она тоже строилась в браузере
- [x] 4.4 Убрать `decorateCode` и правила подсветки из `app.js`; оставить тему, меню, ведение активного пункта и якоря заголовков
- [x] 4.5 Проверить: с выключенным JavaScript код в блоках подсвечен, у блока видны имя файла и язык

## 5. Тема внутри файла

- [x] 5.1 Встроить `styles.css` в `<style>` и `app.js` в `<script>` при сборке; `THEME`-копирование убрать
- [x] 5.2 Добавить в `styles.css` оформление разделов глав: граница между главами, разрыв страницы при печати
- [x] 5.3 Проверить: `index.html`, скопированный в пустой каталог, открывается с оформлением и подсветкой

## 6. Документация и проверки

- [x] 6.1 Обновить `scripts/site/README.md`: один файл на выходе, тема внутри, подсветка на сборке
- [x] 6.2 Обновить `docs/README.md` в части описания вывода
- [x] 6.3 Обновить шапку-комментарий `build.mjs`
- [x] 6.4 Сверить `.claude/skills/docs-audit/scripts/check.mjs`: правило «сгенерированный HTML не отслеживается git» работает при одном файле
- [x] 6.5 Обновить `Purpose` главной спеки `docs-site` при синхронизации: дельта-формат его не несёт, а он описывает страницы

## 7. Definition of Done

- [x] 7.1 Все задачи выше отмечены
- [x] 7.2 `yarn verify` зелёный (`build` + `typecheck` + `lint` + `test` + `type-budget` по всем пакетам)
- [x] 7.3 README затронутых пакетов обновлены — пакеты change не трогает, затронут `scripts/site/README.md`
- [x] 7.4 `design/` и `decisions/` синхронизированы по правилам `docs/README.md`
- [x] 7.5 `yarn docs:audit` → 0 ERROR
- [x] 7.6 Главы гайда не менялись — пересверять плашки не требуется; `examples/*` change не затрагивает
- [x] 7.7 Тексты правок прогнаны через `node .claude/skills/docs-style/scripts/lint.mjs` → 0 запрещённых слов
- [ ] 7.8 Коммиты осмысленные, ветка запушена
