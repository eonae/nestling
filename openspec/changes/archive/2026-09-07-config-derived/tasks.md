## 1. Объявление вычисляемого поля

- [x] 1.1 `config/declaration.ts`: класс `DerivedField` (список зависимостей и
  `fn`), тип рекорда вычисляемых полей, тип конструктора `derived`,
  типизированного рекордом секции (`deps` — кортеж из `keyof record`,
  аргументы `fn` выведены из схем полей)
- [x] 1.2 `config/declaration.ts`: проекция секции складывается из значений
  полей рекорда и возвратов `fn`; запись вычисляемого поля в декларации
  (имя, зависимости, секретность)
- [x] 1.3 `config/section.ts`: третий аргумент у `makeConfig`,
  `makeConfig.reloadable` и `makeConfig.family`; разбор рекорда вычисляемых
  полей в записи декларации
- [x] 1.4 `config/section.ts`: ошибка объявления, когда имя вычисляемого поля
  занято полем рекорда, — с именем секции и поля; та же проверка против
  зарезервированного `onChange` у reloadable-секции

## 2. Вычисление и ошибки

- [x] 2.1 `config/errors.ts`: `ConfigDerivedError` — секция, поле, список
  зависимостей, исходная ошибка в `cause`
- [x] 2.2 `config/project.ts`: вычисление после успешной валидации всех полей;
  при отказе валидации `fn` не вызывается
- [x] 2.3 `config/load.ts`: то же вычисление при первичном чтении секции на
  фазе 0 — одним кодом с проекцией из контейнера, без второй копии правила
- [x] 2.4 `config/project.ts`: пересчёт вычисляемых полей при перезагрузке —
  только если изменилось значение хотя бы одной зависимости (сравнение по
  идентичности); ошибка `fn` при перезагрузке оставляет последний валидный
  снапшот, не дёргает подписчиков и пишет предупреждение

## 3. Секретность

- [x] 3.1 `config/redact.ts`: имена секретных полей проекции включают
  вычисляемые поля с секретной зависимостью; секретность зависимости берётся
  эффективная (`isSecretKey`)
- [x] 3.2 Проверить обе поверхности печати — `toJSON()` и хук `inspect` — на
  секции с вычисляемым секретным полем

## 4. Реестр и снимок

- [x] 4.1 `config/registry.ts`: запись вычисляемых полей секции в реестре;
  в перечень ключей и в key-центричный индекс они не попадают
- [x] 4.2 `config/registry.ts`: `describeConfig(options?)` с
  `options.converters`; описание ключа несёт JSON Schema листа и исход
  конвертации, полученные диспетчером `leafJsonSchema` с `io: 'input'`
- [x] 4.3 `config/registry.ts`: перечень вычисляемых полей в снимке секции —
  имя, зависимости, эффективная секретность
- [x] 4.4 Без `converters` снимок прежний: полей JSON Schema в описании ключа
  нет

## 5. Источник окружения с префиксом

- [x] 5.1 `config/source.ts`: `env(options?)` — чтение `<prefix><KEY>` из живой
  ссылки на `process.env`, имя источника для предупреждений и перечня
  опрошенных источников
- [x] 5.2 Проверить порядок приоритета: привязанный `env({ prefix })`
  выигрывает у неявного `process.env`, а ключ без префикса читается неявным
  источником

## 6. Экспорт пакета

- [x] 6.1 `config/index.ts`: экспорт `derived`-конструктора через третий
  аргумент `makeConfig`, `env`, `ConfigDerivedError` и типов снимка
- [x] 6.2 `packages/nestling.app/src/index.ts`: те же имена в публичном
  экспорте пакета

## 7. Тесты

- [x] 7.1 Рантайм-тесты объявления: вычисляемое поле в проекции, порядок
  полей, отсутствие ключа в `.keys`, ошибка занятого имени
- [x] 7.2 Рантайм-тесты вычисления: значение из зависимостей, отмена
  вычисления при невалидном поле, `ConfigDerivedError` с именем секции и поля,
  вычисление в `load()`
- [x] 7.3 Рантайм-тесты секретности: маска в `toJSON()` и `inspect`,
  наследование от чужого объявления ключа, поле без секретных зависимостей
- [x] 7.4 Рантайм-тесты перезагрузки: пересчёт при изменении зависимости,
  отсутствие вызова `fn` при совпадении значений, keep-last-good при ошибке
  `fn`
- [x] 7.5 Рантайм-тесты снимка: описание и умолчание из схемы через конвертер,
  исход «конвертера нет», снимок без опций, вычисляемые поля в снимке
- [x] 7.6 Рантайм-тесты источника: префикс перекрывает неявный `process.env`,
  общий ключ читается без префикса, имена ключей секции не меняются
- [x] 7.7 Проверки типов (`*.type-test.ts`): опечатка в имени зависимости,
  ложная аннотация аргумента `fn`, ссылка на другое вычисляемое поле — все три
  дают ошибку компиляции; тип проекции несёт возврат `fn`

## 8. Документация

- [x] 8.1 `docs/design/config.md`: §1 — форма вычисляемого поля отдельным
  аргументом взамен записи о листе рекорда; §3 — `env({ prefix })`; §8 —
  содержимое снимка с конвертерами и перечень вычисляемых полей
- [x] 8.2 `docs/design/schemas.md`: строка таблицы потребителей конвертеров
  сверяется с реализованным поведением снимка
- [x] 8.3 `docs/decisions/ideas.md`: в записи [2026-09-06] отметить уточнение
  формы вычисляемого поля при реализации и его причину
- [x] 8.4 `docs/guide/22-config-sources.md`: разделы про вычисляемое поле,
  префикс окружения и снимок с описаниями; дата в плашке «сверено с кодом»
  обновлена
- [x] 8.5 `docs/glossary.md`: термин «вычисляемое поле», если его там ещё нет
- [x] 8.6 `packages/nestling.app/README.md`: разделы конфига, таблицы
  экспортов и плашка статуса
- [x] 8.7 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по всем
  изменённым текстам — 0 запрещённых слов

## 9. Пример

- [x] 9.1 `examples/users-service`: секция получает вычисляемое поле, на
  которое ссылается глава гайда
- [x] 9.2 Пример собирается и запускается: `yarn build` и его собственный
  прогон

## 10. Definition of Done

- [x] 10.1 Все задачи выше отмечены
- [x] 10.2 `yarn verify` зелёный (`build`, `typecheck`, `lint`, `test`,
  `type-budget` по всем пакетам)
- [x] 10.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 10.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 10.5 `yarn docs:audit` — 0 ERROR
- [x] 10.6 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [x] 10.7 Коммиты осмысленные, ветка `change/config-derived` запушена
