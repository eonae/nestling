/**
 * Источники собранного сайта и их порядок.
 *
 * Источник двух видов. `folder` — папка с оглавлением `README.md`: её
 * README открывает разделы источника, каждый файл папки становится своим
 * разделом. `page` — отдельный файл: он даёт один раздел.
 *
 * Порядок записей задаёт порядок разделов в `docs/.site/index.html` и
 * порядок пунктов сайдбара. Поле `group` называет группу сайдбара: у
 * папки с заголовками `## Часть N. …` группы дают эти заголовки, а
 * `group` остаётся за её оглавлением; у папки без них и у страницы
 * группа одна и названа здесь.
 *
 * Заголовок пункта берётся из заголовка первого уровня самого файла.
 */
export const SECTIONS = [
  { kind: 'folder', path: 'docs/guide', group: 'Гайд' },
  { kind: 'folder', path: 'docs/recipes', group: 'Рецепты' },
  { kind: 'page', path: 'docs/guarantees.md', group: 'Справочники' },
  { kind: 'page', path: 'docs/from-nestjs.md', group: 'Справочники' },
];
