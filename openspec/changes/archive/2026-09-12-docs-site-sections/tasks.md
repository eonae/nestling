# docs-site-sections — задачи

Порядок повторяет план миграции из [design.md](./design.md): сначала
модель отделяется от печати, потом появляется вторая форма вывода, потом
источники и всё остальное. Вывод сборки виден глазами на каждом шаге.

## 1. Модель разделов отдельно от печати

- [x] 1.1 Выделить в `scripts/site/build.mjs` шаг, который читает
      источники и строит модель разделов: заголовок, секция, слаг,
      группа, путь исходного файла, размеченный HTML, заголовки второго
      уровня
- [x] 1.2 Перевести печать одного файла на модель: `build()` больше не
      размечает markdown по ходу обхода источников
- [x] 1.3 Сверить вывод с прежним: `docs/.site/index.html` совпадает по
      составу разделов и якорям

## 2. Адреса и две формы вывода

- [x] 2.1 Ввести адресатор формы: функция от раздела и якоря к `href`,
      по таблице из design.md (путь для дерева, якорь для одного файла)
- [x] 2.2 Сделать слаг уникальным внутри источника, а `id` раздела в
      одном файле — с приставкой секции; обновить сообщение об ошибке
      совпадения слагов
- [x] 2.3 Напечатать дерево `<секция>/<слаг>/index.html` с оглавлением
      источника в `<секция>/index.html`
- [x] 2.4 Переименовать форму одного файла в `nestling-docs.html`,
      освободив `index.html` под стартовую страницу
- [x] 2.5 Перевести сайдбар и пейджер на адресатор: в дереве пункт ведёт
      путём, активный пункт определяется адресом страницы
- [x] 2.6 Идентификаторы заголовков: без приставки в дереве, с приставкой
      секции и слага в одном файле

## 3. Источники

- [x] 3.1 Добавить поле секции и необязательное поле `badge` в записи
      `sections.mjs`
- [x] 3.2 Подключить `docs/design/` как папку-источник с бейджем
      «целевое состояние V1»
- [x] 3.3 Подключить страницы `docs/glossary.md`, `docs/conventions.md`,
      `docs/decisions/roadmap.md`, `docs/decisions/deferred.md`
- [x] 3.4 Перевести раздел «Пакеты» в `docs/README.md` на четыре
      заголовка `###` вместо вводных строк
- [x] 3.5 Реализовать источник вида `packages`: оглавление из раздела
      «Пакеты», слаг из имени каталога, заголовок из первой строки README
      пакета
- [x] 3.6 Сверка состава: каталог `packages/` без строки в карте и строка
      без каталога останавливают сборку с именем каталога
- [x] 3.7 Обобщить чтение групп: `## Часть N. …` у папки, `###` у пакетов

## 4. Ссылки

- [x] 4.1 Добавить поле `repository` в корневой `package.json`
- [x] 4.2 Переписать разрешение ссылок на два исхода: адрес раздела или
      абсолютный адрес на GitHub
- [x] 4.3 Проверить ссылки README пакетов (`../../docs/design/*.md`,
      `../nestling.app/`) — они ведут в разделы сайта
- [x] 4.4 Проверить плашки design-доков: ссылка в `decisions/ideas.md`
      ведёт на GitHub
- [x] 4.5 Ссылка на несуществующий файл по-прежнему останавливает сборку

## 5. Стартовая страница и метаданные

- [x] 5.1 Написать `docs/index.md`: что такое Nestling, карточки по
      принципам, ссылки на первую главу, рецепты и справочник
- [x] 5.2 Добавить шаблон `scripts/site/home.html` без сайдбара и
      пейджера, оформление сетки карточек в `styles.css`
- [x] 5.3 Метаданные страницы: `title`, `description` из первого абзаца,
      Open Graph, `favicon.svg`
- [x] 5.4 Базовый адрес параметром `--base` и переменной `DOCS_BASE`;
      `canonical` и `sitemap.xml` печатаются только с ним
- [x] 5.5 Напечатать `robots.txt` и `404.html`

## 6. Поиск

- [x] 6.1 Напечатать `search-index.json` из модели разделов
- [x] 6.2 Поиск в `app.js`: загрузка индекса по первому обращению,
      совпадение подстрокой, ранг по месту совпадения
- [x] 6.3 Оформление поля поиска и списка результатов в `styles.css`
- [x] 6.4 В форме одного файла поиска нет

## 7. Сборка и проверки

- [x] 7.1 `yarn docs:dev` поднимает `StaticServer` поверх каталога вывода
      и печатает адрес
- [x] 7.2 Правила подсветки для `bash` и `json`
- [x] 7.3 Новые инварианты `docs:audit`: `docs/index.md` существует,
      файл источника назван в оглавлении, вывод не отслеживается git
- [x] 7.4 Проверить `.gitignore`: состав вывода изменился

## 8. Документация

- [x] 8.1 Переписать `scripts/site/README.md`: две формы вывода, три вида
      источника, адреса, метаданные, поиск, `docs:dev` с сервером
- [x] 8.2 Обновить `docs/README.md`: карта, правила ведения, ссылка на
      стартовую страницу
- [x] 8.3 Отметить волну 4 в записи [2026-09-05] «Структура
      документации» в `docs/decisions/ideas.md`: что вышло целиком, что
      уехало в волну 5, чем реализация уточнила решение
- [x] 8.4 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
      всем правленым текстам

## 9. Definition of Done

- [x] 9.1 Все задачи выше отмечены
- [x] 9.2 `yarn verify` зелёный
- [x] 9.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 9.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [x] 9.5 Запись `ideas.md`, по которой шёл change, несёт пометку
      «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем
      реализация уточнила решение
- [x] 9.6 `yarn docs:audit` — 0 ERROR
- [x] 9.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом» — примеров change не
      трогает, мигрировать нечего
- [x] 9.8 `main` не тронут: слияние делает Merger после `/opsx:archive`
