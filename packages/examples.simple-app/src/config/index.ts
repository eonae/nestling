/**
 * Наружу из папки конфига идёт **только описание ключей** — право привязки.
 *
 * Токен секции остаётся приватным: в многопакетном раскладе он не пересёк
 * бы границу пакета вовсе, и чужой инжект было бы нечем написать. Здесь
 * потребители живут в том же приложении и берут токен глубоким импортом
 * `../config/app.config`.
 */
export { appConfigKeys } from './app.config';
