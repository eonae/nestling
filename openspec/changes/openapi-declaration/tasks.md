## 1. Фаза 0 как значение

- [x] 1.1 Новый модуль `packages/nestling.app/src/root/composition.ts`:
  чистая функция `resolveComposition(spec, args)` — тело нынешнего
  `AssembledApp#select()`. Возвращает значения переключателей, выбранные
  фичи, единицы сборки, имена выбранных фич, `includeDeps`, объявления
  транспортов и серверов
- [x] 1.2 `AssembledApp#select()` переведён на `resolveComposition`:
  метод присваивает поля из результата и больше ничего не считает
- [x] 1.3 Спека модуля (`composition.spec.ts`): раскрытие веток и выбор
  фич считаются один раз на сборку; результат детерминирован
- [x] 1.4 Прогнать `app.spec.ts`, `switches.spec.ts`, `selection.spec.ts`,
  `check.spec.ts` — поведение `run()` и `check()` не изменилось

## 2. Метод `App.discover(args?)`

- [x] 2.1 Метод в `src/root/app.ts`: `discover(args?: AssembleArgs<S>): EndpointDiscovery`
  через `resolveComposition` и внутренний проход по единицам
- [x] 2.2 JSDoc метода: фаза 0 и остановка, синхронность, перечень ошибок
  фазы 0 и явная граница — ошибки графа остаются за `check()`
- [x] 2.3 Рантайм-тесты (`discovery.spec.ts` или новый `discover.spec.ts`):
  выбор фич отражён в результате; ветка `docs=off` не даёт endpoint
  плагина; вызов без источников конфига проходит; опечатка в имени фичи
  бросает ошибку фазы 0; неудовлетворённая зависимость методу не мешает
- [x] 2.4 Тест равенства составов: паттерны и атрибуция из
  `app.discover(args)` совпадают с отчётом `app.check(args)` для трёх
  аргументов
- [x] 2.5 Type-тест: поле переключателя без умолчания обязательно в
  аргументе `discover`, неизвестное поле отвергается компилятором

## 3. Одна публичная дверь в discovery

- [x] 3.1 `src/root/index.ts` перестаёт отдавать `discoverEndpoints`:
  разложить `export *` на явные реэкспорты `Discovery$`,
  `EndpointDiscovery`, `DiscoveredEndpoint`
- [x] 3.2 Type-тест границы: импорт `discoverEndpoints` из `@nestling/app`
  не компилируется
- [x] 3.3 Внутренние вызовы (`app.ts`, спеки пакета) импортируют функцию
  из модуля напрямую
- [x] 3.4 README `@nestling/app`: раздел про discovery и таблица экспортов
  описывают `app.discover(args?)`; строки про `discoverEndpoints(bundles)`
  удалены

## 4. `@nestling/openapi`

- [x] 4.1 JSDoc `document.ts` и `types.ts`: вход — `app.discover(args).endpoints`,
  пример вызова для CI переписан
- [x] 4.2 Шапка `index.ts`: три поверхности пакета названы с новым входом
- [x] 4.3 Тест `document.spec.ts`: документ строится из декларации
  приложения, и аргумент сборки меняет состав `paths`
- [x] 4.4 README пакета: раздел «Три способа получить документ» и пример
  входа приведены к `app.discover(...)`

## 5. Пример `app-with-http`

- [x] 5.1 `src/openapi.ts`: аргумент сборки из аргумента командной строки,
  документ через `buildOpenApiDocument(app.discover(args).endpoints, …)`,
  запись `openapi.json` в корень пакета
- [x] 5.2 Команда `openapi` в `package.json` примера; `openapi.json`
  добавлен в корневой `.gitignore`
- [x] 5.3 Прогнать команду без аргумента и с `users`, сверить состав
  `paths` — эти значения цитирует глава 12

## 6. Документация

- [x] 6.1 `docs/design/schemas.md` §2.1: в таблице поверхностей строка про
  перегрузку заменена строкой про `app.discover(args?)` как вход
  генератора
- [x] 6.2 `docs/design/composition.md`: фаза 1 ASSEMBLE и раздел про состав
  приложения называют `discover()` публичным входом фазы 0
- [x] 6.3 Гайд, глава 12: раздел «Документ в CI» со скриптом примера и
  объяснением, зачем ему аргумент сборки; дата в плашке «сверено с кодом»
  обновлена
- [x] 6.4 Гайд, глава 17: `discover()` назван рядом с `check()` там, где
  перечисляются проверки состава без запуска
- [x] 6.5 `docs/glossary.md`: статья про discovery сверена с новой
  поверхностью
- [x] 6.6 `docs/decisions/roadmap.md`, строка change'а 52: описание
  приведено к итоговому решению — метод декларации вместо перегрузки

## 7. Definition of Done

- [x] 7.1 Все задачи выше отмечены
- [x] 7.2 `yarn verify` зелёный (build, typecheck, lint, test, type-budget)
- [x] 7.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 7.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 7.5 `yarn docs:audit` — 0 ERROR
- [x] 7.6 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [ ] 7.7 Коммиты осмысленные, ветка запушена
