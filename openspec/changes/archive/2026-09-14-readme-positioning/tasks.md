## 1. Вступление README, русский оригинал

- [x] 1.1 `README.ru.md`, блок-цитата под заголовком: фреймворк написан
  агентами, служит агенту и человеку на равных, всё сводится к короткой
  петле обратной связи. Имени другого фреймворка в первой фразе нет.
- [x] 1.2 Раздел «Что это»: три места петли в порядке решения 1
  [design.md](./design.md) — приложение целиком в одном процессе; ошибка при
  компиляции; сборка падает раньше сокета. Четвёртой строкой — тестовый
  composition root. Каждая строка ведёт на главу гайда или на
  `docs/guarantees.md`.
- [x] 1.3 Новый раздел «Для кого» на месте «Когда Nestling не инструмент»:
  адресат опытный (вопросы приложения в проде), порог входа описан причиной
  (понятия знакомы по другим фреймворкам), случай «не нужен» — сервис на
  один процесс без типизированного клиента и без читателей документации.
- [x] 1.4 Порядок разделов по решению 6: «Работа с агентом» выше «Быстрого
  старта», «Статус» перед обоими.
- [x] 1.5 Раздел «Документация»: ссылка на `docs/guarantees.md` в перечне
  папок, рядом со строкой про проверки.
- [x] 1.6 Блок кода «Быстрого старта» не трогать: он сверен с кодом и к
  позиционированию отношения не имеет.

## 2. Английская пара

- [x] 2.1 `README.md`: те же правки, что в задачах 1.1–1.5. Пути ссылок —
  английские (`docs/en/...`), ссылка на таблицу проверок ведёт в
  `docs/en/guarantees.md`.
- [x] 2.2 Прогнать английский список запрещённых слов: `simple`, `just`,
  `powerful`, `seamless`, `robust`, `intuitive`, `comprehensive`,
  `effortless` в новом тексте не появляются; `magic` допустим только
  оборотом «no runtime magic».

## 3. Стартовая страница сайта

- [x] 3.1 `docs/index.md`: заголовок первого уровня называет происхождение
  кода. Лид — один абзац: ось и три места петли одной фразой каждое.
  Второго абзаца прозы до первого `##` нет.
- [x] 3.2 Карточки `::::cards`: три места петли тем же порядком плюс
  четвёртая про тестовый composition root. Названия принципов из карточек
  уходят — принципы остаются по ссылке на `design/principles.md`.
- [x] 3.3 Новый раздел `##` «Когда Nestling не нужен» без списка: шаблон
  печатает его строкой под колонками входов, рядом со «Статусом».
- [x] 3.4 Раздел «Что ещё есть»: строка про `guarantees.md` остаётся и
  ведёт на таблицу проверок.
- [x] 3.5 `docs/en/index.md`: то же самое по-английски, ссылки внутри
  английского дерева.
- [x] 3.6 `yarn docs:build` и глазами проверить первый экран: заголовок,
  лид, кнопки, пример, карточки, колонки, строка статуса и новый раздел на
  местах.

## 4. Остальные места одной фразы

- [x] 4.1 `docs/design/principles.md`, первая фраза после заголовка: ось
  вместо «меньше, современнее и архитектурно лучше NestJS». Текст принципов
  ниже не трогать.
- [x] 4.2 `docs/en/design/principles.md`: английская пара той же правки.
- [x] 4.3 `package.json`, поле `description`: вместо «Lightweight
  replacement for Nest.js with ECMAScript decorators and other nice
  simplifications» — фраза по оси, по-английски.
- [x] 4.4 `CLAUDE.md`, строка с описанием фреймворка: та же ось. Скобочная
  цитата «меньше, современнее и архитектурно лучше NestJS» уходит.

## 5. Инвариант `landing-lead`

- [x] 5.1 `.claude/skills/docs-audit/scripts/check.mjs`: проверка
  стартовой страницы каждого языка — между заголовком первого уровня и
  первым `##` ровно один абзац прозы. Ссылочная строка, забор кода и
  `::::cards` абзацем не считаются. Путь файла берётся из источника вида
  `home` в `scripts/site/sections.mjs`, второго перечня не заводить.
- [x] 5.2 Сообщение: ERROR `landing-lead`, имя файла, номер строки второго
  абзаца, подсказка про `homeParts` в `scripts/site/build.mjs`.
- [x] 5.3 `.claude/skills/docs-audit/SKILL.md`, слой 1: описание инварианта
  рядом с остальными механическими.
- [x] 5.4 Проверить обе стороны: два абзаца дают ERROR, один — нет.

## 6. Документация и журнал

- [x] 6.1 `docs/decisions/roadmap.md`: строка 80 — **done** со ссылкой на
  архив и итогом; убрать из раздела «Что осталось», поправить счётчик строк
  и абзац про порядок.
  Делается в сессии `/opsx:archive`: ссылка на архив ведёт в папку, которой
  до архивации нет, и `broken-link` в `yarn docs:audit` даёт на неё ERROR.
- [x] 6.2 `docs/decisions/ideas.md`: пометка «РЕАЛИЗОВАНО» в записи
  [2026-09-12] «Разбор обзоров d/10 и d/13», пункт 5 — что вышло целиком,
  что уехало дальше и чем реализация уточнила решение. Уточнение назвать
  прямо: ось выбрана не «три гарантии первым абзацем», а короткая петля
  обратной связи с происхождением кода в первой фразе. Оглавление —
  `node .claude/skills/docs-audit/scripts/ideas-toc.mjs`.
- [x] 6.3 Проверить, что запись [2026-09-13] «Разбор фидбэка по коду d/15»
  ссылается на этот change по пункту 2.3, и пометка там согласована.

## 7. Definition of Done

- [x] 7.1 Все задачи выше отмечены.
- [x] 7.2 `yarn verify` зелёный.
- [x] 7.3 README затронутых пакетов обновлены, включая плашки статуса.
  Здесь — ни одного: change правит README репозитория, не пакетов.
- [x] 7.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`;
  запись `ideas.md`, по которой шёл change, несёт пометку «РЕАЛИЗОВАНО».
- [x] 7.5 `yarn docs:audit` — 0 ERROR.
- [x] 7.6 Затронутые `examples/*` мигрированы; главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом». Здесь — ни одного и ни
  одной: кода change не трогает.
- [x] 7.7 Скилл обновлён, если change изменил публичные имена, формы вызова
  или состав пакетов. Здесь не изменил — скилл не трогается.
- [x] 7.8 Тексты прогнаны линтером стиля:
  `node .claude/skills/docs-style/scripts/lint.mjs README.md README.ru.md docs/index.md docs/en/index.md docs/design/principles.md docs/en/design/principles.md`
  → 0 запрещённых слов.
- [x] 7.9 `main` не тронут — слияние делает Merger после `/opsx:archive`.
