## 1. Правка README гайда

- [ ] 1.1 Сразу после вводного абзаца `docs/guide/README.md` добавить
  одну-две фразы, называющие [приложение Б «Из
  NestJS»](../../../docs/guide/appendix-b-from-nestjs.md) вторым
  документом для читателя с опытом Nest (design.md, D1) — ссылка на файл,
  без списка
- [ ] 1.2 Добавить раздел `## Карта понятий` между «Как читать» и «Что
  проверяется до первого запроса» (design.md, D1)
- [ ] 1.3 Заполнить таблицу «Понятие | Одно предложение | Глава» по
  составу design.md D2: источник — `docs/glossary.md`, порядок — порядок
  появления в частях 1–4, ссылка на главу в последней колонке (не в
  первой — иначе строка станет страницей превью, см. design.md D3),
  около 20–25 строк
- [ ] 1.4 Открыть раздел вводной фразой со ссылкой на [приложение
  В](../../../docs/guide/appendix-c-coverage.md) как на полный список
  понятий с файлами примеров (design.md D5)

## 2. Сверка со словарём и стилем

- [ ] 2.1 Каждое понятие таблицы сверить с `docs/glossary.md`: термин
  написан так же, как в глоссарии; понятий, которых там нет, в таблице
  нет
- [ ] 2.2 Каждая формулировка «одно предложение» — по правилам
  `docs-style` (глагол в предложении, без вложенных скобок и стрелок в
  прозе)
- [ ] 2.3 `node .claude/skills/docs-style/scripts/lint.mjs
  docs/guide/README.md` — 0 запрещённых слов
- [ ] 2.4 Если у `docs/guide/README.md` есть плашка с датой (сегодня её
  нет — docs-audit проверяет плашку «сверено с кодом» только у глав вне
  README и `appendix-*`) — дата обновлена; если плашки нет, задача
  закрывается без правки

## 3. Проверка сборки и аудита

- [ ] 3.1 `yarn docs:preview` собирается без ошибок
- [ ] 3.2 `docs/preview/index.html` открыт: раздел «Карта понятий» виден
  на странице и как пункт сайдбара под группой «Гайд»; лишних страниц
  превью не появилось (строки таблицы карты понятий не стали страницами)
- [ ] 3.3 `yarn docs:audit` — 0 ERROR
- [ ] 3.4 Ссылки раздела (на приложение Б, на приложение В, на все главы
  колонки «Глава») резолвятся — проверено `docs:audit`, раздел «Битые
  ссылки», отдельно не перепроверяется
- [ ] 3.5 `openspec validate guide-concept-map --type change` — без ошибок
  (дельта `specs/docs-preview-guide/spec.md` разбирается корректно)

## 4. Definition of Done

- [ ] 4.1 Все задачи разделов 1–3 отмечены
- [ ] 4.2 `yarn docs:preview` зелёный, раздел «Карта понятий» виден в
  собранном `index.html` (задачи 3.1–3.2)
- [ ] 4.3 `yarn docs:audit` — 0 ERROR (задача 3.3)
- [ ] 4.4 Линтер `docs-style` по `docs/guide/README.md` — 0 запрещённых
  слов (задача 2.3)
- [ ] 4.5 `design/` и `decisions/` не требуют новой синхронизации: правка
  не меняет API и поведение, `docs/design/principles.md` уже приведён в
  соответствие отдельным change, новой записи в `decisions/ideas.md` не
  требуется — использована существующая запись «[2026-09-04] Подача
  гайда: соответствия из NestJS вторым документом, карта понятий в
  начале»
- [ ] 4.6 README пакетов и `packages/examples.*` не затронуты: change
  меняет только `docs/guide/README.md`, миграция примеров не требуется
- [ ] 4.7 Коммиты осмысленные, ветка `change/guide-concept-map` запушена
