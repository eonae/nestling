## 1. Разведка: seam-методы BuiltApp

Развилка всего change'а. Если обход неприемлем по читаемости — часть B
пересматривается до того, как потрачено время на аннотации.

- [x] 1.1 Переписать `[CHECK_SEAM]` и `[TEST_SEAM]` в
      `packages/nestling.app/src/root/app.ts:697,750` слиянием деклараций:
      контракт интерфейсом `BuiltApp`, реализация на прототипе в `static {}`
- [x] 1.2 Проверить, что `packages/nestling.app/src/testing/index.ts:92` и
      остальные вызыватели не изменились
- [x] 1.3 Прогнать `tsc --isolatedDeclarations --emitDeclarationOnly` по `app` и
      убедиться, что `TS9038` ушёл
- [x] 1.4 Показать результат пользователю и получить решение: продолжаем часть B
      или ограничиваемся частью A

## 2. Часть A: граф задач и конфигурация

Осмысленна сама по себе, от части B не зависит.

- [x] 2.1 Перенести `skipLibCheck: true` в `tsconfig.base.json`, убрать его из
      `drizzle.pg`, `viz`, `microservice`, `modular-app`
- [x] 2.2 Добавить `"build"` в `dependsOn` таргетов `typecheck`, `lint` и `test`
      в `nx.json`
- [x] 2.3 Поставить параллелизм шесть по умолчанию
- [x] 2.4 Вынести каталог кэша nx из рабочего дерева через
      `NX_CACHE_DIRECTORY` в скриптах корневого `package.json`
- [x] 2.5 Убрать `yarn run typecheck &&` из `build` у `viz`, `cli`,
      `microservice`, `modular-app`; порядок задать графом nx
- [x] 2.6 Переписать `scripts/boundary/public-api-validator.mjs` на одну
      программу вместо двадцати
- [x] 2.7 Добавить скрипт `verify:strict`: `typecheck` без `skipLibCheck` по
      собранному `dist`, с явным списком исключённых пакетов
- [x] 2.8 Замерить прогон при шести слотах трижды подряд: гонка не
      воспроизводится, время около 112 секунд

## 3. Снимок публичных типов

- [x] 3.1 Собрать пакеты и сохранить копию всех `dist/**/*.d.ts` вне репозитория
- [x] 3.2 Записать в change'е, где лежит снимок и на каком коммите снят

## 4. Аннотации публичных экспортов

От мелких пакетов к крупным: ошибки в мелких дешевле заметить.

- [x] 4.1 Пересчитать места по всем пакетам:
      `tsc -p <pkg>/tsconfig.build.json --isolatedDeclarations
      --emitDeclarationOnly` и свести в таблицу
- [x] 4.2 Аннотировать `container` (1 место) и `common.*`
- [x] 4.3 Аннотировать `transport.http` (7) и `operations` (8)
- [x] 4.4 Переписать `makeConfig` и `makeConfig.reloadable`
      (`packages/nestling.app/src/config/section.ts:221,244`) через интерфейс
- [x] 4.5 Переписать `REQUEST_ATTRIBUTES` и `CALL_ATTRIBUTES`
      (`packages/nestling.app/src/metrics/kernel-group.ts:47,54`) через
      именованный тип
- [x] 4.6 Аннотировать оставшиеся места в `app`
- [x] 4.7 Аннотировать остальные пакеты по таблице из 4.1

## 5. Сборка без проверки типов

- [x] 5.1 Перевести один пакет на swc и убедиться, что декларации совпадают с
      прежними, а стандартные декораторы и `useDefineForClassFields` не поехали
- [x] 5.2 Включить `isolatedDeclarations: true` в `tsconfig.base.json`
- [x] 5.3 Перевести остальные пакеты; список исключённого тестового кода держать
      одинаковым у сборщика и у проекта пакета
- [x] 5.4 Убедиться, что `smoke` проходит: Node грузит собранные пакеты

## 6. Сверка и документация

- [x] 6.1 Собрать декларации заново и сверить со снимком из 3.1: дифф пуст.
      Швы `BuiltApp` сделаны до снимка, поэтому исключений нет
- [x] 6.2 Обновить README затронутых пакетов и плашки статуса
- [x] 6.3 Записать в `docs/` решение о составе проверки: что идёт в повседневном
      прогоне, что в строгом
- [x] 6.4 Уточнить у пользователя, заводить ли запись в `docs/decisions/ideas.md`:
      change пошёл от профилирования, а не от зафиксированной идеи
- [x] 6.5 Прогнать `yarn docs:audit`

## 7. Definition of Done

- [ ] 7.1 `yarn verify` зелёный
- [ ] 7.2 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 7.3 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [ ] 7.4 Запись `ideas.md` несёт пометку «РЕАЛИЗОВАНО» — либо в change'е
      записано, почему записи нет (решение по 6.4)
- [ ] 7.5 `yarn docs:audit` — 0 ERROR
- [ ] 7.6 Затронутые `examples/*` мигрированы, гайды пересверены с обновлённой
      датой в плашке «сверено с кодом»
- [ ] 7.7 `main` не тронут — слияние делает Merger после `/opsx:archive`
