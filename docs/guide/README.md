# Гайд по Nestling

Гайд ведёт от первого сервиса к приложению из нескольких фич,
развёрнутому в нескольких процессах. Каждая глава начинается с задачи,
которую вы и так собираетесь решать, и раскрывает ровно те возможности
фреймворка, без которых задача не решается. Возможности, которые задаче
пока не нужны, глава не называет вовсе: понятие появляется там, где его
объясняют, а не раньше.

Если вы писали на NestJS, начните с
[приложения Б «Из NestJS»](./appendix-b-from-nestjs.md). Оно построчно
называет, чем в Nestling записывается то, что вы делали в Nest.

Читатель гайда знает TypeScript и писал сервисы на Nest или Express.
Термины берутся из [глоссария](../glossary.md). Целевое описание каждой
подсистемы лежит в [`design/`](../design/README.md), причины решений в
[`decisions/ideas.md`](../decisions/ideas.md); главы ссылаются на них в
плашке.

## Как читать

Части 1 и 2 читайте подряд: они собирают один сервис, и каждая глава
опирается на код предыдущей. Правила именования, которым следуют
примеры, собраны в [conventions.md](../conventions.md). Часть 3 нужна, когда областей в приложении
становится больше одной. Часть 4 нужна, когда одного процесса мало.
Часть 5 читается по потребности: каждая её глава самостоятельна.

Код всех глав лежит в `examples/*`. Части 1 и 2 собирают
`users-service`, части 3 и 4 продолжают его в
`app-with-http` и `split-nats`. Сниппет в главе
начинается с пути к файлу примера и совпадает с ним. Сниппет, который
показывает промежуточный шаг, помечен комментарием «шаг главы»; итоговая
версия файла названа рядом.

## Карта понятий

Таблица называет понятие, одно предложение о нём и главу, где оно
вводится. В неё вошли понятия частей 1–4. Полный список понятий всех
частей, с файлом примера для каждого, собран в
[приложении В](./appendix-c-coverage.md).

| Понятие | Одно предложение | Глава |
|---|---|---|
| endpoint | То, что приложение обслуживает по адресу: HTTP-маршрут, CLI-команда, реализация операции | [1](./01-first-service.md) |
| фича | Единица приложения с собственными endpoint'ами, которую можно вынести в отдельный процесс | [1](./01-first-service.md) |
| декларация приложения (`makeApp`) | Значение с составом приложения: фичи, плагины, транспорты, политики, привязка конфига | [1](./01-first-service.md) |
| транспорт | Узел графа, который принимает запросы извне и передаёт их в `dispatch` | [1](./01-first-service.md) |
| схема (Standard Schema) | Любое значение, реализующее Standard Schema v1: zod, valibot, arktype | [2](./02-input.md) |
| отказ, `Fail` | Ошибка, которую endpoint объявил в `errors:`, а хендлер возвращает значением | [3](./03-errors.md) |
| хендлер | Функция или класс с методом `handle`, который получает проверенный вход и возвращает `Ok` или `Fail` | [4](./04-handler-class.md) |
| токен | Ключ, по которому у контейнера запрашивают зависимость | [5](./05-repository.md) |
| провайдер | Описание того, как получить значение для токена | [5](./05-repository.md) |
| секция (конфига) | Объект «поле → схема», который инжектируется как обычная зависимость | [6](./06-config.md) |
| пайплайн | Последовательность юнитов вокруг хендлера: `.pre` до него, `.ok` и `.catch` после, `.finally` в конце | [8](./08-logging.md) |
| юнит | Одна функция или класс в пайплайне | [8](./08-logging.md) |
| слой (пайплайна) | Один вызов `makePipeline()` с цепочкой методов, который `compose` складывает с другими | [8](./08-logging.md) |
| контекст (`ctx`) | Типизированный объект запроса, который дополняют `.pre`-юниты, а читают хендлер и остальные юниты | [8](./08-logging.md) |
| политика | Инвариант над собранным графом, который проверяется на фазе ASSEMBLE | [9](./09-auth.md) |
| форма io | Вид входа или выхода endpoint'а: `value`, `stream(T)`, `events(T)`, `multipart()`, `upload()` | [10](./10-files-and-streams.md) |
| операция | Единица общения между фичами: имя, схемы `input` и `output`, список `errors` | [12](./12-features.md) |
| реализация (операции) | Endpoint, который обслуживает операцию | [12](./12-features.md) |
| вызыватель, эмиттер | Объекты вызова операции: `caller.call` для запроса, `emitter.emit` для команды и события | [12](./12-features.md) |
| плагин | Сквозная инфраструктура, которая есть в каждом процессе и доступна по токенам | [12](./12-features.md) |
| модуль | Объект `makeModule`, который группирует провайдеры под именем | [12](./12-features.md) |
| подписчик | Имя, под которым фича подписывается на событие | [13](./13-events.md) |
| `select` | Какие фичи включить в сборку: `'all'`, список имён или `{ features, includeDeps }` | [16](./16-select.md) |
| интерком | Роль переносчика операций между процессами, назначенная объявленному транспорту | [17](./17-split.md) |
| split-развёртывание | Фичи одного приложения запущены в разных процессах и общаются через брокер | [17](./17-split.md) |

## Что проверяется до первого запроса

Nestling переносит проверки как можно раньше: часть ошибок приложения
нельзя написать, остальные останавливают старт, а не запрос. Таблица
называет проверку, момент срабатывания и главу, где проверка вводится.

| Что проверяется | Когда | Глава |
|---|---|---|
| Список зависимостей совпадает с параметрами конструктора по типам, порядку и длине | при компиляции | [5](./05-repository.md) |
| Требования слоя к внешнему контексту выполнены в точке `compose` | при компиляции | [8](./08-logging.md) |
| Хендлер возвращает то, что описывает схема `output` | при компиляции | [1](./01-first-service.md) |
| Хендлер возвращает только объявленные в `errors:` отказы | при компиляции | [3](./03-errors.md) |
| Отказ pre-юнита объявлен на слое | при компиляции | [9](./09-auth.md) |
| Отказы слоя входят в `errors:` операции | при компиляции | [12](./12-features.md) |
| Путь endpoint'а непустой, без повторяющихся path-параметров | при создании декларации | [2](./02-input.md) |
| Ключи конфига заданы и проходят схемы | фаза BOOTSTRAP, до контейнера | [6](./06-config.md) |
| Все зависимости зарегистрированы, циклов в графе нет | фаза ASSEMBLE | [5](./05-repository.md) |
| Каждый endpoint подключил обязательный слой | фаза ASSEMBLE | [9](./09-auth.md) |
| Пайплайн объявил переменную, которую читают из глубины графа | фаза ASSEMBLE | [9](./09-auth.md) |
| Формы io деклараций поддержаны транспортом | фаза ASSEMBLE | [10](./10-files-and-streams.md) |
| Для каждой схемы есть конвертер в OpenAPI | фаза ASSEMBLE | [11](./11-openapi-and-client.md) |
| Между двумя фичами нет прямого ребра графа | фаза ASSEMBLE | [12](./12-features.md) |
| У каждой вызываемой операции есть владелец или шина | фаза ASSEMBLE | [17](./17-split.md) |
| Отказ вне `errors:` не уходит клиенту | на запросе, заменяется на `internal_error` | [3](./03-errors.md) |
| Вход соответствует схеме `input` | на запросе, ответ `400` | [2](./02-input.md) |

Проверки фазы ASSEMBLE выполняются до `@OnInit` и до открытия сокета:
нарушение останавливает процесс раньше, чем он захватит ресурсы. Те же
проверки прогоняет `app.check(select)` по матрице топологий в CI
([глава 16](./16-select.md)) и тестовая сборка `assembleTest`
([глава 7](./07-testing.md)).

## Часть 1. Первый сервис

| Глава | Задача | Пример |
|---|---|---|
| [1. Поднять сервис, который отвечает на запрос](./01-first-service.md) | endpoint, фича, `makeApp`, транспорт `http()` | `users-service` |
| [2. Принять данные и не пропустить мусор](./02-input.md) | схема `input`, path и query, `bind`, ответ `400` | `users-service` |
| [3. Сказать клиенту, что пошло не так](./03-errors.md) | `makeFail`, код с категорией, `errors:`, `Ok.created` | `users-service` |
| [4. Хендлер как класс](./04-handler-class.md) | поле `handler`, `@Injectable`, юнит-тест через `new` | `users-service` |
| [5. Откуда хендлер берёт репозиторий](./05-repository.md) | токен интерфейса, `providers`, `@OnInit`, значения-провайдеры | `users-service` |
| [6. Порт и адрес базы из окружения](./06-config.md) | `makeConfig`, ключи, `secret`, fail-fast | `users-service` |
| [7. Убедиться, что работает, без запуска сервера](./07-testing.md) | `assembleTest(app, …)`, `overrides`, `vars`, юнит-тест хендлера | `users-service` |

## Часть 2. Сервис в проде

| Глава | Задача | Пример |
|---|---|---|
| [8. Видеть каждый запрос в логе](./08-logging.md) | пайплайн `.pre` и `.finally`, слой, `compose`, `Ctx(RequestId)` | `users-service` |
| [9. Пускать только своих](./09-auth.md) | pre-юнит с отказом, контекст слоя, политики `hasLayer` и `hasVar`, `detached` | `users-service` |
| [10. Файлы и большие выгрузки](./10-files-and-streams.md) | `multipart`, `upload`, `stream(T)` на входе и выходе | `users-service` |
| [11. Отдать фронтенду документацию и клиент](./11-openapi-and-client.md) | `openapi()`, `doc:`, операция с `http:`, `makeClient` | `users-service` |

## Часть 3. Приложение растёт

| Глава | Задача | Пример |
|---|---|---|
| [12. Выделить вторую область](./12-features.md) | граница фич, операция `request`, `implement`, `.caller`, плагины, модули | `app-with-http` |
| [13. Оповещать соседей о случившемся](./13-events.md) | `event`, `command`, `subscriber`, ключ идемпотентности | `app-with-http` |
| [14. Живая лента для клиента](./14-live-feed.md) | `events(T)`, `sse:`, `Topic`, `AbortSignal` | `app-with-http` |
| [15. Тестировать фичу без соседей](./15-testing-features.md) | `stubs`, `testApp.emit`, `contextValue`, `checkTopologies` | `app-with-http`, `split-nats` |

## Часть 4. Разворачивать по частям

| Глава | Задача | Пример |
|---|---|---|
| [16. Запускать только часть фич](./16-select.md) | `assemble(select)`, `includeDeps`, `load()` до сборки, `check()` | `app-with-http` |
| [17. Разнести фичи по процессам](./17-split.md) | `nats()`, `intercom`, `durable`, `propagate` | `split-nats` |
| [18. Не сломать соседей при изменении операции](./18-compatibility.md) | версия в имени, снапшот операций, `diffOperations` | `app-with-http` |

## Часть 5. Редкие задачи

| Глава | Задача | Пример |
|---|---|---|
| [19. Webhook с проверкой подписи](./19-webhook.md) | `rawBody`, требование слоя к стартовому контексту | `app-with-http` |
| [20. CLI-утилита на тех же примитивах](./20-cli.md) | `cliEndpoint`, `cli()`, argv и REPL, поток из stdin | `simple-cli` |
| [21. Логгер с именем потребителя и сбор вкладов](./21-token-families.md) | семейства токенов, `.auto`, `.all`, `familyProvider` | `container` |
| [22. Конфиг из файла и без перезапуска](./22-config-sources.md) | источники и привязка, `.keys`, общие ключи, `reloadable` | `container` |
| [23. Кто сейчас подключён и как его отключить](./23-ops.md) | реестр подписок, `tracked`, административные endpoint'ы | `app-with-http` |
| [24. Без `makeApp`](./24-standalone.md) | `makeDispatch`, `serve`, `ContainerBuilder` | `simple-http-server`, `container` |
| [25. Расширить ядро своим пакетом](./25-extending.md) | граница ядра, satellite, subpath `./testing` | `@nestling/subscriptions` |

## Приложения

| Приложение | О чём |
|---|---|
| [А. Альтернативные формы](./appendix-a-alternatives.md) | функция с `deps`, отказ броском, `.ok` и `.catch` |
| [Б. Из NestJS](./appendix-b-from-nestjs.md) | чему соответствуют понятия Nest |
| [В. Карта понятий](./appendix-c-coverage.md) | понятие, глава, файл примера |
