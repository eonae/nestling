{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Корневой конфиг используется только редактором и ad-hoc запусками tsc из корня.
    // Сборка пакетов идёт через их собственные tsconfig, наследующие tsconfig.base.json.
    "noEmit": true
  }
}
