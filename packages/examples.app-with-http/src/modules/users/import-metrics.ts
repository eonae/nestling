/**
 * Счётчики импорта — цель для `.tap` во входной item-цепочке.
 *
 * Модульное значение, а не провайдер: io-декларация создаётся вне
 * контейнера, поэтому комбинаторы цепочки остаются функциями и замыканиями.
 * Логике, которой нужен DI, место в хендлере или в request-юните.
 */
export const importMetrics = {
  /** Сколько строк дошло до цепочки за всё время жизни процесса */
  rowsSeen: 0,
};
