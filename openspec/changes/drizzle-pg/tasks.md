## 1. Каркас пакета

- [x] 1.1 Создать `packages/nestling.drizzle.pg` с полным набором конфигов:
  `package.json`, `tsconfig.json`, `tsconfig.build.json`, `eslint.config.js`,
  `jest.config.js` — копией набора `packages/nestling.outbox`
- [x] 1.2 Объявить в `package.json` подпуть `./outbox` в `exports`,
  peer-зависимости `drizzle-orm` и `pg`, необязательную peer-зависимость
  `@nestlingjs/outbox` через `peerDependenciesMeta`
- [x] 1.3 Добавить `drizzle-orm`, `pg`, `@types/pg` и `drizzle-kit` в
  devDependencies рабочей области, проверить `yarn install` и разрешение
  соседних пакетов
- [x] 1.4 Завести минимальные Standard-Schema-значения полей секции
  (`str`, `int`, `flag`) по образцу `packages/nestling.outbox/src/schema.ts`
- [x] 1.5 Собрать барель `src/index.ts` поимённым `export` и проверить
  загрузку `dist/index.js` скриптом `node scripts/smoke.mjs`

## 2. Соединение

- [x] 2.1 Объявить семейство конфиг-секций `database`: адрес секретом,
  размер пула, таймауты подключения, простоя и запроса, признак TLS,
  вычисляемое поле хоста
- [x] 2.2 Реализовать значение соединения `PgConnection<S>`: экземпляр
  drizzle на пуле и выдача сессии с открытой транзакцией
- [x] 2.3 Объявить соединение ресурсом: `acquire` открывает пул и проверяет
  его первым запросом, `release` закрывает, `health` выполняет `SELECT 1`
  с сигналом
- [x] 2.4 Написать фабрику `drizzlePg(options)`: DI-токен соединения,
  переменная транзакции, дескриптор ключей, плагин; ключи контекста и
  идентификатор DI-токена выводятся из имени экземпляра
- [x] 2.5 Остановить сборку при двух соединениях с одинаковым именем;
  текст ошибки называет имя и оба объявления
- [x] 2.6 Дать понятный текст ошибки подключения на фазе INIT: имя
  соединения и ключ конфига с адресом

## 3. Транзакция запроса

- [x] 3.1 Реализовать `PgSession<S>`: открытие командой `BEGIN` с уровнем
  изоляции, `commit`, `rollback`, `release` с откатом незавершённой
  транзакции; все три идемпотентны
- [x] 3.2 Собрать слой `db.transaction(options?)`: юнит-мост с
  зависимостью от соединения, писатель переменной, `.ok` с коммитом,
  `.catch` с откатом, `.finally` с возвратом клиента в пул
- [x] 3.3 Проверить, что провалившийся `COMMIT` меняет ответ на ошибку
- [x] 3.4 Добавить политику `db.requiresTransaction(filter?, label?)`
  поверх `everyEndpoint(...).hasVar(...)`
- [x] 3.5 Зафиксировать в `core-limits.spec.ts` пакета места замера
  границы ядра из design.md: `Var.provide` без зависимостей, `.ok` без
  превращения успеха в объявленный отказ, ресурс без фактов жизненного
  цикла

## 4. Тесты пакета

- [x] 4.1 Написать подстановку пула: выдача клиента, выполнение запроса,
  возврат клиента, закрытие пула, журнал выполненных команд
- [x] 4.2 Покрыть слой: `BEGIN` до хендлера, `COMMIT` на успехе,
  `ROLLBACK` на отказе, возврат клиента на отмене запроса, повторное
  завершение без новых команд
- [x] 4.3 Покрыть политику: сборка падает на фазе ASSEMBLE без слоя и
  проходит с ним
- [x] 4.4 Написать type-тесты: тип схемы доходит до значения соединения и
  до значения переменной, чужая таблица — ошибка компиляции
- [x] 4.5 Завести `e2e/` пакета: тесты на работающем PostgreSQL
  пропускаются без `DATABASE_URL`; проверить видимость записи внутри
  транзакции, откат, уровень изоляции

## 5. Адаптер хранилища outbox'а

- [x] 5.1 Объявить таблицу записей объявлением drizzle с настраиваемым
  именем и константу DDL; колонки и индексы — по design.md
- [x] 5.2 Реализовать `append`: вставка транзакцией вызывающего, приведение
  непрозрачного значения, ошибка с текстом об обеих починках
- [x] 5.3 Реализовать `claim` одним `UPDATE … FROM (SELECT … FOR UPDATE
  SKIP LOCKED) RETURNING *` с условием раздела и моментом готовности
- [x] 5.4 Реализовать `settle`: запрос на статус, идемпотентность, запись
  числа попыток и момента готовности для повтора
- [x] 5.5 Написать `pgOutboxStore(db)`: плагин с DI-токеном хранилища в
  подпуте `./outbox`
- [x] 5.6 Вынести набор проверок хранилища в общий файл и прогнать его на
  `InMemoryOutboxStore` и на адаптере; прогон на адаптере пропускается без
  `DATABASE_URL`
- [x] 5.7 Проверить, что приложение без `@nestlingjs/outbox` собирается, а
  корневой подпуть грузится настоящим Node

## 6. Пример `users-service`

- [x] 6.1 Описать схему drizzle: таблица пользователей и таблица записей
  outbox'а из пакета
- [x] 6.2 Настроить drizzle-kit: конфиг, каталог миграций, первая миграция,
  скрипты `db:up`, `db:migrate`
- [x] 6.3 Добавить `docker-compose.yml` с PostgreSQL и переменные в
  `.env.example`
- [x] 6.4 Переписать `database.ts` и `persistence.ts` на `drizzlePg` и
  `pgOutboxStore`; убрать таблицу в памяти, поле `databaseUrl` из секции
  `app` и юнит-мост примера
- [x] 6.5 Перевести репозиторий на drizzle: чтение соединением, запись
  транзакцией запроса
- [x] 6.6 Обновить спеки примера: тесты, которым нужна база, пропускаются
  без `DATABASE_URL`; проверка прохода relay сохраняется

## 7. Прогон и CI

- [x] 7.1 Поднять PostgreSQL сервисом в `.github/workflows/ci.yml` и
  передать `DATABASE_URL` в шаг прогона тестов
- [x] 7.2 Прогнать миграции примера перед тестами в CI
- [x] 7.3 Проверить `yarn verify` на машине без базы: тесты, которым нужна
  база, пропущены, прогон зелёный

## 8. Документация

- [x] 8.1 Написать README пакета по шести разделам в фиксированном порядке,
  не длиннее 120 строк, с плашкой статуса и перечнем экспортов, совпадающим
  с барелем
- [x] 8.2 Написать `docs/design/persistence.md` — целевое описание
  соединений, транзакции запроса и адаптера хранилища; поставить ссылку из
  `docs/design/operations.md` и строку в карту `docs/design/README.md`
- [x] 8.3 Переписать главу 27 гайда на настоящую базу, обновить дату в
  плашке «сверено с кодом», убрать раздел про адаптер «за кадром»
- [x] 8.4 Обновить README `@nestlingjs/outbox`: раздел границ называет
  готовый адаптер
- [x] 8.5 Добавить строку change'а в `docs/decisions/roadmap.md` и записать
  решение в `docs/decisions/ideas.md` с разделами «Контекст», «Решение»,
  «Отвергнутые варианты» и «Результат замера границы»
- [x] 8.6 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по всем
  изменённым текстам — 0 запрещённых слов

## 9. Definition of Done

- [x] 9.1 Все задачи выше отмечены
- [x] 9.2 `yarn verify` зелёный: `build`, `typecheck`, `lint`, `test`,
  `type-budget` по всем пакетам
- [x] 9.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 9.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [x] 9.5 Запись `ideas.md`, по которой шёл change, несёт пометку
  «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем
  реализация уточнила решение
- [x] 9.6 `yarn docs:audit` — 0 ERROR
- [x] 9.7 Пример `users-service` мигрирован, глава 27 пересверена с
  обновлённой датой в плашке «сверено с кодом»
- [x] 9.8 Коммиты осмысленные, ветка `change/drizzle-pg` запушена
