# port-invocation

## Purpose

Call-site порта одинаков независимо от того, где живёт реализация: `call`
всегда асинхронен и возвращает `Ok | Fail` закрытого множества отказов
контракта, `emit` — fire-and-forget без результата обработки. Отказ
ре-гидрируется по коду одной и той же процедурой на co-located и remote путях,
детали за границу не протекают, а вызов исполняется в собственном
request-scope обработчика — без общей транзакции и без ambient-контекста
вызывающего.

## Requirements

### Requirement: `call` всегда асинхронен и Fail-able

`Port<C>.call(input, meta?)` SHALL возвращать
`Promise<Ok<Output> | Fail<E ∪ UnknownError>>`, где `E` — отказы, объявленные
в `errors:` контракта. Форма SHALL быть одинаковой для co-located и remote
биндинга: тип call-site SHALL NOT зависеть от того, где живёт реализация.

Синхронной формы вызова и формы, бросающей отказ вместо возврата, SHALL NOT
существовать: локальный порт обязан уметь падать, иначе потребители не
обработают `Fail` и разъезд фич по процессам их сломает.

`meta` SHALL быть необязательным словарём с зарезервированным ключом
`signal: AbortSignal`; словарь SHALL быть открыт для полей эксплуатационного
профиля (`deadline`, `idempotencyKey`), добавляемых последующими change'ами
без изменения call-site.

#### Scenario: Успешный вызов

- **WHEN** потребитель зовёт `await billing.call({ orderId, amount })` и
  реализация возвращает `Ok`
- **THEN** вызыватель возвращает `Ok` со значением, разобранным по
  `output`-схеме контракта

#### Scenario: Отказ — данные, а не исключение

- **WHEN** реализация возвращает объявленный отказ `CardDeclined`
- **THEN** вызыватель возвращает `Fail` с `isFail === true` и `code ===
  'CARD_DECLINED'`, а исключения не бросает

#### Scenario: Отмена вызова

- **WHEN** потребитель передал `meta.signal` и взвёл его во время обработки
- **THEN** обработчик видит отмену своим `meta.signal`, а вызов завершается
  отказом, а не зависает

### Requirement: Отказ ре-гидрируется по коду одинаково на обоих путях

Вызыватель SHALL нормализовать ответ реализации в `Ok | Fail` одной и той же
процедурой для co-located и remote путей: код отказа SHALL сопоставляться с
определениями `errors:` контракта, и при совпадении SHALL создаваться
**настоящий** `Fail` этого определения (со `status`, `code` и `details`);
незадекларированный или отсутствующий код SHALL давать `UnknownError`.

Детали, не входящие в контракт отказа (`stack`, внутренние сообщения), SHALL
NOT пересекать границу порта: вызов SHALL выполняться с тем же уровнем
раскрытия деталей, что и вызов по проводу.

#### Scenario: Объявленный отказ восстанавливается

- **WHEN** реализация упала отказом с кодом из `errors:` контракта
- **THEN** потребитель получает `Fail`, для которого `CardDeclined.is(fail)`
  истинно

#### Scenario: Незадекларированный отказ

- **WHEN** реализация упала отказом, которого нет в `errors:` контракта
- **THEN** потребитель получает `UnknownError`, а оригинал уходит в
  диагностический хук

#### Scenario: Stack не пересекает границу

- **WHEN** реализация бросила необработанное исключение
- **THEN** потребитель получает `UnknownError` без `stack` и без текста
  исходной ошибки

### Requirement: `emit` — fire-and-forget без результата обработки

`Emitter<C>.emit(payload, meta?)` SHALL возвращать `Promise<void>`,
резолвящийся по факту **доставки** (постановки вызова подписчикам), а не по
факту обработки. Отказ обработчика SHALL NOT всплывать вызывающему и SHALL
уходить в диагностический хук ядра.

Для вида `command` доставка SHALL идти ровно одному обработчику; для вида
`event` — всем подписчикам, причём отказ одного SHALL NOT влиять на доставку
остальным.

#### Scenario: Событие доставлено двум подписчикам

- **WHEN** эмиттер публикует событие, на которое подписаны две фичи
- **THEN** обе реализации вызываются, и `emit` резолвится, не дожидаясь их
  завершения

#### Scenario: Отказ подписчика изолирован

- **WHEN** один из двух подписчиков падает
- **THEN** второй обрабатывает событие штатно, `emit` не отвергается, а отказ
  первого попадает в диагностический хук

#### Scenario: Команда — ровно один обработчик

- **WHEN** эмиттер команды публикует payload
- **THEN** вызывается ровно одна реализация контракта

### Requirement: Вызов порта не участвует в транзакции и контексте вызывающего

Вызов порта SHALL исполняться в **собственном request-scope** обработчика:
ambient-контекст вызывающего SHALL NOT протекать внутрь реализации,
и общей транзакции между вызывающим и обработчиком SHALL NOT существовать —
даже когда обе фичи живут в одном процессе.

Валидация `input` по схеме контракта SHALL выполняться на обоих путях
биндинга — как для запроса, пришедшего по проводу.

#### Scenario: Контекст вызывающего не виден обработчику

- **WHEN** вызывающий обрабатывает HTTP-запрос с установленной
  ambient-переменной и зовёт порт
- **THEN** реализация видит собственный request-scope, а не переменную
  вызывающего

#### Scenario: Невалидный вход отвергается

- **WHEN** потребитель зовёт порт с payload, не проходящим `input`-схему
  контракта
- **THEN** вызов возвращает отказ валидации, реализация не вызывается
