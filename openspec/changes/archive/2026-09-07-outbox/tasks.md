## 1. Каркас пакета

- [x] 1.1 Создать `packages/nestling.outbox` по общему набору конфигурации:
      `tsconfig.json`, `tsconfig.build.json`, `eslint.config.js`,
      `jest.config.js`, скрипты `clear`/`typecheck`/`build`/`lint`/`lint:fix`/`test`
- [x] 1.2 Объявить зависимости: только `@nestling/container`,
      `@nestling/app`, `@nestling/operations`, `@common/misc`; внешних нет
- [x] 1.3 Добавить проект в `nx.json` и убедиться, что `yarn verify` его видит
- [x] 1.4 Объявить плагин `outbox(options)` через `makePlugin` с полями
      `transaction`, `store`, `operations`

## 2. Транзакционный эмиттер

- [x] 2.1 Объявить семейство DI-токенов `outboxed(Operation)` со значением
      `Emitter<C>`; операция вида `request` не компилируется
- [x] 2.2 Рецепт семейства: искать операцию по имени в `operations`,
      ронять сборку с текстом и починкой, если имени нет
- [x] 2.3 Реализовать `emit`: валидация payload схемой `input`, чтение
      транзакции ридером контекста, запись через `OutboxStore.append`
- [x] 2.4 `emit` без транзакции завершается ошибкой с обеими починками
- [x] 2.5 Собрать запись: идентификатор, subject, payload, `durable`,
      `partitionKey`, момент создания, переданный контекст вызова
- [x] 2.6 Тесты: попадание в транзакцию, откат, ошибка схемы, ошибка вне
      транзакции, сосуществование с `Op.emitter`

## 3. Хранилище

- [x] 3.1 Объявить типы `OutboxRecord` и `OutboxStore` с `append`, `claim`,
      `settle`
- [x] 3.2 Реализовать хранилище в памяти для тестов, включая конкурентный
      `claim`
- [x] 3.3 Тесты интерфейса на реализации в памяти: партия не выдаётся дважды,
      `settle` идемпотентен

## 4. Relay

- [x] 4.1 Объявить relay ресурсом: `static acquire` конструирует,
      `@OnStart(signal)` повторяет `drain()` до сигнала, `release()`
      дожидается партии
- [x] 4.2 Метод `drain()` — один проход по партии, вызываем без цикла
- [x] 4.3 Публикация через `IMessageBus.publish`, ключ идемпотентности равен
      идентификатору записи
- [x] 4.4 Остановка: сигнал взведён на SHUTDOWN, партия дописана, relay
      освобождается раньше адаптера хранилища
- [x] 4.5 Порядок внутри `partitionKey`; между разделами порядка нет
- [x] 4.6 Backoff и отметка «застряла» после исчерпания попыток
- [x] 4.7 Тесты через `drain()`, без таймеров: доставка, повтор с тем же
      ключом, остановка на партии, порядок раздела, застревание

## 5. Конфигурация и наблюдение

- [x] 5.1 Объявить секцию конфига: интервал опроса, размер партии, backoff,
      число попыток, признак «relay в этом процессе»; наружу только `.keys`
- [x] 5.2 Объявить операции-факты `outbox.published` и `outbox.stuck`
- [x] 5.3 Тест: процесс с выключенным relay пишет записи и не запускает задачу
- [x] 5.4 Тест: приложение без подписчиков на факты собирается и работает

## 6. Границы сборки

- [x] 6.1 Отдать политику значением: фильтр endpoint'ов плюс предикат
      `hasVar` по переданной переменной транзакции
- [x] 6.2 Зависимость от DI-токена шины; сборка без шины падает с обеими
      починками
- [x] 6.3 Тесты: нарушение политики останавливает сборку до INIT; сборка без
      шины падает с ожидаемым текстом

## 7. Замер границы ядра

- [x] 7.1 Тест границы пакета: перечень имён, импортируемых из
      `@nestling/app`, не содержит сборки приложения и объявления фичи
- [x] 7.2 Тест самодостаточности: внешних зависимостей и драйверов нет
- [x] 7.3 Зафиксировать живыми тестами упёршиеся места: юнит-мост для
      `Var.provide`, отсутствие типизированного ключа идемпотентности у
      событий
- [x] 7.4 Проверить, что диff по kernel-пакетам пуст

## 8. Пример и гайд

- [x] 8.1 Добавить в `examples/users-service` слой транзакции, адаптер
      хранилища в памяти и один `emit` через `outboxed(Op)`
- [x] 8.2 Подключить политику предпосылки в корне примера
- [x] 8.3 Написать главу гайда про базу данных и транзакцию: репозиторий
      читает транзакцию из контекста, слой открывает и закрывает её
- [x] 8.4 Обновить оглавление гайда и карту понятий

## 9. Документация и журнал

- [x] 9.1 README пакета по структуре из docs-style, с плашкой статуса и
      предпосылкой первой строкой
- [x] 9.2 `docs/design/operations.md`: раздел о транзакционном emit'е и
      ссылка на запись журнала
- [x] 9.3 `docs/decisions/ideas.md`: запись о точке врезки, отвергнутых
      вариантах и результате замера границы
- [x] 9.4 `docs/decisions/deferred.md`: пометить запись [2026-07-13]
      сработавшей со ссылкой на новую запись
- [x] 9.5 `docs/README.md`: строка пакета в таблице сателлитов
- [x] 9.6 `docs/decisions/roadmap.md`: строка change'а и статус

## 10. Definition of Done

- [x] 10.1 Все задачи выше отмечены
- [x] 10.2 `yarn verify` зелёный
- [x] 10.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 10.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [x] 10.5 `yarn docs:audit` — 0 ERROR
- [x] 10.6 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [x] 10.7 Коммиты осмысленные, ветка `change/outbox` запушена
