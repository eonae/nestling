## 1. Брендированная проверка отказов

- [x] 1.1 Объявить в `@nestlingjs/operations` тип `ValidateHandlerFails`:
  вычитание объявленных отказов и `FailOf<KernelFail>` из возвращённых,
  отсечка по `FailCode`, анонимный литерал с полями `__error`,
  `returned`, `declared`, `hint` (design D3, D5, D6, D7)
- [x] 1.2 JSDoc типа называет причину двух позиций проверки и ссылается на
  фикстуру, которая её закрепляет (design D4)
- [x] 1.3 Применить проверку в слоте `handler` формы с классом и в
  возвращаемом типе формы с функцией у `httpEndpoint` и `cliEndpoint`
- [x] 1.4 Применить её же у `implement` в `@nestlingjs/app`
- [x] 1.5 Рантайм-тест: незаявленный отказ, дошедший до границы, заменяется
  на `internal_error` — проверка типов ослаблена до бренда, и граница
  остаётся единственной гарантией (design D5)

## 2. Два конструктора у транспорта HTTP

- [x] 2.1 Объявить `httpEndpoint.implement(Operation, { pipeline?, handler,
  detached?, on? })` двумя перегрузками; операция первым аргументом
- [x] 2.2 Переименовать `HttpOperationDictionary` в
  `HttpImplementDictionary`, удалить ключ `operation` и девять полей
  `never`; оставить `pipeline`, `detached`, `on`
- [x] 2.3 Удалить из `httpEndpoint` две перегрузки формы с операцией и
  ветку `'operation' in declaration`; у имени остаются две перегрузки
- [x] 2.4 Сохранить три рантайм-проверки `httpEndpoint.implement`:
  `assertOperation`, `assertOperationOwned`, отсутствие секции `http:`;
  префикс текстов — `httpEndpoint.implement(Op, { … })`
- [x] 2.5 Обновить состав экспортов `index.ts` и README пакета
- [x] 2.6 Спеки пакета: `operation-endpoint.spec.ts`,
  `handler-form.type-test.ts`, `helpers.spec.ts`, интеграционные спеки
  транспорта и клиента — переезд на новое имя

## 3. Фикстуры и снапшоты диагностик

- [x] 3.1 Перевести фикстуры формы с операцией на `httpEndpoint.implement`
  (`operation-redeclared-input`, `operation-layer-fail-undeclared`)
- [x] 3.2 Добавить фикстуры: незаявленный отказ у обеих форм обоих
  конструкторов; ключ `operation` в словаре `httpEndpoint`
- [x] 3.3 Добавить фикстуры верных деклараций и проверку «диагностик нет»
  отдельно от снапшотов
- [x] 3.4 Добавить фикстуру хендлера без выводимого результата (переменная
  типа `HandlerFn`) — закрепляет границу проверки
- [x] 3.5 Снять снапшоты и прочитать каждый глазами: текст обязан называть
  код отказа и садиться на строку `handler:`

## 4. Бюджет типов

- [x] 4.1 Снять замер бенч-графа `@nestlingjs/app/type-tests/bench` до
  правки (`yarn workspace @nestlingjs/app type-budget --report`)
- [x] 4.2 Снять замер после и добавить строку в `BUDGET.md` с датой,
  версией TypeScript и причиной роста
- [x] 4.3 Если порог задет — не двигать его молча: записать причину и
  вынести решение отдельно

## 5. Примеры и скилл

- [x] 5.1 Мигрировать `examples/users-service` и `examples/app-with-http`
  (четыре декларации формы с операцией)
- [x] 5.2 Мигрировать сниппеты скилла `get-user.endpoint.ts`,
  `create-user.endpoint.ts` и три файла `references/`
- [x] 5.3 Написать `references/diagnostics.md` — последней задачей, цитируя
  снятые снапшоты, а не по памяти
- [x] 5.4 Добавить `diagnostics.md` в таблицу «куда смотреть дальше»
  `SKILL.md`; проверка состава скилла обязана пройти
- [x] 5.5 Поднять версию скилла и обновить README пакета

## 6. Документация

- [x] 6.1 Главы гайда 12, 13, 14, 27 — переезд на новое имя, дата в плашке
  «сверено с кодом» обновлена
- [x] 6.2 `docs/glossary.md` — `httpEndpoint.implement` назван рядом с
  `implement`; оговорка «только в анонимном `httpEndpoint`» у
  HTTP-хендлера становится «только в `httpEndpoint`»
- [x] 6.3 `docs/design/endpoints.md`, `operations.md`, `transports.md` —
  форма с операцией названа новым именем
- [x] 6.4 README `@nestlingjs/transport.http` — таблица экспортов и плашка
  статуса

## 7. План работ

- [x] 7.1 `docs/decisions/roadmap.md`: строка **72**
  `http-method-constructors` (71 занят `inbox`), абзац о порядке — 72
  после 68; строка 68 переписана под `httpEndpoint.implement` вместо
  `httpImplement` из записи `ideas.md`
- [x] 7.2 `docs/decisions/deferred.md`: запись [2026-09-03] о конструкторе
  с методом в имени получает пометку о сработавшем триггере и ссылку на 72
- [x] 7.3 `docs/decisions/ideas.md`: запись [2026-09-12] получает пометку
  «РЕАЛИЗОВАНО» с уточнением решения 2 — брендирование у функции живёт в
  возвращаемом типе, а проверка в слоте для неё недостижима

## 8. Definition of Done

- [ ] 8.1 Все задачи выше отмечены
- [ ] 8.2 `yarn verify` зелёный (`build`, `typecheck`, `lint`, `test`,
  `type-budget` по всем пакетам)
- [ ] 8.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 8.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`
- [ ] 8.5 Запись `ideas.md`, по которой шёл change, несёт пометку
  «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем
  реализация уточнила решение
- [ ] 8.6 `yarn docs:audit` — 0 ERROR
- [ ] 8.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [ ] 8.8 Коммиты осмысленные, ветка `change/http-endpoint-two-names`
  запушена
