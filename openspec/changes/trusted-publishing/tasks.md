## 1. Публикация без ключа доступа

- [x] 1.1 `.github/workflows/release.yml`: шаг публикации без
  `NODE_AUTH_TOKEN`, комментарий шапки про доверенного издателя
- [x] 1.2 `.github/workflows/release.yml`: `npm install -g npm@latest` —
  доверенного издателя npm понимает с 11.5.1
- [ ] 1.3 Доверенный издатель заведён на странице каждого публикуемого
  пакета: владелец `eonae`, репозиторий `nestling`, файл `release.yml`
  *(действие человека, не кода; возможно только после того, как пакеты
  появились в реестре)*
- [ ] 1.4 Секрет `NPM_TOKEN` удалён из настроек репозитория *(действие
  человека, не кода)*

## 2. Первая публикация имени

- [x] 2.1 `scripts/publish.mjs`: режим `--interactive` с проверкой входа
  через `npm whoami` и предупреждением об отсутствии provenance
- [x] 2.2 `scripts/publish.mjs`: в интерактивном режиме ввод и вывод
  достаются `npm publish` — диалог второго фактора ведёт он, и passkey
  подтверждается адресом в браузере
- [x] 2.3 `scripts/publish.mjs`: в режиме по умолчанию вывод
  перехватывается — workflow запускает скрипт без терминала
- [x] 2.4 Версия `0.1.1` опубликована с рабочей машины *(действие
  человека, не кода)*

## 3. Свежесть глав гайда

- [x] 3.1 `.claude/skills/docs-audit/scripts/check.mjs`: манифест пакета
  исключён из расчёта даты последнего изменения примера
- [x] 3.2 Проверено, что правка кода примера по-прежнему даёт WARN, а
  подъём версии — нет

## 4. Документация

- [x] 4.1 `RELEASING.md`: раздел «Первая публикация имени», требование
  2FA в «Что нужно один раз», разбор отказа `403`
- [x] 4.2 `RELEASING.md`: два workflow на один `git push --follow-tags`
- [ ] 4.3 `docs/decisions/ideas.md`, запись [2026-09-10] «Публикация в
  npm»: пометка «РЕАЛИЗОВАНО» дополнена тем, чем реализация уточнила
  решение — ключа доступа в процессе нет, первая версия имени уходит с
  рабочей машины
- [ ] 4.4 `docs/decisions/archlog.md`: запись [10.09.2026] про публикацию
  в npm дополнена или продолжена абзацем про доверенного издателя
- [ ] 4.5 `docs/decisions/roadmap.md`: change добавлен в раздел «После
  волны 6» со статусом

## 5. Definition of Done

- [ ] 5.1 Все задачи выше отмечены
- [ ] 5.2 `yarn verify` зелёный (build, typecheck, lint, test,
  type-budget)
- [ ] 5.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 5.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`
- [ ] 5.5 Запись `ideas.md`, по которой шёл change, несёт пометку
  «РЕАЛИЗОВАНО»
- [ ] 5.6 `yarn docs:audit` — 0 ERROR
- [ ] 5.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [ ] 5.8 Коммиты осмысленные, ветка запушена
