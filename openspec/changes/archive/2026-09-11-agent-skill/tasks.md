## 1. Каркас пакета

- [x] 1.1 Создать `packages/nestling.agent-skill/` с полным набором конфигов по
  образцу `packages/nestling.eslint-plugin/`: `tsconfig.json`,
  `tsconfig.build.json`, `eslint.config.js`, `jest.config.js`
- [x] 1.2 `tsconfig.json` включает `src` и `snippets`; `tsconfig.build.json`
  берёт только `src` и не эмитит спеки
- [x] 1.3 Написать `package.json`: имя `@nestlingjs/agent-skill`, общие поля
  манифеста (`keywords`, `license`, `repository`, `homepage`, `bugs`,
  `engines`, `publishConfig`), скрипты `clear`, `typecheck`, `build`, `lint`,
  `test`, поле `files: ["dist", "skill"]`, одна команда в `bin`
- [x] 1.4 Объявить в `devDependencies` через `workspace:*` пакеты, которые
  импортируют сниппеты: `app`, `container`, `operations`, `transport.http`,
  `testing`; плюс `zod`. В `dependencies` не должно быть ничего
- [x] 1.5 Проверить, что пакет виден `scripts/packages.mjs`:
  `node -e "import('./scripts/packages.mjs').then(m => console.log(m.publishablePackages().map(p => p.name)))"`

## 2. Команда установки

- [x] 2.1 Написать `src/index.ts` с `installSkill(options)`: копирование
  каталога скилла в `.claude/skills/nestling/`, разбор трёх исходов по файлу
  (создан, совпал, расходится), возврат отчёта значением
- [x] 2.2 Каталог-источник находить через `new URL('../skill/', import.meta.url)`,
  а не через `process.cwd()`
- [x] 2.3 Написать `src/cli.ts` с shebang: разбор `--dir` и `--force`, вызов
  `installSkill`, печать трёх чисел и поимённого списка расходящихся файлов,
  код выхода 1 при расхождении без `--force`
- [x] 2.4 Написать спеки на `installSkill` во временном каталоге: чистый
  проект, повторный запуск, изменённый файл без `--force` и с ним, `--dir`
- [x] 2.5 Проверить, что `tsc` сохранил shebang в `dist/cli.js`, а файл
  запускается: `node dist/cli.js --dir <временный каталог>`

## 3. Сниппеты и проверка

- [x] 3.1 Написать `scripts/snippets.mjs`: разбор файлов скилла на блоки с
  пометкой `<!-- snippet: … -->`, режим сверки и режим перезаписи, findings
  списком
- [x] 3.2 Сверка идёт в обе стороны: блок без файла, файл без блока,
  расхождение содержимого, блок ```typescript без пометки
- [x] 3.3 Добавить в `package.json` скрипт, который переписывает блоки из
  файлов сниппетов
- [x] 3.4 Написать `src/snippets.spec.ts`: вызов сверки, ожидание пустого
  списка findings; сообщение об ошибке называет и markdown, и файл сниппета
- [x] 3.5 Добавить в спеку проверки состава скилла: перечень восьми файлов
  `references/`, ссылки из таблицы `SKILL.md` в обе стороны, потолки строк
  (250 и 200), состав пяти частей `SKILL.md`, наличие `name` и `description`
  во frontmatter, отсутствие кириллицы в файлах скилла

## 4. Текст скилла: `SKILL.md`

- [x] 4.1 Написать frontmatter: `name: nestling` и `description` с поводами
  загрузки — код на Nestling, разбор ошибки сборки, перенос с NestJS
- [x] 4.2 Часть 1: чем Nestling отличается от NestJS — декларации значениями,
  явный список зависимостей, отказы значениями, операции между фичами,
  проверки до первого запроса
- [x] 4.3 Часть 2: скелет сервиса файлами по `docs/conventions.md` и
  `examples/users-service`
- [x] 4.4 Часть 3: минимальное работающее приложение одним сниппетом —
  операция, endpoint, `makeApp`, `assemble().run()`
- [x] 4.5 Часть 4: правила, которые ловит компилятор или фаза ASSEMBLE —
  пять пунктов из требования спеки, каждый с коротким «пиши так» и «не так»
- [x] 4.6 Часть 5: таблица «нужно X — читай `references/Y.md`» со всеми восемью
  файлами
- [x] 4.7 Решить открытый вопрос design'а: помещается ли таблица «из Nest» в
  `SKILL.md` целиком; если да — убрать `references/from-nest.md` из перечня
  спеки, если нет — оставить

## 5. Текст скилла: `references/`

- [x] 5.1 `endpoints.md` — операция и endpoint, `input`/`output`/`errors`,
  привязка path и query, формы io, две формы хендлера
- [x] 5.2 `container.md` — DI-токен, провайдеры, `@Component`/`@Resource`/`@Handler`,
  модули, фазы жизненного цикла, семейства DI-токенов
- [x] 5.3 `pipeline.md` — `makePipeline`, `.pre`/`.ok`/`.catch`/`.finally`,
  `compose`, контекст и его переменные, политики над собранным графом
- [x] 5.4 `errors.md` — `makeFail`, категория и уточнение в коде, `errors:`
  операции, `Ok`/`Fail`, тип `Output`, отказы слоя
- [x] 5.5 `config.md` — `makeConfig`, секции, `secret`, `derived`, источники,
  чтение до контейнера
- [x] 5.6 `features.md` — `makeFeature`, границы фичи, вызыватель и эмиттер,
  подписчики событий, переключатели, `select`, split через интерком
- [x] 5.7 `testing.md` — `assembleTest`, `overrides`, стабы операций,
  `checkTopologies`, юнит-тест хендлера через `new`
- [x] 5.8 `from-nest.md` — таблица соответствий по
  `docs/guide/appendix-b-from-nestjs.md`
- [x] 5.9 Каждый файл называет README пакета, откуда читать перечень имён, и не
  повторяет его

## 6. Проверки репозитория

- [x] 6.1 Добавить в `nx.json` в именованный вход `sources` пути
  `{projectRoot}/snippets/**/*` и `{projectRoot}/skill/**/*`
- [x] 6.2 Проверить, что кэш инвалидируется: правка `skill/SKILL.md` и
  повторный `nx run @nestlingjs/agent-skill:test` выполняются, а не берутся
  из кэша
- [x] 6.3 Добавить в `scripts/pack-check.mjs` проверку поля `bin`: файл каждой
  команды есть в тарболе и запускается из установленного пакета
- [x] 6.4 Прогнать `yarn pack:check` целиком: новый пакет ставится и
  импортируется

## 7. Документация

- [x] 7.1 Написать `packages/nestling.agent-skill/README.md` по-русски по общей
  структуре шести разделов, с плашкой статуса и таблицей экспортов
- [x] 7.2 Добавить пакет в таблицу «Инструменты и сателлиты» в `docs/README.md`
- [x] 7.3 Добавить раздел про скилл в `README.md` и `README.ru.md`: одна
  команда установки и одна фраза о том, что скилл даёт
- [x] 7.4 Завести строку change'а в `docs/decisions/roadmap.md`
- [x] 7.5 Записать решение в `docs/decisions/ideas.md`: формат, доставка
  пакетом, английский текст, сниппеты компилируемыми файлами и отвергнутые
  варианты из `design.md`. **Требует явного «запиши» от пользователя** —
  без записи change нельзя заархивировать: `docs:audit` требует пометки
  «РЕАЛИЗОВАНО» у записи сделанного change'а
- [x] 7.6 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
  README пакета и изменённым файлам `docs/`; каталог скилла линтеру не
  передавать

## 8. Definition of Done

- [x] 8.1 Все задачи выше отмечены
- [x] 8.2 `yarn verify` зелёный
- [x] 8.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 8.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 8.5 Запись `ideas.md`, по которой шёл change, несёт пометку
  «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем реализация
  уточнила решение
- [x] 8.6 `yarn docs:audit` — 0 ERROR
- [x] 8.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [x] 8.8 Коммиты осмысленные, ветка запушена
