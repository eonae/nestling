## 1. Правило

- [x] 1.1 Записать состав и порядок шести разделов README пакета в
  `docs/README.md`, раздел «Правила ведения»: заголовок и «что это», плашка
  статуса, установка, минимальный пример, экспорты, границы пакета; потолок
  120 строк; бюджет раздела «Экспорты» — 40 строк
- [x] 1.2 Привести раздел «README пакета» в `.claude/skills/docs-style/SKILL.md` к
  тому же составу: убрать «основные понятия» и «справочник API», добавить
  «экспорты»
- [x] 1.3 Описать в `docs/README.md` два формата перечня экспортов: таблица
  «имя — что делает», пока имена помещаются по одному в строку, и группы по
  подсистемам, когда не помещаются

## 2. Проверка в `docs:audit`

- [x] 2.1 Написать извлечение публичных имён пакета: барели из поля `exports`
  файла `package.json`, рекурсивное раскрытие `export * from './x/index.js'`,
  разбор через TypeScript compiler API по `src`, без обращения к `dist`
- [x] 2.2 Покрыть извлечение проверкой на трёх пакетах с подпутями:
  `@nestling/app` (`./testing`), `@nestling/container` (`./tokens`),
  `@nestling/transport.nats` (`./testing`)
- [x] 2.3 Добавить в `check.mjs` инвариант состава и порядка разделов; в
  сообщении назвать файл и лишний либо переставленный заголовок
- [x] 2.4 Добавить инвариант потолка 120 строк с фактической длиной в сообщении
- [x] 2.5 Добавить инвариант плашки статуса: блок цитаты за первым абзацем, у
  `@nestling` — ссылки в `docs/design/` и `docs/guide/`, не больше трёх каждого
  вида; у `@common` — одна строка без ссылок
- [x] 2.6 Добавить инвариант полноты перечня экспортов: сверка с барелем в обе
  стороны, вывод двумя списками, реэкспорт соседнего пакета одной строкой,
  группа на каждый подпуть, `bin` вместо перечня
- [x] 2.7 Добавить инвариант бюджета раздела «Экспорты» — 40 строк
- [x] 2.8 Замерить время `yarn docs:audit quick` до и после; если разбор барелей
  заметно замедлил его, оставить в `quick` первые три инварианта, а полноту
  перечня перенести в полный аудит
- [x] 2.9 Снять с прогона аудита список всех расхождений по восемнадцати
  пакетам — он служит точкой возобновления для групп 3–5

## 2a. Сужение барелей `app` и `operations`

Заведена на apply: перечни оказались втрое больше оценки, и требование
полноты (D4) при прежних барелях невыполнимо. Решение — D10 в `design.md`.

- [x] 2a.1 Собрать читателей каждого публичного имени: импорт вне `src` пакета,
  упоминание в `docs/design` и `docs/guide`
- [x] 2a.2 Переписать `packages/nestling.app/src/index.ts` поимённым `export`;
  реэкспорт `@nestling/operations` и `@common/misc` — отдельными операторами
- [x] 2a.3 То же для `packages/nestling.operations/src/index.ts`
- [x] 2a.4 Вернуть имена, которые нужны сборке потребителя: без них `tsc` не
  может назвать выводимый тип (TS2742) — `Feature`, `ReloadableConfig`,
  типы переменных контекста, `EventOperation` и секции конфигурации
- [x] 2a.5 Обновить снапшоты диагностик типов: порядок членов объединения в
  тексте ошибки следует за порядком экспорта
- [x] 2a.6 Записать правило барреля в спеку `packages-layout`

## 3. Пакеты без переезда текста

- [x] 3.1 `@common/graphs` — привести к структуре
- [x] 3.2 `@common/static-server` — привести к структуре
- [x] 3.3 `@common/misc` — привести к структуре, плашка внутреннего пакета
- [x] 3.4 `@nestling/viz` — привести к структуре; вместо перечня имён назвать
  команду `nestling-viz`; починить расхождение `exports` в `package.json` с
  отсутствующим `src/index.ts`
- [x] 3.5 `@nestling/openapi.zod` — привести к структуре
- [x] 3.6 `@nestling/client` — привести к структуре

## 4. Пакеты со средним объёмом пересказа

- [x] 4.1 `@nestling/transport.cli` (143 строки) — разложить абзацы по трём
  корзинам, переписать
- [x] 4.2 `@nestling/openapi` (150) — то же
- [x] 4.3 `@nestling/eslint-plugin` (199) — то же; перечень правил линтера
  оставить в разделе «Экспорты» группой
- [x] 4.4 `@nestling/transport.nats` (218) — то же; группа на подпуть `./testing`
- [x] 4.5 `@nestling/outbox` (226) — то же
- [x] 4.6 `@nestling/subscriptions` (228) — то же
- [x] 4.7 `@nestling/models` (284) — то же

## 5. Крупные пакеты

- [x] 5.1 Провести инвентарь пяти крупных README: каждый абзац отнести к одной
  из трёх корзин — копия существующего текста, семантика без копии, шаг обучения
  без копии; результат записать в файл инвентаря внутри каталога change'а
- [x] 5.2 Перенести семантику без копии в design-доки подсистем; при расхождении
  README и кода сначала сверить с кодом и записать результат сверки
- [x] 5.3 Перенести шаги обучения без копии в главы гайда; обновить дату в
  плашке «сверено с кодом» у затронутых глав — шагов без копии не нашлось,
  главы не менялись, даты не двигались (см. `inventory.md`)
- [x] 5.4 `@nestling/testing` (422 строки) — переписать
- [x] 5.5 `@nestling/operations` (481) — переписать; 154 имени группами по
  подсистемам
- [x] 5.6 `@nestling/transport.http` (595) — переписать; 62 имени
- [x] 5.7 `@nestling/container` (992) — переписать; 96 имён, группа на подпуть
  `./tokens`
- [x] 5.8 `@nestling/app` (2092) — переписать; 168 собственных имён семью
  группами, реэкспорт `@nestling/operations` одной строкой, группа на подпуть
  `./testing`
- [x] 5.9 Собрать имена, которым при инвентаре не нашлось читателя, в список для
  журнала: кандидаты на сужение публичного барреля

## 6. Журнал и план

- [x] 6.1 Добавить в `docs/decisions/ideas.md` запись с уточнением структуры
  README: минимальный пример остаётся, потому что README открывают на странице
  npm, где ни гайда, ни `design/` рядом нет
- [x] 6.2 Поставить пометку «РЕАЛИЗОВАНО» у записи
  [2026-09-05] «Структура документации»: волна 2 закрыта, назвать уточнения из
  6.1 и что уехало в волны 3–5
- [x] 6.3 Если список из 5.9 не пуст — записать его в `ideas.md` и завести строку
  в `docs/decisions/roadmap.md` на сужение публичного барреля отдельным change'ом
  — барели `app` и `operations` сужены здесь (2a), перечень убранного в
  `inventory.md`; строка 62 роадмапа заведена на остальные шестнадцать пакетов
- [x] 6.4 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по всем
  восемнадцати README и по изменённым файлам `docs/`

## 7. Definition of Done

- [x] 7.1 Все задачи выше отмечены
- [x] 7.2 `yarn verify` зелёный
- [x] 7.3 README затронутых пакетов обновлены, включая плашки статуса — здесь это
  все восемнадцать
- [x] 7.4 `design/` и `decisions/` синхронизированы по правилам `CLAUDE.md`
- [x] 7.5 Запись `ideas.md`, по которой шёл change, несёт пометку «РЕАЛИЗОВАНО» с
  тем, что вышло целиком, что уехало дальше и чем реализация уточнила решение
- [x] 7.6 `yarn docs:audit` — 0 ERROR
- [x] 7.7 Затронутые `examples/*` мигрированы; главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом» — правок не потребовалось:
  имя, названное в `docs/design` или `docs/guide`, из барреля не убиралось,
  и все примеры собираются без изменений
- [ ] 7.8 Коммиты осмысленные, ветка запушена
