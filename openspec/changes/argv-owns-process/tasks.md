## 1. Граница командной строки

- [x] 1.1 Новый модуль `packages/nestling.app/src/root/command-exit.ts`:
      чистая `failureText(error)` строит текст отказа — сообщение, цепочка
      `cause` вглубь с отступом, `AggregateError` вширь по `errors`, обрыв
      цикла по посещённым, значение не-`Error` через `String`
- [x] 1.2 Там же граница `failAsCommand(error): never` — печать текста в
      `stderr` и `process.exit(1)`; комментарий объясняет выход теми же
      словами, что у `--help` в `args.ts`
- [x] 1.3 Там же обрамления `underArgv(args, run)` и
      `underArgvAsync(args, run)`: `isArgv(args)` ложна — вызов проходит
      насквозь и отказ остаётся броском
- [x] 1.4 Спека `command-exit.spec.ts`: текст без причины, текст с цепочкой
      `cause`, `AggregateError`, цикл в цепочке, значение не-`Error`;
      печать в `stderr` и код выхода `1` под моком `process.exit`

## 2. Входы декларации

- [x] 2.1 `run()` в `app.ts` разделён: фазы 0–5 переехали в приватный
      `#runPhases`, идемпотентность (`#started`) живёт внутри него, а
      `run()` остался обрамлением через `underArgvAsync`
- [x] 2.2 `discover()` обрамлён `underArgv`
- [x] 2.3 `check()` обрамлён `underArgvAsync`
- [x] 2.4 JSDoc трёх входов называет новое поведение: маркер завершает
      процесс сам, объектная форма отдаёт отказ вызывающему

## 3. Поведение под маркером

- [x] 3.1 Отказ разбора командной строки у `run()`, `discover()` и
      `check()`: сообщение в `stderr`, код выхода `1`, стека в выводе нет
- [x] 3.2 Отказ фазы WIRE под маркером — недостижимая операция: её
      достижимость проверяет связывание портов, а не фаза BUILD
- [x] 3.3 Отказ фазы INIT под маркером — конструктор провайдера бросает
- [x] 3.4 Существующий тест `app.check(argv(line('--mail', 'carrier-pigeon')))`
      переписан с `rejects.toThrow` на мок `process.exit`
- [x] 3.5 Объектная форма: промис отклонён, процесс не завершён, в
      `stderr` ничего не напечатано
- [x] 3.6 Срезанный список `argv(process.argv.slice(2))` остаётся
      `TypeError`: маркера ещё нет, владеть процессом нечему

## 4. Документация

- [x] 4.1 `docs/design/composition.md` — правило «маркер владеет
      процессом» в разделе про аргумент сборки, рядом со строкой про
      `--help`; английская пара `docs/en/design/composition.md`
- [x] 4.2 `docs/guide/19-select.md` — блок с отказом фазы BUILD показывает
      исход под маркером; плашка «сверено с кодом» получает новую дату и
      SHA; английская пара `docs/en/guide/19-select.md`
- [x] 4.3 `docs/guide/02-composition.md` и английская пара — строка про
      маркер, если она расходится с новым поведением
- [x] 4.4 `packages/nestling.app/README.md` и `README.ru.md` — пример
      точки входа и плашка статуса
- [x] 4.5 `node .claude/skills/docs-style/scripts/lint.mjs <изменённые тексты>`
      → 0 запрещённых слов

## 5. Definition of Done

- [x] 5.1 Все задачи разделов 1–4 отмечены
- [x] 5.2 `yarn verify` зелёный (`build` + `typecheck` + `lint` + `test` +
      `type-budget` по всем пакетам)
- [x] 5.3 README затронутых пакетов обновлены, включая плашки статуса
- [x] 5.4 `design/` и `decisions/` синхронизированы по правилам
      `CLAUDE.md`
- [x] 5.5 Запись [ideas.md [2026-09-14] «Разбор отчёта об обновлении до
      0.3.0»](../../../docs/decisions/ideas.md), пункт 7, несёт пометку
      «РЕАЛИЗОВАНО» с тем, что вышло целиком, что уехало дальше и чем
      реализация уточнила решение
- [x] 5.6 `yarn docs:audit` → 0 ERROR
- [ ] 5.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
      обновлённой датой в плашке «сверено с кодом»
- [x] 5.8 `main` не тронут — слияние делает Merger после `/opsx:archive`
