## 1. Тип источника

- [x] 1.1 Параметризовать `ConfigSource<Needs = void>` в
  `packages/nestling.app/src/config/source.ts`: поле
  `needs?: ConfigSectionToken<Needs>` и `init?(values: Needs)`
- [x] 1.2 Сделать `bind()` дженериком по `Needs`; хранить в `Binding`
  источник как `ConfigSource<unknown>`, чтобы список привязок оставался
  однородным
- [x] 1.3 Проверить типами, что источник без `needs` пишется как раньше:
  `*.type-test.ts` на `env()`, `dotenv(path)` и источник с `needs` чужой
  формы (ожидаемая ошибка компиляции)

## 2. Очерёдность подъёма

- [x] 2.1 Считать граф зависимостей привязок в `reader.ts`: ребро «`A`
  после `B`», если таргет `B` покрывает ключ секции `A.needs` и `B` — не
  сама `A`
- [x] 2.2 Стабильный топологический порядок: не связанные зависимостью
  привязки сохраняют порядок списка
- [x] 2.3 Отказ по циклу до первого `init()` — новый `ConfigSourceCycleError`
  с цепочкой имён источников; `optional` его не проглатывает
- [x] 2.4 Писать порядок подъёма в логгер ядра уровнем `debug`
- [x] 2.5 Спеки: порядок при пустых `needs`, Vault выше `.env` в списке и
  позже него по подъёму, цикл из двух и из трёх источников

## 3. Проекция секции `needs`

- [x] 3.1 Перед `init()` зависимого источника находить декларацию секции по
  префиксу `needs.keys`; отсутствие в реестре — отказ с префиксом и именем
  источника
- [x] 3.2 Отказывать, если секция объявлена `makeConfig.reloadable`
- [x] 3.3 Читать ключи секции только из привязок, поднявшихся раньше, и
  фиксировать значения в снимке; финальный проход по объявленным ключам не
  перечитывает то, что уже лежит в снимке
- [x] 3.4 Исключить привязку из разрешения ключей её собственной секции
  `needs` — в единственной точке поиска значения (`#lookup`)
- [x] 3.5 Проецировать и валидировать секцию тем же кодом, что и граф;
  помечать декларацию потреблённой
- [x] 3.6 Отказ проекции — новый `ConfigSourceNeedsError`: источник,
  секция, недостающие ключи, перечень поднятых источников, исходная ошибка
  в `cause`; секретные значения в текст не попадают
- [x] 3.7 `optional: true` проглатывает отказ проекции так же, как отказ
  `init()`
- [x] 3.8 Спеки: значения приходят в `init(values)`; источник, поднятый
  позже, координат не меняет; непокрытые ключи называются в отказе;
  `optional` пропускает источник; reloadable-секция и необъявленная секция
  отказывают

## 4. Пакет `@nestlingjs/config.vault`

- [ ] 4.1 Завести `packages/nestling.config.vault` по общему набору файлов
  (`package.json`, `tsconfig.json`, `tsconfig.build.json`,
  `eslint.config.js`, `vitest.config.js`, `LICENSE`)
- [ ] 4.2 Объявить секцию `VaultConfig` (`vault`): `addr`, `token`
  (`secret()`), `mount` с умолчанием `secret`, `path`
- [ ] 4.3 Реализовать `vault(section, options?)`: `needs` — переданная
  секция, тип аргумента ограничен `VaultCoordinates`
- [ ] 4.4 `init(values)` — один запрос `GET {addr}/v1/{mount}/data/{path}`
  штатным `fetch` с заголовком `X-Vault-Token`; рекорд `data.data`
  запоминается, `get(key)` читает из него синхронно
- [ ] 4.5 Повторы по опции `retries` (умолчание `0`): сетевой отказ и
  `5xx` повторяются с паузой от 250 мс с удвоением, `401`, `403` и `404` —
  нет
- [ ] 4.6 Отказы называют адрес, точку монтирования и путь; значение
  `VAULT_TOKEN` не попадает ни в сообщение, ни в `cause`
- [ ] 4.7 Спеки пакета на подставном `fetch`: успешное чтение, форма
  запроса, повторы, отказы прав и отсутствия секрета

## 5. Прогон против живого Vault

- [ ] 5.1 `docker-compose.yml` пакета: Vault в dev-режиме на нестандартном
  порту, `healthcheck`, скрипты `vault:up` и `vault:down` в `package.json`
- [ ] 5.2 Каталог `e2e`: обвязка с пропуском без `TEST_VAULT_ADDR`, запись
  секрета через HTTP API, чтение источником, секрет без ключа
- [ ] 5.3 Сервис Vault и переменные `TEST_VAULT_ADDR`, `TEST_VAULT_TOKEN`
  в `.github/workflows/ci.yml`

## 6. Документация

- [ ] 6.1 README пара нового пакета (`README.ru.md` и `README.md`) с
  плашкой статуса по правилам `docs-package-readme`
- [ ] 6.2 Строка `@nestlingjs/config.vault` в таблице пакетов
  `docs/README.md` и счёт каталогов там же
- [ ] 6.3 Раздел рецепта `docs/recipes/config-sources.md` про источник,
  берущий координаты из другого источника, и его английская пара
- [ ] 6.4 Уточнить `docs/design/config.md` §3 до итоговой формы опций
  `vault(...)` (у минимального источника есть `retries`, наблюдения нет) и
  английскую пару
- [ ] 6.5 Запись в `docs/decisions/deferred.md` про наблюдение за Vault
  (опрос против внешнего сигнала) с триггером
- [ ] 6.6 Прогнать `node .claude/skills/docs-style/scripts/lint.mjs` по
  всем изменённым текстам — 0 запрещённых слов

## 7. Definition of Done

- [ ] 7.1 Все задачи разделов 1–6 отмечены
- [ ] 7.2 `yarn verify:fresh` зелёный — кэш nx реплеит прошлый вывод, и
  обычный `yarn verify` может соврать зелёным
- [ ] 7.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 7.4 `design/` и `decisions/` синхронизированы по правилам
  `CLAUDE.md`
- [ ] 7.5 Запись [ideas.md [2026-09-13]](../../../docs/decisions/ideas.md)
  «Конфигурация: привязки на `run()`, `env()` и `dotenv()` умолчанием,
  `bind()`, `needs` у источника» несёт пометку «РЕАЛИЗОВАНО» с тем, что
  вышло целиком, что уехало дальше и чем реализация уточнила решение
- [ ] 7.6 `yarn docs:audit` — 0 ERROR
- [ ] 7.7 Затронутые `examples/*` мигрированы, главы гайда пересверены с
  обновлённой датой в плашке «сверено с кодом»
- [ ] 7.8 Строка 92 `roadmap.md` обновлена, оглавление `ideas.md`
  пересобрано (`node .claude/skills/docs-audit/scripts/ideas-toc.mjs`)
- [ ] 7.9 Коммиты осмысленные, `main` не тронут: слияние делает Merger
  после `/opsx:archive`
