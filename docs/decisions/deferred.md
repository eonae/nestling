# Отложенные темы (будущее развитие)

Темы, которые **обсуждались и осознанно отложены**: не приняты (нет записи в
[ideas.md](./ideas.md)), не запланированы (нет change в [roadmap.md](./roadmap.md)),
но уже продуманы. Назначение файла — при возникновении потребности не начинать
с нуля и не переоткрывать уже пройденные развилки.

Открытый вопрос *уже принятого* решения живёт в его записи ideas.md, пока
идёт change. Пережил change — переезжает сюда одной записью, а в ideas.md
вычёркивается со ссылкой на неё. Одна тема — одна запись: новая запись
ideas.md, где вопрос всплыл снова, ссылается сюда, а не пересказывает.

Формат записи: контекст → что уже придумано → почему отложено → триггер возврата.

---

## [2026-07-13] `header()`: параметры операции в HTTP-заголовках

**Откуда:** обсуждение канонизации HTTP-input — конвенция размещения + bind-карта
([d/06, продолжение](../history/discussions/06-critical-design-review.md)).

**Граница, зафиксированная в обсуждении** (meta vs input для заголовков):

- **ambient / cross-cutting** заголовки (`Authorization`, `traceparent`,
  `X-Request-Id`) — это *контекст* запроса, одинаковый для всех ручек: живут в
  meta/ctx через pre-юниты (`withIdentity` и т. п.), как сейчас. На клиенте
  симметрично: конфигурируются один раз при создании клиента, не аргумент вызова.
- `Accept` / `Content-Type` / `Last-Event-ID` — концерн транспорта
  (content negotiation, SSE-реконнект), до пользователя не доходят вообще.
- **заголовок-«параметр операции»** — клиент обязан передать его осознанно
  per-call, без него вызов не имеет смысла (`If-Match`/expectedVersion для
  оптимистичной блокировки; данные в заголовках внешних контрактов, которые мы
  не контролируем) — кандидат в **input** через пометку
  `header('If-Match', schema)` в bind-карте. Тогда контракт остаётся
  transport-agnostic: на NATS то же поле едет просто в payload, заголовок —
  деталь HTTP-биндинга.

**Почему параметр операции нельзя оставить в meta:** декларация endpoint'а
интроспектируема, pre-юнит — непрозрачная функция. Заголовок, поднятый в meta
pre-юнитом, выпадает из статически выводимого контракта: OpenAPI не узнает об
обязательном заголовке, типизированный клиент не потребует аргумент. Всё, что
клиент передаёт per-call, должно жить в декларации (input + bind).

**Почему отложено:** реальных кейсов мало — правило «бизнес-данные в body/query,
не в кастомных заголовках» в силе; v1 HTTP-канона обходится path/query/body.
Bind-карта аддитивна: добавить `header()` позже — обратно-совместимо
(асимметрия рисков — тот же аргумент, что при отказе от `.after`).

**Триггеры возврата:**

- первый реальный кейс оптимистичной блокировки (`If-Match`) или интеграции с
  внешним API, возящим данные операции в заголовках;
- дизайн типизированного клиента / OpenAPI-генерации, если им понадобится полная
  модель заголовков;
- смежная отложенная под-тема, вернуть вместе: **типизация ответных заголовков**
  в output-декларации (`Location`, `ETag`) — сейчас их задаёт
  `HttpResponse.of(ok, { headers })`, и схема `output` про них ничего не
  знает.

---

## [2026-07-13] Outbox / saga: точка интеграции с портами

> **Сработало** 2026-09-07 в части outbox'а: запись
> [ideas.md [2026-09-07]](./ideas.md) «Транзакционный outbox: точка
> врезки, предпосылка транзакции и результат замера границы», пакет
> [`@nestlingjs/outbox`](../../packages/nestling.outbox/). Saga остаётся
> отложенной: у неё своя тема — корреляция и состояние процесса.

**Откуда:** критическое ревью
([d/06 §5](../history/discussions/06-critical-design-review.md)). Дисциплина
портов ([ideas.md, 2026-07-08](./ideas.md)) уже фиксирует: кросс-фичевая
согласованность = события + outbox/saga, не общая транзакция — но точка
интеграции не определена.

**Что уже придумано:**

- **Outbox** — транзакционный emit: событие пишется в БД в одной транзакции с
  бизнес-изменением, отдельный relay доставляет его в шину. Вероятная точка
  интеграции — **не ядро, а satellite-пакет**: обёртка emitter'а поверх
  хранилища + relay-процесс, встроенный в lifecycle (стартует в RUN-фазе,
  корректно гасится на SHUTDOWN). Контрактный API потребителя (`.emit()`)
  не меняется — меняется биндинг на сборке, в духе dispatch-политик.
- **Saga** — уровень приложения поверх `command`/`event`; от фреймворка ей
  нужны идемпотентность (принята, см. [ideas.md, 2026-07-13](./ideas.md)) и
  correlation в meta — отдельного механизма в ядре не предполагается.

**Почему отложено:** нет реального кейса; черта «kernel 1.0» — ядро не тянет
хранилище; дизайн зависит от принятых, но ещё не реализованных вещей (порты,
NATS-транспорт, идемпотентность).

**Триггеры возврата:**

- первый реальный кейс кросс-фичевой согласованности с БД (событие обязано
  выжить при падении после коммита);
- ~~реализация L4/NATS в roadmap — спроектировать relay вместе с
  транспортом~~ — **сработал** 2026-07-31 (change 12 `transport.nats`) и
  сознательно не привёл к решению: транспорт даёт долговечную **доставку**
  (`durable` в контракте), но транзакционный emit остаётся про хранилище, а
  ядро хранилище не тянет. Точка интеграции подтверждена реализацией:
  обёртка эмиттера — обычный провайдер, relay — обычный `@OnStart`-хук,
  правок ядра ни то ни другое не требует;
- ~~запрос transactional emit от пользователей портов~~ — **сработал**
  2026-09-07 и привёл к решению: точкой врезки стал явный DI-токен
  `outboxed(Op)`, а не подмена члена `EmitterFamily`, как предполагала эта
  запись. Причина — повторная регистрация рецепта семейства запрещена
  контейнером, а невидимая транзакционность спорит с «explicit over
  implicit»;
- саге по-прежнему нет реального кейса: триггер её возврата остаётся
  открытым.

---

## [2026-07-14] Health/readiness из фаз lifecycle

> **Сработало** 2026-09-06: запись [ideas.md [2026-09-06]](./ideas.md) «Пробы:
> `HealthCheck$` и `Health$` в ядре, транспорты адаптируют», change
> `health` ([архив](../../openspec/changes/archive/2026-09-09-health/)).

> **Superseded** записью [ideas.md [2026-09-06]](./ideas.md) «Пробы: `HealthCheck$` и `Health$` в ядре, транспорты адаптируют»: семейство и единый узел проб в ядре, транспорты только адаптируют.

**Откуда:** критическое ревью, предложение №8
([d/06](../history/discussions/06-critical-design-review.md)).

**Что уже придумано:**

- Оба сигнала выводимы из фазовой модели без пользовательского кода:
  **readiness** = «процесс дошёл до RUN», **liveness** = «SHUTDOWN не
  начался». Никакой отдельной подсистемы — чтение текущей фазы App.
- Транспортная поверхность (`/healthz`, `/readyz`) — **сантехника
  HTTP-транспорта**, не endpoints: без схем и контрактов, включается
  опцией транспорта.
- Прикладные проверки (БД доступна, очередь жива) — уже спроектированный
  паттерн `IHealthCheck` + `Family.all` (multi-injection, ideas.md
  [2026-07-10]): вклады едут с фичами, состав массива следует за `select`.

**Почему отложено:** аддитивно по построению (опция транспорта + чтение
фазы) — не входит в закрытое breaking-окно фиксации API V1 (ideas.md
[2026-07-14] «Kernel 1.0»); дизайн тривиален после реализации фазовой
модели (change 10).

**Триггеры возврата:** реализация change 10 (`features`, фазы/go-live);
первый k8s-деплой nestling-приложения.

---

## [2026-07-14] Wire-формат ошибок: RFC 9457 (problem+json)

> **РЕАЛИЗОВАНО 2026-09-14** — change `problem-json` (#83 roadmap).
> Реализовано целиком: HTTP-граница отвечает на отказ документом RFC 9457
> под медиатипом `application/problem+json`, и три места сериализации —
> ответ пайплайна, ответ до пайплайна и кадр `event: error` у SSE —
> свелись к одной функции. Типизированный клиент и генератор OpenAPI
> читают и описывают тот же документ.
> Реализация уточнила две вещи. Первая: идентичность отказа переехала
> в `type` значением `urn:error:<код>`, а поля `code` в теле не осталось
> вовсе. Задуманный «`code` → `type` (URI-суффикс)» оказался недописанным
> решением: голый код синтаксически не проходит (`not_found:user`
> читается по RFC 3986 как схема `not_found` с путём `user`, а
> подчёркивание в схеме запрещено), а расширение `code` рядом с
> `about:blank` оставило бы стандартную ось типа пустой. Вторая: место
> модуля выбрали зависимости, а не концерн. Сериализация — дело
> HTTP-транспорта, но документ читает типизированный клиент, который
> работает в браузере и серверный пакет не тянет; формат лёг в
> `@nestlingjs/operations` и реэкспортируется транспортом. Член `title`
> вывелся из категории фразой статуса HTTP: поле `title` у `makeFail`
> расширяло бы публичный API ядра, и это отдельная работа.
> Открытым осталось то, что в замысле не значилось: член `instance` и
> корреляция запроса в теле (нужен доступ к `RequestId` в момент ответа)
> и ссылки на документацию сервиса в `type` (нужны страницы по кодам
> отказа).

**Откуда:** критическое ревью, предложение №10
([d/06](../history/discussions/06-critical-design-review.md)).

**Что уже придумано:**

- `ErrorDetails` ложится на RFC 9457 почти один в один: `code` → `type`
  (URI-суффикс), `status` → `status` (через транспортный маппинг),
  `message` → `detail`, `details`/`requestId` → extension members;
  `Content-Type: application/problem+json`.
- Место в архитектуре очевидно: сериализация тела ошибки — **концерн
  HTTP-транспорта** (маппинг `ErrorStatus` → HTTP-статус уже там);
  модель ошибок (Fail, code-идентичность, `E ∪ UnknownError`) не меняется.

**Почему отложено:** чисто аддитивная сериализация — не влияет на
error-model (change 15) и не входит в breaking-окно; решать при реализации
транспортного маппинга ошибок, когда появится формат по умолчанию.

**Триггеры возврата:** реализация change 15 (`error-model`) — выбрать
дефолтный wire-формат; первый внешний потребитель ошибок (типизированный
клиент/OpenAPI-доки), которому нужен стандартный медиатип.

---

## [2026-07-31] Вариадический `compose(...layers)`

**Откуда:** change 23 `pipeline-type-dx` — переписывание сигнатуры `compose`
на прямой вывод тип-параметров ([roadmap](./roadmap.md), волна 2).

**Что уже придумано:** пока `compose` объявлен через `A extends AnyPipeline`,
вариадическая форма была нереалистична — каждый уровень переразворачивал всю
цепочку. После перехода на прямой вывод (`Pipeline<RA, AA, NA>` в позиции
параметра, накопление прямыми `AA & AB` в возвращаемом типе) variadic tuple
types становятся выполнимыми: `...rest: Pipelines<…>` с накоплением
свёрткой по кортежу. Замеры показали, что дорога именно вложенность, а не
арность: 50 слоёв вложенными вызовами — 64 286 инстанциаций против
476 561 514 до правки ([BUDGET.md](../../packages/nestling.app/type-tests/BUDGET.md)).

**Почему отложено:** снятие потолка в 4 слоя за вызов — **расширение
публичного API**, а change 23 закрывал breaking-окно фиксации API V1.
Расширение аддитивно (перегрузки 2–4 остаются), поэтому спешить незачем;
цена ожидания — ноль.

**Триггеры возврата:** первый реальный композит, которому 4 слоя за вызов
тесны (вложенность работает, но читается хуже); появление слоёв,
собираемых программно (список известен только в рантайме сборки).

---

## [2026-07-31] Форма типа-ошибки `{ __error; … }` как правило фреймворка

**Откуда:** change 23 `pipeline-type-dx` — форма зафиксирована для pipeline
(точка композиции, pre-тракт) и её единственного существующего потребителя
(слот `pipeline` в `httpEndpoint`).

**Что уже придумано:** словарь формы — `__error` (строковый литерал с
проблемой на естественном языке) + рекорд «имя поля → его тип»
(`missing`, для перезаписи — `conflicting: { поле: [было, стало] }`),
опциональный `hint` там, где подсказка называет конкретное действие.
Инвариант печати: тип-ошибка обязана быть **анонимным развёрнутым литералом
в точке печати** (именованный дженерик-алиас TypeScript печатает именем, и
текст сообщения пропадает) — исполнимая проверка правила — снапшоты в
`packages/nestling.app/type-tests/`.

**Почему отложено:** общее правило на весь фреймворк заводить рано — точек
всего две с половиной. Кандидаты (`endpoint`-словари, `bind`-карта, будущие
`makeContract`/`makeConfig`) приводятся к форме по мере надобности, по
месту.

**Триггеры возврата:** третья независимая подсистема, которой понадобился
свой тип-ошибка (тогда форма фиксируется записью в ideas.md и общим
хелпером в ядре); либо расхождение словарей между уже существующими
точками.

---

## [2026-07-31] `isOneOf([A, B])` — матчинг юниона определений отказа

**Откуда:** change 15 `error-model` — открытый вопрос design'а: нужен ли
`.catch`-юниту хелпер для разбора нескольких кодов сразу.

**Что уже придумано:** `isOneOf([OrderNotFound, CardDeclined])(res)` —
предикат, сужающий до юниона отказов; форма зеркалит `Definition.is`.

**Почему отложено:** на миграции примеров ни одного `.catch`-юнита,
разбирающего больше одного кода, не появилось — единственный
(`auditDeletions` в `examples.app-with-http`) матчит ровно один отказ.
Хелпер на гипотетическую потребность — лишняя поверхность API; пока
достаточно последовательных `A.is(res)`.

**Триггеры возврата:** первый реальный `.catch`, разбирающий три и более
кода (два ещё читаются цепочкой `if`); либо потребность в таком предикате
у клиента (`makeClient`), где ветвление по коду — основной способ работы
с ответом.

---

## [2026-07-31] `defineFail` без схемы деталей, но с произвольным `details`

**Откуда:** change 15 `error-model` — открытый вопрос design'а: разрешать
ли `defineFail('X', { status, message })` с нетипизированными деталями в
конструкторе.

**Что уже придумано:** решение — **не разрешать**. Формы ровно две: со
схемой (конструктор принимает детали, валидирует и выводит их тип) и без
неё (конструктор без аргументов). Проверено на миграции: отказы без
осмысленных деталей действительно оформляются как конструктор без
аргументов (`NothingToUpdate()`, `EmptyStdin()`), а всё, у чего детали
есть, схему объявляет — она же нужна OpenAPI (#20) и клиентам (#22).

**Почему отложено:** отложено не решение, а его пересмотр — запись
фиксирует, что вариант рассматривался и отвергнут по schema-first.

**Триггеры возврата:** случай, где детали отказа принципиально
не описываются схемой (например, произвольный payload чужой системы,
пробрасываемый как есть) — тогда нужна форма с `details: unknown` и явным
отказом от валидации, а не молчаливое ослабление правила.

---

## [2026-07-31] Гранулярность `bytesIn`/`bytesOut` в `summary`

**Откуда:** change 6 `streaming-v2` — открытый вопрос design'а: считать
байты до или после сжатия.

**Что уже придумано:** V1 считает **байты на проводе так, как их видит
транспорт**: HTTP дописывает прочитанное тело и записанные байты ответа.
Сжатия в V1 нет ([transports.md](../design/transports.md) — compression вне
скоупа), поэтому «до» и «после» сейчас совпадают и вопрос не наблюдаем.
Поле опционально (`bytesIn?`/`bytesOut?`) именно для того, чтобы транспорт,
который байтов не знает, ничего не выдумывал.

**Почему отложено:** решать нечего, пока нет второй точки отсчёта.
Выбирать семантику вслепую — значит зафиксировать её до появления
compression-сантехники, которая сама и определит, где стоит счётчик.

**Триггеры возврата:** появление сжатия в HTTP-транспорте (тогда решить:
две пары счётчиков или одна с явно документированной точкой замера);
транспорт, у которого байты и элементы принципиально не совпадают
(бинарный протокол с фреймами).

---

## [2026-07-31] `events` во входе: клиент льёт поток событий

**Откуда:** change 6 `streaming-v2` — форма `events(T)` объявлена
транспортно нейтральной, но HTTP объявляет её только в `output`.

**Что уже придумано:** механизм формы полностью симметричен — item-цепочки,
поэлементная валидация и счётчики написаны один раз и не знают о слоте.
Отсутствует не поддержка формы, а **способность транспорта**: у HTTP-запроса
нет входного канала с семантикой открытой подписки, и `capabilities.input`
это честно отражает. Значит, включение — это одна строка в capabilities
транспорта, у которого такой канал есть.

**Почему отложено:** нет реального кейса. Кандидат — WebSocket-транспорт,
которого в V1 нет; выдумывать под него семантику заранее — проектировать
без потребителя.

**Триггеры возврата:** появление транспорта с двунаправленным каналом
(WebSocket, gRPC streaming); запрос «клиент шлёт бесконечный поток
телеметрии», который сегодня решается `stream(T)` без семантики подписки.

---

## [2026-07-31] `throttle` при переполнении: буферизовать или ронять

**Откуда:** change 6 `streaming-v2` — открытый вопрос design'а о поведении
комбинатора, когда источник быстрее заданной частоты.

**Что уже придумано:** V1 **буферизует**: `throttle` растягивает элементы
во времени и не теряет ни одного. Это следствие pull-модели — комбинатор
просто не запрашивает следующий элемент, и backpressure распространяется
на источник сам. Ронять элементы в V1 умеет ровно одно место — `Topic`, и
только по своей явной политике `onSlowConsumer`, где потеря видна в
счётчике `dropped`.

**Почему отложено:** «ронять» имеет смысл только там, где источник
неостановим (push-источник без backpressure), а такой источник в
архитектуре ровно один — `Topic`, и у него политика уже есть. Заводить
второй способ терять данные без потребителя — плодить конфигурацию.

**Триггеры возврата:** push-источник вне `Topic`, который нельзя
притормозить (нативный поток внешней библиотеки); либо реальный кейс, где
буфер `throttle` растёт неограниченно — тогда решать: `sample`-семантика
отдельным комбинатором или опция на самом `throttle`.

---

## [2026-07-31] Инлайновый пайплайн в позиции аргумента: `TNeeds` схлопывает вывод

> **Триггер сработал, решения нет** (2026-09-12): сигнатуру конструкторов
> переписывали дважды — change'ами `http-endpoint-two-names` (#68) и
> `http-method-constructors` (#72), — и починку заодно не делали. Остаётся
> отложенным; при следующем касании сигнатуры проверить, воспроизводится ли
> схлопывание на текущих типах.

**Откуда:** change 6 `streaming-v2` — обнаружено при написании
интеграционных тестов транспорта.

**Что уже придумано:** декларация, созданная **прямо в аргументе**
`transport.route(...)`, получает контекстный тип, фиксирующий `TNeeds =
never` (это и есть гарантия «в транспорт едет только исполнимое»). При этом
TypeScript выводит тип-параметр юнита в цепочке `.finally(...)`/`.pre(...)`
из контекстного возвращаемого типа — и он схлопывается в `never`, а
диагностика читается как «Argument of type '(outcome: any) => void' is not
assignable to parameter of type 'never'». Обходной путь тривиален и
совпадает с каноном: пайплайн — отдельное значение
(`const observing = makePipeline().finally(…)`), как `basePipeline` во всех
примерах.

**Почему отложено:** канонический стиль от этого не страдает — пайплайны
переиспользуются константами по построению ([pipeline.md](../design/pipeline.md)),
и во всех примерах и гайдах инлайновой формы нет. Лечение затрагивает
сигнатуру `ITransport.endpoint`/`route` — то есть публичный API транспорта,
только что зафиксированный; цена — сомнительная ради стиля, который мы не
рекомендуем.

**Триггеры возврата:** третий репорт «непонятная ошибка `never` в
декларации» от пользователя; либо ревизия сигнатуры `endpoint()` по другой
причине (например, `serve(dispatch)` в change 10) — тогда починить заодно.

---

## [2026-07-31] Автонаследование бюджета вложенным вызовом через ALS

> **Триггер сработал, решение прежнее** (2026-09-12): `propagate` в
> `transport.nats` (#12) есть, бюджет по сети наследуется. Три довода
> против наследования в процессе не изменились — явная передача
> `{ deadline }` остаётся правилом. Возврат — по второму триггеру: цепочка
> из трёх и более hop'ов в реальном коде.

**Откуда:** change 27 `port-deadline-idempotency` — решение D4, зафиксировано
записью [ideas.md [2026-07-31]](./ideas.md) «Порты: бюджет вызова моментом».

**Что уже придумано:** модель gRPC context — вызов, сделанный внутри
обработчика, подхватывает бюджет вызывающего из ALS автоматически, и явная
передача `{ deadline: Ctx(Deadline).peek() }` не нужна. Поле `deadline` в
`meta` уже есть, переменная `Deadline` уже есть, читалка остатка уже есть:
менять пришлось бы **только правило по умолчанию** в вызывателях — «взять
`meta.deadline`, а при его отсутствии посмотреть в ambient-проекцию».

**Почему отложено:** три довода против. Прецедент `signal` — он сегодня
тоже не наследуется (`meta?.signal ?? NEVER_ABORTED`), и два однородных поля
одного конверта не должны жить по разным законам. Требование
`port-invocation`, что ambient-контекст вызывающего не протекает внутрь
реализации: подхват бюджета из ALS вызывающего — ровно протекание. И
`no runtime magic`: «где-то в глубине оборвалось по бюджету, которого я не
задавал» — худший класс диагностики. Явная передача при этом однострочная,
потому что переменная `Deadline` для того и введена.

**Триггеры возврата:** практика покажет, что явная передача шумна —
например, цепочка из трёх и более hop'ов, где каждый обязан протащить
`{ deadline }`, встречается в реальном коде регулярно; либо появление
`propagate` для ambient-переменных (change 12 `transport.nats`) сделает
наследование через провод штатным, и расхождение «по проводу наследуется, в
процессе нет» станет само по себе сюрпризом.

---

## [2026-07-31] Политика диспатча `balanced` (client-side spill)

**Откуда:** change 12 `transport.nats`. `contracts.md` §3 обещал, что
`balanced` «приедет вместе с настоящей remote-шиной»; шина приехала,
политика — нет, и обещание правится по факту.

**Что уже придумано:** `balanced` — вызов идёт в co-located реализацию,
пока она справляется, и «переливается» на remote-реплики, когда перестаёт.
Единственная политика из трёх, решение которой принимается **на вызове**, а
не на сборке: две другие замыкаются в константу при инстанцировании узла.

**Почему отложено:** решение на вызове требует метрик — насыщенности
локального обработчика, задержек реплик, — которых у транспорта нет и
которые он не должен выдумывать. Заводить политику с решением «по
таймауту» значило бы получить худший вариант обеих: и рантайм-выбор, и
отсутствие данных для него. Плюс `balanced` ломает инвариант «на вызове
выбора не происходит», зафиксированный capability `port-binding`, — а
инвариант этот и есть причина, по которой биндинг вообще читаем.

**Триггеры возврата:** первый реальный кейс, где co-located реализация
насыщается, а remote-реплики простаивают; появление у транспорта источника
метрик (например, вместе с health/readiness из фаз lifecycle) — тогда
станет видно, какие данные политика вправе требовать.

---

## [2026-08-01] `idempotencyKey` и `deadline` по HTTP

> **Сведено сюда** 2026-09-12: пункты записей ideas.md [2026-07-13] «Порты:
> deadline, идемпотентность», [2026-07-31] «Порты: бюджет вызова моментом» и
> [2026-08-01] «Клиенты из контрактов» вычеркнуты со ссылкой сюда. Помимо
> HTTP-слота тема включает `idempotencyKey` у `request`-контрактов
> (ретрай POST-подобных вызовов): форма `MetaOf<C>` выбрана так, что
> расширение вида не ломает call-site.

**Откуда:** change 22 `contract-clients`. У портов оба поля `meta` уже есть
и доезжают через NATS; у HTTP-клиента `deadline` работает **только
локально** (fail-fast до отправки и композиция в `AbortSignal`), а
`idempotencyKey` отсутствует вовсе.

**Что уже придумано:** обоим нужен согласованный слот на проводе и его
чтение серверной границей. Кандидаты очевидны — `Idempotency-Key` (черновик
IETF) и относительный timeout заголовком по gRPC-модели, — но выбрать слот
значит зафиксировать его в стороннем контракте: заголовки читают прокси,
шлюзы и логи.

**Почему отложено:** провезти ключ «каким-нибудь заголовком» значит
договориться с самим собой — обе стороны провода наши, и никакой
совместимости такой выбор не даёт. Пока внешнего потребителя
`command`-контракта нет, слот выбирать не на чем; выбранный наугад он
станет обязательством раньше, чем появится требование.

**Триггеры возврата:** первый внешний потребитель `command`-контракта по
HTTP (тогда становится видно, кто чеканит ключ и кто дедуплицирует);
появление ручки, для которой клиентский бюджет обязан обрезать **работу
сервера**, а не только ожидание клиента.

---

## [2026-08-01] Content-negotiation клиента: не-JSON тела

> **Сведено сюда** 2026-09-12: streaming-клиент (`stream` → NDJSON
> `AsyncIterable<T>`, `events` → SSE с реконнектом по `Last-Event-ID`),
> форма io-декларации stream/events для AsyncAPI и streaming через шину —
> одна тема с этой записью; пункты в ideas.md [2026-07-13] «Типизированные
> клиенты», «Схемы: Standard Schema», «Контракт первичен» и [2026-08-01]
> «Клиенты из контрактов» вычеркнуты со ссылкой сюда.

**Откуда:** change 22 `contract-clients`. `makeClient` говорит только JSON и
отвергает при создании контракт, объявивший `'binary'`/`'text'` в `input`
или `output`, — рядом с потоковыми и multipart-формами.

**Что уже придумано:** тело `'binary'` — это `Content-Type:
application/octet-stream` и `ArrayBuffer` на границе; `'text'` —
`text/plain` и строка. Обе формы уже описаны `mediaTypeOf`, то есть
транспортная сторона правило знает.

**Почему отложено:** непотоковые примитивы соседствуют с потоковыми в одном
вопросе — «что клиент умеет кроме JSON», — и открывать их поштучно значит
трижды возвращаться к сборке запроса и разбору ответа. Дешевле открыть их
вместе со streaming-клиентом, где framing проектируется целиком.

**Триггеры возврата:** проектирование streaming-клиента (NDJSON/SSE) —
первый же его шаг делает не-JSON тела частным случаем; либо ручка, у которой
`'binary'` это весь ответ (выгрузка файла) и обходной путь через обычный
`fetch` перестаёт быть приемлемым.


---

## [2026-08-01] Типизированный успешный статус декларации

> **РЕАЛИЗОВАНО 2026-09-14** — change `typed-success-status` (#84 roadmap).
> Реализовано целиком: успешный статус объявляется полем `status` верхнего
> уровня декларации и конструктора операции, `doc.status` удалён, а `Ok`
> параметризован статусом (`Ok<TValue, TStatus>`). Результат хендлера
> ограничен объявленными исходами, рантайм ставит голому значению
> объявленный статус, а граница заменяет `Ok` со статусом вне множества на
> `internal_error`. Документ печатает ответ на каждый исход, клиент
> валидирует тело схемой пришедшего исхода и отвергает код вне множества.
> Реализация уточнила решение в трёх местах. Первое: одного статуса мало —
> появилась развилка исходов `outputs({ ok: User, accepted: Job })` со
> своей формой у каждой ветки и маркером `none()` для исхода без тела;
> задуманный «статус в типе выхода» её случай («202 со своим телом») не
> закрывал. Второе: статус остался значением декларации, а не выводом из
> сигнатуры хендлера, — генератор и клиент читают значения, а анализа
> типов в проекте нет. Третье: умолчание («`ok` при `output`,
> `no_content` без него») считается одной функцией ядра, и её же читают
> рантайм, документ и клиент — расхождение «документ обещает 204, уходит
> 200» исчезло по построению.
> Дальше остался один вопрос — политика `check()` на недостижимую ветку
> развилки; он записан отдельной темой ниже.

**Откуда:** change 20 `openapi`. Успешный статус ответа объявляется в слоте
`doc.status` (`'CREATED'`, `'NO_CONTENT'`, …), из него генератор выводит код
успешного ответа. Никто не сверяет объявленное с тем, что реально вернул
хендлер: `Ok.created(...)` при `doc: { status: 'OK' }` даст документ,
расходящийся с проводом.

**Что уже придумано:** правильное решение — выразить статус **типом
выхода**, а не полем документации. `Ok.created(...)` и `new Ok(...)` уже
различаются в рантайме (`status` на значении), и различие можно поднять в
тип: `Output<T, E, S extends SuccessStatus = 'OK'>` либо отдельные типы
результата под каждый статус. Тогда `doc.status` перестаёт быть нужен вовсе
— документ читал бы статус из сигнатуры хендлера, как читает схемы.

**Почему отложено:** это трогает `Output`/`Ok` — центральный словарь любого
хендлера, — и потому breaking-изменение фазовой поверхности V1, которое в
change документации не помещается. Ослабление, снижающее цену ожидания:
дефолт (`OK`, а без `output` — `NO_CONTENT`) совпадает с самым частым
случаем, и расхождение видно в первом же вызове ручки.

**Триггеры возврата:** первое расхождение документа с проводом, замеченное
потребителем; либо следующее breaking-окно, если оно откроется по другой
причине — тогда изменение `Output` едет вместе с ним.

---

## [2026-09-14] Политика `check()` на недостижимую ветку развилки

**Откуда:** change `typed-success-status` (#84 roadmap), открытый вопрос его
дизайна. Развилка `outputs({ ok: User, accepted: Job })` объявляет два
исхода, а хендлер может возвращать только один из них. Документ обещает
код, которого клиент не увидит никогда.

**Что уже придумано:** проверка выглядит как политика сборки рядом с
остальными (`check()` в `@nestlingjs/app`): она читает декларации и
сообщает об исходе, который ни одна ветка не возвращает.

**Почему отложено:** статическим анализом такая проверка не решается. Ветку
выбирает тело хендлера, а политики читают значения деклараций, а не типы и
не код. Это вопрос гигиены документа, а не корректности: недостижимый исход
даёт лишний ответ в документе, но не даёт неверного ответа в сети.

**Триггеры возврата:** развилка, применённая на реальном endpoint'е, где
ветка оказалась недостижимой; либо появление в проекте анализа тел
хендлеров по другой причине.

---

## [2026-08-01] Кластерная админ-плоскость подписок

**Откуда:** change 7 `subscriptions-registry`, находка №3 записи
[ideas.md [2026-08-01]](./ideas.md) «Реестр подписок: результат
dogfooding-замера». Реестр node-local: `list()` и `abort()` действуют на
подписки своего процесса. «Покажи все подписки кластера» и «убей подписку
на чужом узле» в V1 невыразимы.

**Что уже придумано:** дело не в реестре, а в **режимах подписки шины**.
Сегодня их два, и оба однополучательные по построению: `request`/`command`
имеют ровно одного владельца, `event` в split-топологии доставляется по
queue-group имени подписчика — то есть одной реплике группы. Нужен третий:
широковещательная подписка **без** группы (каждая реплика получает свою
копию) плюс сбор ответов с окном ожидания (scatter-gather), потому что у
админского запроса ответов столько, сколько узлов, и заранее это число
неизвестно. Наблюдение при этом уже работает кластерно: факты
`subscriptions.opened`/`closed` несут имя узла, и приёмник фактов со
стораджем собирает картину всего кластера без единой новой возможности.

**Почему отложено:** третий режим подписки — это изменение модели доставки,
а не фича одного пакета: он тянет за собой семантику ответа (сколько копий
ждать и сколько ждать по времени), поведение при разъезде реплик и вопрос,
что считать успехом такого вызова. Имитировать его `request`-контрактом
внутри пакета отвергнуто прямо: админ получал бы список подписок случайной
реплики, а молчаливо неверный ответ хуже отсутствующей возможности.

**Триггеры возврата:** первое эксплуатационное требование «убей подписку,
не зная узла» (обходной путь — kill через тот узел, который её показал,
перестаёт работать за балансировщиком без sticky-маршрутизации); либо
появление второго потребителя broadcast-подписки на шине — тогда режим
проектируется под оба сразу, а не под админ-плоскость одну.

---

## [2026-09-01] Права на конфиг: `Config(key)` читает любой ключ

**Откуда:** change `remove-module-exports` — удаление `Module.exports` и опции
`strictExports` ([ideas.md](./ideas.md), запись [2026-09-01] «Модуль без
`exports`»). Тема всплыла как аргумент против проверки: одна из дыр, которую
она не закрывала.

**Что уже придумано:** сегодня секция конфига защищена приватностью токена.
Токен секции держат внутри модуля, наружу отдают только `.keys` — тогда чужой
инжект `Config<OrdersConfig>` нечем написать ([config.md](../design/config.md)).
Но `Config(key)` — семейство с параметром-строкой, и любой провайдер может
запросить любой ключ по имени, включая секретный: имя ключа не нужно
импортировать. `isSecretKey` затирает значение только при печати, а не при
чтении. Проверка `strictExports` этого не видела: ключи конфига — члены
семейства, а не токены модуля, и владелец у них тот же для всех.

**Почему отложено:** это модель прав на конфиг, а не границы модулей. Она
требует своего вопроса — кому разрешено читать ключ и по какому признаку это
проверять на сборке, — и своего ответа, который не сводится к списку экспортов
модуля. Тащить её в change про удаление поля значило бы решать две задачи
одним механизмом, чего этот change как раз и избегает.

**Триггеры возврата:** первое требование «этот ключ читает только вот этот
модуль», приходящее не из вкуса, а из аудита или регламента; либо инцидент, где
секрет утёк через `Config(key)` из чужого кода.

---

## [2026-09-03] Конструктор HTTP-декларации с методом в имени: `httpEndpoint.get(path, …)`

> **РЕАЛИЗОВАНО 2026-09-12** — change `http-method-constructors`
> (#72 roadmap).
> Реализовано целиком: `httpEndpoint` перестал быть функцией и стал
> значением-пространством имён с шестью конструкторами по HTTP-методу и
> статиком `.implement`; словарь потерял `method` и `path`, путь пришёл
> первым аргументом. Симметрия CLI сделана тем же change'ем:
> `cliEndpoint('users:list', { … })`, поле `command` из словаря удалено.
> Открытый вопрос о секции `http:` операции закрыт решением «не менять»:
> `http: 'GET /users/:id'` — данные операции, а не конструктор.
> Реализация уточнила две вещи. Первая: статиков шесть, а не восемь —
> `OPTIONS` и `TRACE` входят в union `HttpMethod`, но декларациями не
> адресуются, их обслуживает маршрутизатор транспорта. Вторая: тип
> конструктора объявлен один раз интерфейсом с двумя сигнатурами вызова,
> поэтому цена формы в бюджете типов — +0.13 % инстанциаций, а диагностика
> по-прежнему называет обе формы `handler`.

**Откуда:** ревью гайда (главы 1–5), замечание к главе 1: форма
`httpEndpoint({ method, path, … })` могла бы принимать метод и путь
позиционно, либо метод мог бы стать методом на объекте.

**Что уже придумано:** из двух форм лучше вторая:
`httpEndpoint.get('/users/:id', { input, output, errors, handler })`, по
одному статическому методу на HTTP-метод. Типизация path-параметров от
шаблона сохраняется: путь остаётся строковым литералом в первом аргументе.
Позиционная форма `httpEndpoint('GET', '/health', …)` хуже: две голые
строки, и метод остаётся строкой. Симметрия для CLI:
`cliEndpoint('users:list', …)`. Открытый вопрос: биндинг операции
`http: 'GET /users/:id'` и `http: { method, path }` это данные, а не
конструктор; менять ли его синхронно.

**Почему отложено:** изменение синтаксическое, модель декларации не
трогает. Решать его разумно вместе с формой поля `handler`, чтобы
декларация менялась один раз.

**Триггеры возврата:** change по форме `handler`; либо переписывание
главы 1 гайда под новую композицию, если к тому моменту форма ещё не
решена.

**Триггер сработал 2026-09-12.** Change
[68 `http-endpoint-two-names`](./roadmap.md) развёл формы по двум именам
и сделал `httpEndpoint` пространством имён со статиком `.implement`, то
есть форма `handler` решена. Статики по HTTP-методу вынесены отдельной
строкой [72 `http-method-constructors`](./roadmap.md): множества мест не
пересекаются — 39 против ~305, — и ни один call site не переезжает
дважды. Открытый вопрос о секции `http:` операции остаётся за 72: это
данные, и их форма от конструктора не зависит.

---

## [2026-09-03] Политики сборки: проверка или гарантия

**Откуда:** ревью гайда (главы 1–5), замечание к главе 1: «всё, что
касается policies, очень спорно».

**Что уже придумано:** политика `everyEndpoint(…).hasLayer(…)` проверяет
инвариант на собранном графе, но не обеспечивает его: слой композирует
автор каждого endpoint'а, а исключение помечается `detached: '<причина>'`.
Альтернатива, которая даёт гарантию: слой на транспорте,
`http({ pipeline: observability })`. Транспорт приставляет его каждому
своему endpoint'у, и забыть слой нельзя. Это соответствует принципу
«guarantee over convention», но противоречит записи [2026-07-14]
«Policy-check на собранном графе»: слой всегда виден в декларации
endpoint'а. Компромисс: слой транспорта для инфраструктурных слоёв
(наблюдаемость), политики как opt-in для инвариантов вида «всё, что
меняет данные, за `authed`». Тогда `detached` нужен реже, и вопрос его
имени решается вместе с этим.

**Почему отложено:** это пересмотр записи [2026-07-14], и ему нужна своя
дискуссия в `history/discussions/`. Для гайда достаточно убрать политики
из первых глав.

**Триггеры возврата:** отдельная сессия по политикам; либо первый
инфраструктурный слой, который обязан стоять на каждом endpoint'е и
который забыли композировать не в тесте, а в проде.

---

## [2026-09-12] Шаблон проекта: `nestling-agent-skill --template` или `create-nestling-app`

**Откуда:** первый внешний прогон
([d/14](../history/discussions/14-first-external-run.md), пункт A8): первые
минуты сессии ушли на разведку структуры проекта, которую скилл описывает
словами в разделе «Service skeleton», но не даёт файлами.

**Что уже придумано:** флаг у существующей команды `nestling-agent-skill`
или отдельный пакет `create-nestling-app`. Команда кладёт `package.json`,
`tsconfig.json`, `src/main.ts`, `src/app.ts`, `src/app.config.ts` и один
endpoint. Файлы шаблона могут быть теми же сниппетами скилла: они уже
компилируются против настоящих пакетов.

**Почему отложено:** шаблон требует решения о сборщике. Примеры
репозитория собираются esbuild и запускаются `tsx`, внешнему проекту
достаточно `tsc`, и выбирать один путь за пользователя рано. Большую часть
потребности закрывает `references/setup.md` из change'а `skill-setup`
(#66 roadmap): tsconfig, скрипты и команда запуска в нём есть текстом.

**Триггеры возврата:** второй внешний прогон, в котором разведка структуры
снова занимает заметное время при наличии `setup.md`; либо решение о
сборщике для внешних проектов.

---

## [2026-09-12] Один пакет в импортах endpoint-файла

**Откуда:** обзор [d/13 §4.2](../history/discussions/13-framework-review-3.md).
`create-user.endpoint.ts` в `examples/users-service` импортирует из пяти
пакетов: `@nestlingjs/app`, `container`, `operations`, `outbox`,
`transport.http`. Укрупнение монорепы (запись [ideas.md [2026-09-05]](./ideas.md)
«Раскладка монорепы») число не уменьшило.

**Что уже придумано:**

- Реэкспорт из `@nestlingjs/app` всего, что нужно endpoint-файлу:
  `@Component` из `container`, `makeOperation` из `operations`,
  `httpEndpoint` из `transport.http`. Один импорт для приложения, пакеты
  остаются для тех, кто собирает своё.
- Мета-пакет `nestling` с барелем поверх ядра и транспорта HTTP.

**Почему отложено:** число пакетов в импортах — цена явности границ, а не
дефект. Каждый пакет отвечает за свою границу: контейнер, операции,
транспорт, outbox. Импорт называет её. Реэкспорт из `app` нарушил бы
правило «барель перечисляет публичные имена поимённо» (запись
[ideas.md [2026-09-09]](./ideas.md)): у `app` появились бы имена, которые он не
определяет, и автоимпорт редактора предлагал бы `Ok` из двух мест — то
самое, на что жаловался обзор.

**Триггеры возврата:**

- второй внешний прогон, где число импортов названо помехой само по себе, а
  не через автоимпорт;
- появление мета-пакета по другой причине — например, шаблон проекта из
  записи [2026-09-12] «Шаблон проекта» выше.

---

## [2026-09-12] Снапшот схем контрактов: файл в репозитории или registry

**Откуда:** записи [ideas.md [2026-07-13]](./ideas.md) «Порты: deadline,
идемпотентность, версионирование контрактов» и [ideas.md [2026-07-31]](./ideas.md)
«Версионирование контрактов: снапшот из дискавери, вердикт по слоту». Вопрос
стоял открытым в обеих и сведён сюда 2026-09-12.

**Что уже придумано:** `contract-versioning` (#26) снимает снапшот схем из
дискавери и сравнивает его с baseline; baseline приходит **значением**, и
кто его читает — файл в репозитории, внешний registry, артефакт CI — решает
пользователь. Форма API выбрана так, что ответ на вопрос не меняет ни одной
сигнатуры.

**Почему отложено:** у одного сервиса baseline — файл рядом с кодом, и этого
хватает. Registry нужен, когда снапшоты сравнивают между командами или
между релизами нескольких сервисов, а такого потребителя нет.

**Триггеры возврата:**

- второй сервис или команда, которым нужен общий baseline контрактов;
- проверка совместимости в CI между релизами, где файл в репозитории
  перестаёт быть источником истины.

## [2026-09-13] Фабрики пакетов: `make*` или голые существительные

> **Триггер сработал** 2026-09-14: запись [ideas.md [2026-09-14]](./ideas.md) «Именование: вид называет часть речи, `make` у фабрики плагина». Фабрика, возвращающая плагин, называется `make*`; фабрика транспорта и сервера остаётся существительным. Граница проведена по виду результата.

**Откуда:** разбор фидбэка по коду ([d/15](../history/discussions/15-code-feedback.md),
п. 16–18), запись [ideas.md [2026-09-13]](./ideas.md) «Именование: суффиксы
`Plugin` и `Layer`, переключатели предикатом».

**Что уже придумано:** ядро строит декларации фабриками `make*` (`makePlugin`,
`makeSwitch`, `makeConfig`, `makeToken`, `makeApp`), а пакеты экспортируют
фабрики голыми существительными: `subscriptions()`, `openapi()`, `http()`,
`mcp()`. Существительное занимает имя, которое нужно приложению под
экземпляр; `makeSubscriptions({...})` освободило бы его. Различие по смыслу
есть: `make*` ядра строит декларацию из значений, фабрика пакета — экземпляр
плагина или транспорта из опций.

**Почему отложено:** причиной был занятый идентификатор, и суффикс `Plugin` у
экземпляра закрыл её локально (`subscriptionsPlugin = subscriptions({...})`).
Остаётся вопрос консистентности API пакетов, у которого нет читателя,
которому он мешает.

**Триггеры возврата:** новый пакет, чья фабрика не ложится ни в `make*`, ни в
существительное; или правило линтера для имён экспортов пакетов, которому
нужна одна форма.

## [2026-09-13] Выгрузка графа контейнера из собранного приложения

**Откуда:** change `examples-rework`. Пример `modular-app` выгружает граф
для [`@nestlingjs/viz`](../../packages/nestling.viz/) скриптом
`src/graph.ts`, и взять контейнер ему не у кого: у `AssembledApp` доступа
к графу нет, а `check()` строит контейнер и не отдаёт его.

**Что уже придумано:** скрипт идёт через шов `@nestlingjs/app/testing` —
`wireApp(app, { args })` проводит приложение по фазам до WIRE и отдаёт
`container`, у которого есть `toJSON()`. Условие экспорта `testing`
включается флагом запуска (`tsx --conditions=testing`), брокер
подменяется двойником, а база нужна настоящая: пул открывается на фазе
INIT, и граф известен только после неё.

**Почему отложено:** шов существует ради тестов, и выгрузка графа —
работа того же времени разработки. Боевой двери к контейнеру у
приложения нет намеренно: узел графа берут DI-токеном изнутри, а не
снаружи. Заводить ради визуализации публичный метод — расширять границу
ядра под инструмент.

**Триггеры возврата:**

- визуализация понадобилась в бою — например, странице диагностики
  работающего процесса;
- у `check()` появится второй потребитель, которому нужен граф, а не
  отчёт о составе.

---
## [2026-09-13] Serverless: транспорт или ограничивающий вид приложения

**Откуда:** обсуждение совместимости 2026-09-13, запись
[ideas.md [2026-09-13]](./ideas.md) «Совместимость: справочник
`compatibility.md`, WebSocket и gRPC строками плана».

**Что уже придумано:** от обычного запуска serverless отличается одним —
слушающего сокета нет. Сокет из корня уже вынесен: `server()` это ресурс,
транспорт ссылается на него полем, а `run()` открывает сокеты последним
шагом START отдельным циклом. Транспорт, который сервера не объявляет, в
этот цикл ничего не даёт.

Форма пакета видна целиком. Прецедент однократного запуска — CLI-транспорт
в режиме `'argv'`: `serve(dispatch, signal)` исполняет команду через
`dispatch.call` и возвращается, своей копии исполнения у транспорта нет.
FaaS-транспорт устроен так же, отличий два: вход не `process.argv`, а
событие платформы, и вызовов много. Транспорт хранит `dispatch`,
полученный в `serve`, а экспортируемый обработчик дожидается `run()` и
зовёт `dispatch.call`. Ядро при этом не трогается.

Ограничения платформы выражаются тем, что уже есть. Обработчик за шлюзом
не отдаёт SSE и NDJSON; транспорт, который не объявил потоковые формы в
`capabilities`, роняет сборку на endpoint'е с `stream(T)` в выводе, а не
ломается на запросе.

Главное ограничение платформы — заморозка процесса между вызовами. Фоновая
работа на ней не идёт: relay `@nestlingjs/outbox`, sweeper
`@nestlingjs/inbox`, реестр подписок, обновление reloadable-секций. Это
вопрос состава плагинов, а не формы корня, и проверяется политикой сборки:
состав с фоновым узлом под FaaS-транспортом отвергается на ASSEMBLE.

Если второй вид приложения всё-таки понадобится, он будет **ограничивающим**:
запрещает фичи, фоновые узлы и пробы, а не повторяет composition root в
другой форме. Второй корень, который дублирует прогрессию L0–L4, приносит
вторую модель жизненного цикла рядом с первой — от этого фреймворк уже
отказался, отвергая Fastify под капотом ([ideas.md [2026-09-04]](./ideas.md)
«Граница `@nestling/transport.http`»).

**Почему отложено:** потребителя нет, в FaaS Nestling никто не
разворачивал. Нет и замера холодного старта, а он определяет разговор:
`build()` синхронен и ввода-вывода не делает, поэтому цена сидит в фазе 0
(чтение конфига) и фазе 2 (захват ресурсов). Пока цифры нет, форму
обсуждать рано.

**Триггеры возврата:**

- развёртывание приложения в FaaS, своё или чужое;
- замер холодного старта на `examples/app-with-http`, если он покажет
  цену, которую не снять составом плагинов;
- второй HTTP-адаптер на `Request` и `Response`: он даёт вход платформам,
  у которых `node:http` нет.

---

## [2026-09-14] Метрики: вид `gauge`, политика на каталог, перечень атрибута из конфига

**Откуда:** change `declared-metrics` (#101 roadmap): метрика стала
декларацией-значением, каталог собирается на сборке, накопленное держит
`MetricsStore$`. Три темы обсуждались вместе с ним и вынесены за его
границу.

**Вид `gauge`.** Store хранит счётчики и гистограммы: у обоих запись —
это событие, и накопленное складывается. Текущее значение устроено иначе:
его не пишут, а читают в момент снятия, поэтому `gauge` — это либо
обратный вызов на снимке (`ObservableGauge` в модели OpenTelemetry), либо
значение с правом убывать. Первое добавляет в store чужой код на пути
снимка, второе ломает правило «счётчик только растёт», на котором держится
дельта между двумя снятиями у сборщика. Решать это стоит на живом случае,
а он один и тот же: размер очереди, число открытых подписок, глубина пула.
**Триггер:** прикладной случай, где значение нельзя выразить счётчиком
(«взято» минус «отдано»), и понятно, кто его читает — снимок или поток.

**Политика сборки на каталог (`everyMetric(…)`).** Каталог известен до
INIT, поэтому «у каждой метрики есть `help`», «открытых атрибутов нет»
или «рядов не больше тысячи» проверяются на сборке тем же механизмом, что
`everyEndpoint(…)`. Отложено, потому что предмет политики ещё не устоялся:
порог рядов не выбран (на метрику или на каталог), а запрет `open` нужен
не всякому приложению. **Триггер:** каталог, выросший до сотен рядов на
одном приложении, или требование аудита «описание у каждой метрики».

**Множество значений атрибута из конфига (`from(Config, …)`).** Перечень
значений атрибута сегодня пишется в объявлении литералом. Случай «значения
приходят из секции конфига» — список очередей, список арендаторов —
покрывает `open`: ряд заводится по факту записи, и нулей у него нет.
**Триггер:** нужда в нулевых рядах для значений, которых в этой сборке
может не быть ни одной записи, и подтверждение, что список стабилен между
перезапусками.

**Поток записей store (`tap(sink)`).** Выход проектировался вторым рядом
со снимком: получатель подписывается на записи и получает стартовым
состоянием текущий снимок. Он был написан и снят по ходу того же
change'а: единственный ожидаемый потребитель — push по OTLP в сателлите
`@nestlingjs/otel` — собрал точки из снимка по таймеру, потому что
протокол принимает накопленные значения, а не приращения, и подписчик
потока складывал бы те же ряды второй раз рядом со store. Что уже
продумано: изоляция исключения подписчика с записью в логгер ядра и
стартовое состояние снимком, иначе записи фаз INIT и START теряются.
**Триггер:** получатель, которому важен момент записи, а не состояние:
exemplars со связью записи и трассы, сэмплирование, отправка события в
шину по факту записи.

---

## [2026-09-14] Участок трассы на вызов порта

**Откуда:** change `otel-satellite` (#85 roadmap): участок пишет слой
пайплайна, и у endpoint'а он есть. У вызова порта пайплайна нет, поэтому
своего участка вызов не получает.

Дерево от этого не рвётся. Вызыватель везёт трассу в конверте сообщения,
получатель продолжает её своим `withTracing()`, и участок его endpoint'а
встаёт ребёнком участка вызывателя. Пропадает промежуточное звено — время,
проведённое в сети и в очереди брокера. Его показывает пара метрик ядра:
разность `nestling.port.duration` и `nestling.request.duration` и есть
накладные расходы доставки.

Что уже продумано: участок вызова пришлось бы открывать в узле порта, а
закрывать после ответа, и идентификатор для него ядро сейчас не минтит —
`spanId` создаётся на запрос, а не на вызов. Значит, понадобилась бы
вторая точка создания идентификаторов и способ увезти её соседу вместо
текущего `spanId`.

**Триггер:** разность метрик перестала отвечать на вопрос «где запрос
провёл время» — например, в одном обработчике несколько вызовов, и нужно
разложить задержку по конкретным.

---

## [2026-09-14] Наблюдение за секретом Vault: опрос против сигнала извне

**Откуда:** change `config-source-needs` (#92 roadmap): пакет
`@nestlingjs/config.vault` выпущен без `watch`. Секрет читается один раз
на фазе 0 и живёт в снимке, поэтому ротация доходит до процесса
перезапуском.

Что уже продумано. Способа два, и они разные по цене. **Опрос** —
источник дёргает тот же путь KV раз в интервал и зовёт `notify()`, когда
рекорд изменился: работает с любым Vault, но стоит запроса в интервал на
каждый процесс, а задержка равна интервалу. **Сигнал извне** — процессу
говорят «перечитай»: SIGHUP, админский endpoint операций или событие
шины. Запросов не стоит и срабатывает сразу, но требует того, кто пошлёт
сигнал, — оператора или sidecar, который следит за Vault сам.

Механика перепроекции при этом уже есть: `watch(notify)` у источника и
перечитывание reloadable-секций читалкой работают с change'а
`config-run-bind`. Недостающая часть — только решение, кто дёргает
`notify`.

Смежная граница: динамические секреты Vault с арендой. Там мало
перечитать значение — аренду нужно продлевать, иначе секрет протухнет по
таймеру сервера. Это другая работа, и она сюда не входит.

**Триггер:** ротация секрета, которую нельзя пережидать до перезапуска, —
или первое приложение, которому нужен динамический секрет с арендой.
