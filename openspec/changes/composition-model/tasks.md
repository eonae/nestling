## 1. Контейнер: модуль

- [x] 1.1 Убрать `endpoints` из всех путей контейнера; интерфейс `Module` — `{ name, providers, dependsOn }` (`packages/nestling.container/src/modules/modules.ts`)
- [x] 1.2 Переименовать `Module.imports` в `dependsOn`; обновить `registerModule` и JSDoc про обход (`builder/container.builder.ts`)
- [x] 1.3 Обновить тексты ошибок, где упоминаются `imports` и endpoint'ы модуля (коллизия имён, обход)
- [x] 1.4 `yarn workspace @nestling/container test` зелёный

## 2. Контейнер: объектные токены

- [x] 2.1 Заменить `TokenString` на объектный токен: уникальная ссылка, поле `id`, поле типа-фантома (`src/common.ts`)
- [x] 2.2 `makeToken(id)` возвращает новый объект на каждый вызов; повторный вызов с тем же `id` даёт другой токен — задокументировать
- [x] 2.3 Класс как токен опознаётся по ссылке; `constructor.name` только для отображения (`stringifyToken`, `injectable.decorator.ts`)
- [x] 2.4 Перевести карты билдера на ключ-ссылку; `id` использовать только для отчётов и ошибок
- [x] 2.5 Добавить проверку «два токена с одинаковым `id`» — предупреждение о неоднозначности отчётов
- [x] 2.6 Обновить `toJSON()` и `explain()`: узел идентифицируется `id`
- [x] 2.7 Решить, нужен ли `getById(id)` на собранном контейнере для интроспекции по строке из отчёта

## 3. Контейнер: семейства токенов

- [x] 3.1 Член семейства хранит `family` и `param` полями; убрать разбор строки в `lookupFamilyMember` (шесть мест в билдере и в `@nestling/ports`)
- [x] 3.2 `.auto` и `.all` — выделенные токены вместо зарезервированных параметров `{auto}` и `{all}`
- [x] 3.3 Мемоизация члена по параметру становится обязательной частью контракта семейства
- [x] 3.4 Убрать `suggestFamilyForToken` или переписать на структурную проверку

## 4. Слой приложения: фича и плагин

- [x] 4.1 Ввести типы `Feature` и `Plugin`: `{ name, endpoints?, dependsOn? }` плюс union `providers | modules`
- [x] 4.2 Ввести два конструктора: `makeFeature` и `makePlugin`; роль читается в точке объявления, а не только по полю корня
- [x] 4.3 `dependsOn` у фичи отсутствует; у плагина ссылается только на плагины
- [x] 4.4 Удалить `makeAppModule`, `AppModule`, `module.ts` и `module.spec.ts` из `@nestling/app`
- [x] 4.5 Удалить `modulesOf` и прежний `makeFeature`; дедупликация в замене — по ссылке, с ошибкой на одноимённые разные значения
- [x] 4.6 Правило «имя плагина = имя npm-пакета» — проверка на сборке или запись в документации

## 5. Композиционный корень

- [x] 5.1 Переименовать поле `modules:` в `plugins:` (`AssemblySpec`, `makePlan`)
- [x] 5.2 Добавить объектную форму `select: { features, includeDeps }`; строковая форма сохраняется
- [x] 5.3 Реализовать замыкание `includeDeps` по операциям вида `request` и `command`
- [x] 5.4 Добавить `intercom:` — ссылка на объявленный транспорт с требованием `IMessageBus`
- [x] 5.5 Ветку «шину поставил корень» перевести с поля `transports:` на назначение роли

## 6. Транспорты

- [x] 6.1 Перевести токены транспортов на семейство: `HttpTransport('default')`, `HttpTransport('admin')`
- [x] 6.2 Добавить `name` в опции конструкторов транспортов
- [x] 6.3 Добавить `on:` в декларацию endpoint'а; умолчание — `'default'`
- [x] 6.4 Обновить проверку «транспорт требуется, но не зарегистрирован» под именованные экземпляры

## 7. Операции

- [ ] 7.1 Заменить `makeContract({ kind })` на `makeRequest`, `makeCommand`, `makeEvent`
- [ ] 7.2 Перевести правила видов в типы: нет `output` и `errors` у события, нет `durable` у запроса, `subscriber` обязателен у события
- [ ] 7.3 Переименовать `.port` в `.caller` во всех пакетах, примерах и текстах
- [ ] 7.4 Убрать слово «контракт» из публичных имён и документации; в `docs/glossary.md` — «операция»

## 8. Discovery и граница фич

- [x] 8.1 Переписать `discoverEndpoints` на плоский проход по `features` и `plugins`
- [x] 8.2 Построить карту «модуль → фича-владелец» из состава
- [x] 8.3 Добавить проверку «модуль достижим из двух фич — обязан быть плагином»
- [x] 8.4 Добавить проверку рёбер графа: фича → фича запрещено, фича → плагин разрешено
- [x] 8.5 Запретить ребро плагин → фича: инфраструктура не зависит от бизнес-логики

## 9. Диагностика

- [ ] 9.1 Добавить карту операций в отчёт `check()`: реализовано здесь, уходит наружу, через какой интерком
- [ ] 9.2 Добавить предупреждение «интерком объявлен, операций нет»
- [ ] 9.3 Обновить текст ошибки достижимости под `intercom:`
- [ ] 9.4 Убрать из ошибки про недостающий транспорт совет класть шину в `providers:` модуля
- [x] 9.5 При `includeDeps: true` печатать фактический состав

## 10. Примеры

- [x] 10.1 `examples.simple-app` — модули без `endpoints`, `dependsOn` вместо `imports`
- [x] 10.2 `examples.app-with-http` — фичи и плагины, `plugins:` в корне, `.caller`, три конструктора операций
- [x] 10.3 `examples.split-nats` — `intercom:` вместо шины в `transports:`
- [x] 10.4 Проверить, что ни один пример не нарушает границу фич

## 11. Документация

- [ ] 11.1 `docs/design/container.md` — модуль без `endpoints`, `dependsOn`, объектные токены
- [ ] 11.2 `docs/design/composition.md` — три роли, `plugins:`, `select`, `intercom:`
- [ ] 11.3 `docs/design/contracts.md` и `endpoints.md` — три конструктора операций, `.caller`, endpoint как граница перевода
- [ ] 11.4 `docs/design/transports.md` — именованные экземпляры, роль интеркома
- [ ] 11.5 `docs/glossary.md` — «операция», «фича», «плагин», «интерком»; убрать «контракт» и «порт» в прежнем значении
- [ ] 11.6 README затронутых пакетов и плашки статуса
- [ ] 11.7 Гайды пересверены с примерами, дата в плашке «сверено с кодом» обновлена
- [ ] 11.8 `yarn docs:preview`

## 12. Журнал решений

- [ ] 12.1 Запись в `docs/decisions/ideas.md`: критерий фичи и плагина через границу процесса; почему модуль остаётся в контейнере; почему у фичи нет `dependsOn`
- [ ] 12.2 В той же записи — отвергнутые варианты из `design.md`
- [ ] 12.3 Пометить суперсид в записях про `makeAppModule` и про отсутствие примитива «плагин»
- [ ] 12.4 Обновить статус в `docs/decisions/roadmap.md`

## 13. Definition of Done

- [ ] 13.1 Все задачи выше отмечены
- [ ] 13.2 `yarn verify` зелёный (build + typecheck + lint + test + type-budget)
- [ ] 13.3 README затронутых пакетов обновлены, включая плашки статуса
- [ ] 13.4 `design/` и `decisions/` синхронизированы по правилам CLAUDE.md
- [ ] 13.5 `yarn docs:audit` — 0 ERROR
- [ ] 13.6 Примеры мигрированы, гайды пересверены с обновлённой датой
- [ ] 13.7 Линтер стиля по изменённым текстам — 0 запрещённых слов
- [ ] 13.8 Коммиты осмысленные, ветка запушена
